# Name: recorded (design/roadmap/39-repository-structure.md's "Not everything in `user/` is a
# service", performed by milestone 175). The programs that exist to exercise or measure the system
# rather than to be shipped: test clients, attackers that share the honest path, stand-in servers,
# workloads and benchmarks. 39 named eight of them by hand on a tree less than half this size and
# called mixing them with the real components "much of why the directory reads as shapeless"; this
# is that separation. `tests/` was not available and would have been wrong anyway, since Cargo owns
# that directory name and these are `[[bin]]` targets rather than test harnesses. Nobody has put
# the name to calef.

[package]
name = "fixtures"
version = "0.1.0"
edition = "2024"
license.workspace = true

[[bin]]
name = "hello"
path = "src/hello.rs"
# This is a bare-metal `no_std` binary with no unit tests, and the default test/bench targets link
# libtest (`extern crate test`), which does not exist for the aarch64-unknown-none target. Turning
# them off is what lets `cargo clippy --all-targets` (and any --all-targets build) work here instead
# of failing to find crate `test`. The kernel keeps its test target because it supplies its own
# harness via `custom_test_frameworks`.
test = false
bench = false

# The memory grant depleter (milestone 31): spends exactly the untyped budget `--mem N` grants it
# and reports how many pages it could map. The program that makes the memory grant real, not
# parsed-and-ignored.
[[bin]]
name = "memory_grant_depleter"
path = "src/memory_grant_depleter.rs"
test = false
bench = false

# The network echo client (milestone 590 (the booted system starts its network stack)): the
# prompt's proof that the booted system has
# a network. Connects to the QEMU runners' echo peer through the `net_stack` the progenitor built
# and prints what came back.
[[bin]]
name = "network_echo_client"
path = "src/network_echo_client.rs"
test = false
bench = false

# The unreachable network witness (milestone 590, provisional): declares no network, tries to use
# one anyway, and prints the kernel's refusal. The negative control for the echo client.
[[bin]]
name = "unreachable_network_witness"
path = "src/unreachable_network_witness.rs"
test = false
bench = false

# The greeting (milestone 198 (a package manager) rung 3a, name provisional): a program no boot
# image carries. It reaches a machine only as a package (`packages/greeting.recipe`), which is what
# lets `script/swish-check` install and run something the image has never seen. Listed in
# `packaged_only` below, so no archive packs it.
[[bin]]
name = "greeting"
path = "src/greeting.rs"
test = false
bench = false

# The interrupt_heeder (DECISIONS §24): a long-running job that heeds the cooperative ^C,
# polling a shared flag between work units and exiting cleanly when the shell sets it. The
# cooperative interrupt tier.
[[bin]]
name = "interrupt_heeder"
path = "src/interrupt_heeder.rs"
test = false
bench = false

# The interrupt_ignorer (DECISIONS §24): a runaway that ignores the interrupt; only the forcible
# tier (the shell tearing its region down) ends it. The case the second ^C exists for.
# The soaker (milestone 219): the workload that does not stop, so that `design/fatal-risks.md`'s
# multicore entry has something to run its decisive experiment against. Cross-core IPC round trips
# forever, publishing its progress in a page the kernel reads.
[[bin]]
name = "soaker"
path = "src/soaker.rs"
test = false
bench = false

# The multi-tasking workload's task (milestone 168): one EL0 process running the job mix
# `--features job_mix` sweeps. In every archive, because the instrument develops under QEMU and the
# number is taken on a board, and both have to run the same workload.
[[bin]]
name = "job_mix_task"
path = "src/job_mix_task.rs"
test = false
bench = false

[[bin]]
name = "interrupt_ignorer"
path = "src/interrupt_ignorer.rs"
test = false
bench = false

# The outlaw (milestone 19, the user-test port): reads an address it is not allowed to touch, and
# round-trips through user mode. The two programs kernel::user::tests used to hand-assemble as
# aarch64 machine code; a real ELF here is what lets those tests run on both ISAs.
[[bin]]
name = "outlaw"
path = "src/outlaw.rs"
test = false
bench = false

# The CoreMark workload (milestone 19e): the first real compute workload, run against the native ABI.
[[bin]]
name = "coremark"
path = "src/coremark.rs"
test = false
bench = false

# The allocator workload (milestone 27): the first program that links `extern crate alloc`, over
# the untyped-backed heap in user_mode_runtime. Proves Vec/String/BTreeMap churn, free-in-any-order, and
# reuse-without-growth against a real budget, on both ISAs.
[[bin]]
name = "allocator_exerciser"
path = "src/allocator_exerciser.rs"
test = false
bench = false

# The FS-service client (milestone 32 phase 2): opens a file through a granted directory capability,
# reads it, writes and reads back a pattern, and reports. initrd_aarch64 packs it as "fs_test_client".
[[bin]]
name = "fs_test_client"
path = "src/fs_test_client.rs"
test = false
bench = false

# The EL0 microbenchmark program (primitive suite): self-times syscall/IPC primitives from EL0, the
# lmbench way, so the numbers include the trap the kernel-side benchmarks skip. Run by the bench boot.
[[bin]]
name = "os_primitives_benchmarker"
path = "src/os_primitives_benchmarker.rs"
test = false
bench = false

[[bin]]
name = "flaky"
path = "src/flaky.rs"
test = false
bench = false

# The C seam (milestone 36). Three files, two binaries: `c_shim` is the Rust user_mode_runtime shell that
# links fixtures/c/c_seam.c (compiled by build.rs with a bare-metal clang) and holds every capability
# the foreign component is not allowed to see; `c_confiner` builds it, supervises it, and holds the
# witness pages that prove what the C could not reach. Both portable; both archives carry both.
[[bin]]
name = "c_shim"
path = "src/c_shim.rs"
test = false
bench = false

[[bin]]
name = "c_confiner"
path = "src/c_confiner.rs"
test = false
bench = false

# The client that draws (milestone 29): paints a coordinate-derived pattern into a shared surface and
# asks the driver to show it. Holds an endpoint and the pixels; no device, no DMA authority. The
# separation is the increment's point, so it is its own binary rather than a role of display.
[[bin]]
name = "painter"
path = "src/painter.rs"
test = false
bench = false

# A window: a client of the compositor (milestone 33). One binary, several roles, because an attacker
# that shares the honest path is a fair test of the boundary rather than a different program failing
# for its own reasons. Packed as "window".
[[bin]]
name = "window"
path = "src/window.rs"
test = false
bench = false

[[bin]]
name = "rust_swappable"
path = "src/rust_swappable.rs"
test = false
bench = false

[[bin]]
name = "c_swappable"
path = "src/c_swappable.rs"
test = false
bench = false

[[bin]]
name = "chatty"
path = "src/chatty.rs"
test = false
bench = false

# The credential service's clients (milestone 56): the provisioner that fills the store, the honest
# client that asks questions, and the attacker that holds the identical endowment and tries to write
# it. One binary, three roles, because an attacker on the honest path is a fairer test of a boundary
# than a different program failing for its own reasons. `initrd_aarch64` packs it as "credentialer_test_client".
[[bin]]
name = "credentialer_test_client"
path = "src/credentialer_test_client.rs"
test = false
bench = false

# The login service's test client (milestone 49, provisional name): presents an identity and a
# secret, and on `login_protocol::OK` receives the caretaker's endpoint, the file service's shared
# frame, and a budget, then uses all three to prove they work rather than merely that they arrived.
# `initrd_aarch64` packs it as "login_test_client" for both ISAs.
[[bin]]
name = "login_test_client"
path = "src/login_test_client.rs"
test = false
bench = false

# The sink contract's ends (milestone 50), three programs since milestone 292: a program that
# writes to whatever is in its output slot, the sink that puts a real file behind that slot, and the
# read-back that streams the file out over the same contract. They were one binary picking a role
# out of `arg0` until the role numbers turned out to be three hand-maintained copies of a fact two
# binaries had to agree on; splitting them deleted the numbers. `initrd_aarch64` packs all three
# under their own names for both ISAs.
[[bin]]
name = "sink_transcript_writer"
path = "src/sink_transcript_writer.rs"
test = false
bench = false

[[bin]]
name = "file_sink"
path = "src/file_sink.rs"
test = false
bench = false

[[bin]]
name = "file_source"
path = "src/file_source.rs"
test = false
bench = false

[lints]
workspace = true

# The network time test server (milestone 51, lifted out of the `ntp` binary by milestone 290): an
# NTP server and the network under it, in one process. It holds `READ` on the socket endpoint the
# client holds `WRITE` on and speaks the same contract `net_stack` does, so substituting it at that
# capability boundary runs the client's real, unmodified code. Both ISAs' archives carry it.
[[bin]]
name = "network_time_test_server"
path = "src/network_time_test_server.rs"
test = false
bench = false

# The unwritable clock witness (milestone 51, lifted out of the `ntp` binary by milestone 290): it
# is handed the network time client's five slots plus the address a clock *setter* maps the clock
# page at, reports, writes there, and faults. The claim Unix cannot make, and the one function that
# endows both it and the client (`spawn_with_client_endowment`) is what keeps the two from drifting.
# Both ISAs' archives carry it.
[[bin]]
name = "unwritable_clock_witness"
path = "src/unwritable_clock_witness.rs"
test = false
bench = false

# **Milestone 291 split `hello` into programs.** Each of these was a role of one binary chosen by
# the word the kernel put in `x0`; every one of them now ignores that word and does one thing. All
# fourteen names are PROVISIONAL: calef names programs, and each file's header carries what it does
# and which alternatives were refused.

# The self-checker (milestone 7): a real ELF that verifies its own loaded image
# and makes one capability-free syscall. `hello`'s SELF_CHECK role until milestone 291.
[[bin]]
name = "image_self_checker"
path = "src/image_self_checker.rs"
test = false
bench = false

# The console's test client (milestone 8): writes text into a page it shares with the
# console server and sends the length. `hello`'s PRINTING role until milestone 291.
[[bin]]
name = "console_test_client"
path = "src/console_test_client.rs"
test = false
bench = false

# The memory-region depleter (milestone 11): maps pages out of its own region until
# the region is spent, while the kernel allocates nothing. `hello`'s UNTYPED_DEMO role until 291.
[[bin]]
name = "memory_region_depleter"
path = "src/memory_region_depleter.rs"
test = false
bench = false

# The delegation pair's giving half (milestone 10): hands a capability on, narrowed.
# `hello`'s GRANTER role until milestone 291.
[[bin]]
name = "delegation_granter"
path = "src/delegation_granter.rs"
test = false
bench = false

# The delegation pair's taking half (milestone 10): uses what it was handed and proves
# it cannot pass it on. `hello`'s RECEIVER role until milestone 291.
[[bin]]
name = "delegation_receiver"
path = "src/delegation_receiver.rs"
test = false
bench = false

# The frame pair's sharing half (milestone 19): writes a sentinel into a page it owns
# and delegates a read-only view. `hello`'s PAGE_FRAME_PRODUCER role until milestone 291.
[[bin]]
name = "page_frame_producer"
path = "src/page_frame_producer.rs"
test = false
bench = false

# The frame pair's receiving half (milestone 19): reads the sentinel through its own
# mapping and is refused a writable one. `hello`'s PAGE_FRAME_CONSUMER role until milestone 291.
[[bin]]
name = "page_frame_consumer"
path = "src/page_frame_consumer.rs"
test = false
bench = false

# The Call/Reply server (milestone 12): answers a caller it was never wired to and
# proves the reply capability is one-shot. `hello`'s CALL_SERVER role until milestone 291.
[[bin]]
name = "call_server"
path = "src/call_server.rs"
test = false
bench = false

# The Call/Reply client (milestone 12). `hello`'s CALL_CLIENT role until milestone 291.
[[bin]]
name = "call_client"
path = "src/call_client.rs"
test = false
bench = false

# The revoker (milestone 13): revokes a frame it mapped and finds its own capability
# gone. `hello`'s REVOKE_DEMO role until milestone 291.
[[bin]]
name = "frame_revoker"
path = "src/frame_revoker.rs"
test = false
bench = false

# The rendezvous minter (milestone 19a): retypes a page of its own budget into an IPC
# object no kernel wiring created. `hello`'s EP_MAKER role until milestone 291.
[[bin]]
name = "rendezvous_minter"
path = "src/rendezvous_minter.rs"
test = false
bench = false

# The minted rendezvous's peer (milestone 19a): receives the delegation and listens.
# `hello`'s EP_USER role until milestone 291.
[[bin]]
name = "rendezvous_peer"
path = "src/rendezvous_peer.rs"
test = false
bench = false

# The address-space builder (milestone 19b): constructs an address space from EL0 and
# finds break-before-make enforced inside it. `hello`'s ADDRESS_SPACE_BUILDER role until 291.
[[bin]]
name = "address_space_witness"
path = "src/address_space_witness.rs"
test = false
bench = false

# The granted cycle-counter reader (milestone 229): reads the counter twice across a
# yield and reports. `hello`'s CYCLE_COUNTER_CHILD role until milestone 291.
[[bin]]
name = "cycle_counter_reader"
path = "src/cycle_counter_reader.rs"
test = false
bench = false

# The current-CPU reader (calef's 2026-09-21 ruling that a thread observing itself reads a page):
# reads its own core twice across a yield, with no syscall, and reports both plus the id bound the
# kernel and userspace have to agree on.
[[bin]]
name = "current_cpu_reader"
path = "src/current_cpu_reader.rs"
test = false
bench = false

# **Programs this package builds that no boot image packs** (milestone 198 (a package manager)
# rung 3a). `xtask`'s `declared_programs` reads this and leaves them out of all three archives; a
# program here reaches a machine only as a package a recipe under `packages/` builds. It must also
# be a `[[bin]]` above, and must not be a program the shell or the boot needs, and `xtask` refuses
# the archive otherwise. Key name provisional.
[package.metadata.nife]
packaged_only = ["greeting"]

[dependencies]
abi = { version = "0.1.0", path = "../crates/abi" }
# The words the milestone 7-19 capability demonstrations agree on, now that each is its own
# binary (milestone 291). AGENTS.md rule 7: what two binaries must agree on is a crate.
capability_witness_protocol = { version = "0.1.0", path = "../crates/capability_witness_protocol" }
# A program's check that its own ELF image was loaded correctly (milestone 291). Two fixtures
# need it, so it is a crate rather than a second copy.
loaded_image_check = { version = "0.1.0", path = "../crates/loaded_image_check" }
c_seam = { version = "0.1.0", path = "../crates/c_seam" }
supervision_protocol = { version = "0.1.0", path = "../crates/supervision_protocol" }
# The interactive system, built once (milestone 96). Both inits are a table of the slot numbers
# their own kernel granted plus a call into this.
system_initializer = { version = "0.1.0", path = "../crates/system_initializer" }
# The supervisor half of milestone 23's component manifest: the operator wires a component from
# its declaration rather than from literals, and `chatty` and `broker` derive their own slot
# numbers from the declarations they are built against.
component_plan = { version = "0.1.0", path = "../crates/component_plan" }
swap_protocol = { version = "0.1.0", path = "../crates/swap_protocol" }
grant_plan = { version = "0.1.0", path = "../crates/grant_plan" }
# The soak workload's shared page (milestone 219). The kernel reads what the soaker writes, so the
# layout and the reply transform live in one crate rather than in two copies.
soak_page = { version = "0.1.0", path = "../crates/soak_page" }
# The multi-tasking workload's definition (milestone 168). The task runs what the kernel-side
# supervisor times, from one description of the mix rather than two copies.
job_mix = { version = "0.1.0", path = "../crates/job_mix" }
credential_protocol = { version = "0.1.0", path = "../crates/credential_protocol" }
login_protocol = { version = "0.1.0", path = "../crates/login_protocol" }
compositor = { version = "0.1.0", path = "../crates/compositor" }
coremark = { version = "0.1.0", path = "../crates/coremark" }
nifefs = { version = "0.1.0", path = "../crates/nifefs" }
elf = { version = "0.1.0", path = "../crates/elf" }
filesystem_protocol = { version = "0.1.0", path = "../crates/filesystem_protocol" }
graphics_protocol = { version = "0.1.0", path = "../crates/graphics_protocol" }
line_editor = { version = "0.1.0", path = "../crates/line_editor" }
byte_sink_protocol = { version = "0.1.0", path = "../crates/byte_sink_protocol" }
# The durable schedule store's shared names (milestone 152, DECISIONS §122/§125): the fixed file
# names `session_reviver.rs` and `fs_test_client.rs`'s `ROLE_SCHEDULE_SEED` both depend on, and the
# manifest format neither `timetable` nor `filesystem_protocol` already owns.
schedule_store = { version = "0.1.0", path = "../crates/schedule_store" }
user_mode_runtime = { version = "0.1.0", path = "../crates/user_mode_runtime" }
# The current-CPU page's layout and its `CPU_ID_BOUND`, which `current_cpu_reader` reports back so
# the kernel's test can check that the two binaries still agree on it.
current_cpu_protocol = { version = "0.1.0", path = "../crates/current_cpu_protocol" }
# The three crates `network_time_test_server` and `unwritable_clock_witness` need (milestone 290),
# which arrived with them when the `ntp` binary's test-only roles moved out of `components/`. All
# three are already in the shipping graph through `components`; nothing external was taken.
network_time_protocol = { version = "0.1.0", path = "../crates/network_time_protocol" }
socket_protocol = { version = "0.1.0", path = "../crates/socket_protocol" }
clock_protocol = { version = "0.1.0", path = "../crates/clock_protocol" }
