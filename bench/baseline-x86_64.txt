# toolchain: nightly-2026-09-20
# qemu: unrecorded
# The emulator that counted these instructions. An icount count is a function of two things, the
# compiler that emitted the instructions and the emulator that counted them, and this tree recorded
# neither until 2026-09-21. `unrecorded` is the truthful answer for the counts below and not a
# placeholder: nobody wrote down which QEMU produced them, and inventing a version now would be
# worse than the gap. `cargo xtask bench --save` writes the real one from the binary it runs, and
# `--check` compares it against the emulator in front of it from that point on.
#
# What is known, and it is why this line exists at all: `.qemu-version` pins **11.0.2**, this
# machine has had **11.1.1** installed since 2026-08-28, and `script/bench` never looked. So the
# counts below were read against an emulator that may well not be the pinned one, `script/qemu-check`
# asserted they were "recorded against" the pin with nothing to support it, and the divergence has
# already reached published figures. Which version this project should run is calef's call and is
# deliberately not settled here.
# The nightly this file's counts are read against. Written by `cargo xtask bench --save` and
# checked against `rust-toolchain.toml` by `script/lint`, because a compiler bump changes the
# instruction sequence these counts measure and so silently revalues every number below. On this
# first stamping the line is a VERIFICATION rather than a recording: the counts were recorded on an
# earlier nightly and re-checked green against this pin on 2026-09-21, worst margin +0.02% on
# aarch64 and +2.27% on x86_64, with riscv64's `rfence_self` 7.49% FASTER than its floor. The next
# `--save` makes it an ordinary recording.
# bench/baseline-x86_64.txt: deterministic icount tick counts (cargo xtask bench --save).
# Recorded against the QEMU pinned in .qemu-version. icount counts guest instructions, so the
# emulator version is part of what these numbers mean: script/qemu-check warns when the QEMU on
# PATH is not the pinned one, precisely because that is when a baseline comparison stops being
# apples to apples.
             # Updating this file is a statement that a performance change is intended and
             # understood; do it in the commit that causes the change. Checked by --check, a coarse
             # 10% tripwire (icount counts drift across builds; see notes/benchmarks.md).
#
# 2026-09-21, milestone 541 (a timed window that excludes preemption). Every row here moved, and
# two different causes are mixed in this save, which is why they are separated rather than left to
# a reader to guess at:
#
#   - `map_new` is the intended change. It masks interrupts across its timed window, so the row no
#     longer records whether a scheduler tick happened to land in 250 microseconds. Before this it
#     read 180,604 or 228,356 on x86_64 depending only on the phase an unrelated code-size change
#     left the timer in, a 26.4% swing over a byte-identical map path. It now reads the same number
#     either way. See design/roadmap/541-a-timed-window-that-excludes-preemption.md.
#   - **Every other row moved because the kernel grew**, not because anything on its path changed:
#     this milestone adds a per-core preemption counter (`cpu::PerCpu::preemptions`) and one relaxed
#     increment in `sched::count_preemption`, and icount counts drift across builds when live code
#     is added at all (the module header in kernel/src/bench.rs says why). The moves are 0.0% to
#     0.3% and in both directions, with `spawn_reap` the outlier at +2.3% on x86_64 and inside the
#     tripwire on all three. Nothing here is a claim that a path got faster or slower.
#
yield_switch 19288787 2000
tss_iomap_switch 24037905 2000
tss_iomap_lazy_switch 23695902 2000
tss_iomap_lazy_nop 19719724 2000
ipc_rtt 17249316 1000
relay_rtt 34430872 1000
call_reply 17786313 1000
broker_rtt 35525938 1000
spawn_reap 2894729 64
map_new 180537 64
coremark 306216311 256
