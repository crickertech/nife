# Name: recorded (design/roadmap/39-repository-structure.md's naming section, performed by milestone
# 175). A **component** is this project's word for the shippable unit, a binary plus its manifest,
# and a **service** is what one offers over a contract. 39 refused the Unix word for a long-running
# background process on a technical rather than an aesthetic argument, and that argument is written
# there rather than repeated here, because `script/lint` keeps the rejected word inside `design/`
# and design/naming.md where the case for rejecting it lives.
#
# The package holds every EL0 program a distribution would ship because somebody wants its function:
# the services and drivers, the progenitor and supervision spine, and the tools a person invokes at the
# prompt. What it is not is in `fixtures/`, and the dividing question is what a program IS rather
# than who runs it, since nearly everything here is reached only from a kernel test. Nobody has put
# either name to calef, which is what `recorded` claims and all it claims.
[package]
name = "components"
version = "0.1.0"
edition = "2024"
license.workspace = true

# The console server (milestone 19f.3): the second program lifted out of `hello`. `initrd_aarch64` packs
# it under "console"; the progenitor loads it by name.
[[bin]]
name = "console"
path = "src/console.rs"
test = false
bench = false

# The input driver (milestone 19f.4): the receive half of the terminal, lifted out of hello.
# Since milestone 28 it is a raw byte forwarder; the editing lives in line_editor.
[[bin]]
name = "input"
path = "src/input.rs"
test = false
bench = false

# The line discipline (milestone 28): the tty layer as a swappable userspace component between
# the input/console drivers and applications. Serves the terminal contract
# (notes/terminal-contract.md); the editing engine is the host-tested `line_editor` crate.
[[bin]]
name = "line_editor"
path = "src/line_editor.rs"
test = false
bench = false

# `terminal_sink_caretaker` (milestone 50's last remainder): the terminal as a destination for the sink
# contract. A caretaker in `fs_file_caretaker`'s shape, and a separate process for a capability
# reason rather than a scheduling one: the terminal endpoint also carries OP_READLINE, so a child
# handed it as its output slot would hold the keyboard. **Provisional name.**
[[bin]]
name = "terminal_sink_caretaker"
path = "src/terminal_sink_caretaker.rs"
test = false
bench = false

# `swish` (milestone 19f.5): the shell, the last program lifted out of hello, and the most wired.
# A swish is the shot that goes through the net touching nothing, which is least authority in one
# word. `initrd_aarch64` packs it as "swish"; `system_initializer` loads it by that name.
[[bin]]
name = "swish"
path = "src/swish.rs"
test = false
bench = false


# The interrupt-driven console input driver (milestone 20): an unprivileged userspace device driver.
# It holds an Irq capability, a report endpoint, and a device mapping of the UART, and services the
# receive interrupt with the seL4 WAIT/read/report/ACK loop. Portable; the riscv initrd packs it as
# "serial_driver". The aarch64 input driver is the PL011-wired role of `hello`; this is its portable analog.
[[bin]]
name = "serial_driver"
path = "src/serial_driver.rs"
test = false
bench = false

# **The progenitor** (milestone 266): the first process, on all three architectures. The kernel
# loads it as the boot process and it builds the interactive system (console, input, line
# discipline, shell) out of its own budget, then stays alive as the spawn service. Until 266 this
# was riscv64's only and aarch64 booted a role of `hello` instead; the archive entry is now
# `progenitor` on every board and the alias layer that hid the difference is gone.
[[bin]]
name = "progenitor"
path = "src/progenitor.rs"
test = false
bench = false

# The virtio-blk driver as its own binary (parity C): the shared `virtio` module (the same file
# hello's aarch64 roles compile) behind a dedicated-binary skeleton, so riscv runs the identical
# driver, attacker included, without porting the PL011-wired hello multiplexer.
[[bin]]
name = "block_driver"
path = "src/block_driver.rs"
test = false
bench = false

# The file caretaker (milestone 31 phase 2): a directory capability attenuated to one file, in one
# direction. It is the caretaker that stands between the FS server and a program the command line
# granted a single file, and it is a separate process so the narrowing is an address-space boundary
# rather than a branch. initrd_aarch64 packs it as "fs_file_caretaker".
[[bin]]
name = "fs_file_caretaker"
path = "src/fs_file_caretaker.rs"
test = false
bench = false

# The subtree caretaker (milestone 47): the same caretaker one rung up, narrowing a directory
# capability to one SUBTREE with an explicit rights ladder. It is what `cd` and a per-process
# namespace resolve to, and it is a separate process for fs_file_caretaker's reason: the FS
# server's handle table is per server, so only an address-space boundary keeps a program off the
# parent directory. initrd_aarch64 packs it as "fs_subtree_caretaker".
[[bin]]
name = "fs_subtree_caretaker"
path = "src/fs_subtree_caretaker.rs"
test = false
bench = false

# The nameset caretaker (milestone 47's globbing lane): the same caretaker over a namespace of the
# names a pattern matched. A third caretaker rather than a mode on `fs_subtree_caretaker`, because
# that program's design property is that it performs no checks at all, and a name filter is a check
# on every name-taking verb. initrd_aarch64 packs it as "fs_nameset_caretaker".
[[bin]]
name = "fs_nameset_caretaker"
path = "src/fs_nameset_caretaker.rs"
test = false
bench = false

# The authority-shrinking supervision tree (milestone 22 phase B.2). Four programs, one shared module
# (src/suptree.rs): `root_supervisor` is a progenitor that hands its construction authority to `spawner` and its
# restart policy to `sub_server_supervisor` and then DELETES its budget; `flaky` is the supervised sub-server that
# crashes once so the tree has something real to keep alive. Portable, both ISAs; the kernel's
# authority_tests drive them.
[[bin]]
name = "root_supervisor"
path = "src/root_supervisor.rs"
test = false
bench = false

[[bin]]
name = "spawner"
path = "src/spawner.rs"
test = false
bench = false

[[bin]]
name = "sub_server_supervisor"
path = "src/sub_server_supervisor.rs"
test = false
bench = false

# The interactive boot's corpse collector (milestone 22, the interactive increment). Holds one
# endpoint capability with READ and nothing else; the progenitor endows every job it builds with the same
# endpoint, and the pages come back to the progenitor's budget when this collects the corpse.
[[bin]]
name = "job_undertaker"
path = "src/job_undertaker.rs"
test = false
bench = false

# The net server (milestone 30, piece 3): smoltcp as a userspace TCP/IP stack driving the confined
# NIC. Links `extern crate alloc` over user_mode_runtime's heap. `initrd_aarch64` packs it as "net_stack" for both ISAs.
[[bin]]
name = "net_stack"
path = "src/net_stack.rs"
test = false
bench = false

# The GPU driver (milestone 29, the display ladder's rung one): drives the virtio-gpu device
# over the confined transport and serves the framebuffer contract to clients. `initrd_aarch64` packs
# it as "gpu_driver" for both ISAs.
[[bin]]
name = "gpu_driver"
path = "src/gpu_driver.rs"
test = false
bench = false

# The framebuffer driver (the shell on the firmware screen, milestone 198's rung 1b): serves the
# same framebuffer contract as `gpu_driver`, over a screen the FIRMWARE already set up, so a PC's
# monitor shows the shell with no device of our own to program. Holds the covered part of the
# aperture as a spawn-time device mapping and copies a client's damage onto it. **Name
# provisional**, in the `<device>_driver` shape `gpu_driver` and `block_driver` take.
[[bin]]
name = "framebuffer_driver"
path = "src/framebuffer_driver.rs"
test = false
bench = false

# The compositor (milestone 33, rung two): multiplexes one screen among mutually distrusting clients.
# It is rung one's client at the display seam (display cannot tell it from `painter`), and it holds one
# doorbell endpoint for all its clients plus one input endpoint per focusable client. `initrd_aarch64` packs
# it as "compositor" for both ISAs.
[[bin]]
name = "compositor"
path = "src/compositor.rs"
test = false
bench = false

# The display terminal (milestone 29's remaining increment): the component that makes the
# framebuffer readable. One binary, two wirings (rung one's display endpoint, or rung two's
# doorbell), because being a client at both seams is what proves neither contract needed to change
# to carry text. `initrd_aarch64` packs it as "display_terminal" for both ISAs.
[[bin]]
name = "display_terminal"
path = "src/display_terminal.rs"
test = false
bench = false

# Live component replacement (milestone 23, DECISIONS §39). Four programs, one shared module
# (src/swap.rs): `swapper` is the operator that swaps a running component under a talking client;
# `rust_swappable` and `c_swappable` are the two instances of that component (the second computes
# its answers in C, so the replacement is a different implementation in a different language);
# `chatty` is the client that keeps talking across the swap and the attacker that tries to become
# the server. Portable, both ISAs; the kernel's live_swap_tests drive them.
[[bin]]
name = "swapper"
path = "src/swapper.rs"
test = false
bench = false

# The queue broker (milestone 23, the latency ladder's middle rung): the opt-in, per-channel
# lifecycle decoupler. A producer never blocks on an absent consumer; the broker buffers in its own
# budget and the new consumer drains the backlog in order. Never the default, because it costs an
# extra hop, which `broker_rtt` prices.
[[bin]]
name = "broker"
path = "src/broker.rs"
test = false
bench = false

# The clock service (milestone 51, DECISIONS §43): owns the RTC's registers and the wall clock's
# offset, publishes the offset into a page its readers hold read-only, and serves proposals it is
# free to refuse. Both RTC drivers ride in it, on both ISAs, because the device is a fact about the
# board rather than the instruction set. `initrd_aarch64` packs it as "clock" for both ISAs.
[[bin]]
name = "clock"
path = "src/clock.rs"
test = false
bench = false

# `date` (milestone 51, DECISIONS §43): reads the clock page, hands the number to the host-tested
# `calendar` crate, prints. It holds a read-only mapping and therefore cannot set the clock; there is
# no `date -s` here on purpose. `initrd_aarch64` packs it as "date" for both ISAs.
[[bin]]
name = "date"
path = "src/date.rs"
test = false
bench = false

# `printenv` (milestone 47's environment-variable fork, DECISIONS §111): reads the inert-
# configuration page the progenitor endows and prints TZ/LANG/TERM. `date`'s own shape, one manifest field
# over: a read-only mapping and nothing else, so there is no way for this program to change what a
# shell hands its own children. `initrd_aarch64` packs it as "printenv" for both ISAs.
[[bin]]
name = "printenv"
path = "src/printenv.rs"
test = false
bench = false

# `rm` (milestone 47's rmdir lane): the first program endowed a **directory** capability, and the
# reason `rm` stopped being a shell builtin. A builtin runs with the shell's whole endowment; this
# takes an attenuated grant, so its recursion can only reach what it was handed. `initrd_aarch64` packs it
# as "rm" for both ISAs.
[[bin]]
name = "rm"
path = "src/rm.rs"
test = false
bench = false

# `disk_surveyor` (milestone 57): what drives are attached, and how the one it holds is divided up.
# It holds a read-only roster mapping (the listing) and a block-service endpoint (the one disk), and
# the whole point is that those are two authorities rather than one. Its second role is the negative
# control: it writes to the roster's address and dies. `initrd_aarch64` packs it for both ISAs.
#
# A surveyor maps land they do not own, which is the split this program exists to demonstrate.
[[bin]]
name = "disk_surveyor"
path = "src/disk_surveyor.rs"
test = false
bench = false

# `disk_partitioner` (milestone 57's write half): writes the map the surveyor reads. The pair of
# authorities it needs is the demonstration: ONE disk, and an entropy endpoint for the unique ids a
# GPT requires. Without the second it writes nothing at all rather than inventing an id, and the
# same binary's verify role reads the disk back to show which happened. `initrd_aarch64` packs it for both
# ISAs. Provisional name (milestone 57's lane): calef settles program names.
[[bin]]
name = "disk_partitioner"
path = "src/disk_partitioner.rs"
test = false
bench = false

# `installer` (milestone 198 (a package manager, and the trivial install that makes a second
# customer possible), rung 2a): the program a booted stick runs to put this system on the machine's
# own disk. The most destructive program in this tree, and it holds exactly what
# `disk_partitioner` holds plus a read-only copy of the file this machine booted from: one disk, one
# entropy endpoint, no path to type. The question that precedes it is asked by
# `kernel/src/user/install_service.rs`, before the disk capability exists. **Provisional name**,
# set aside for it by `crates/stick_maker`'s own header.
[[bin]]
name = "installer"
path = "src/installer.rs"
test = false
bench = false

# The keyboard driver (milestone 29's input): a confined userspace virtio-input driver whose whole
# power to inject a keystroke is the mapping of the compositor's input ring, not the doorbell every
# client holds. `initrd_aarch64` packs it as "keyboard_driver" for both ISAs.
[[bin]]
name = "keyboard_driver"
path = "src/keyboard_driver.rs"
test = false
bench = false

# The Rust multi-line editor (milestone 169), on the raw-keystroke primitive
# (OP_RAWMODE/OP_READRAW) that milestone added to the terminal contract. See components/src/rmle.rs.
[[bin]]
name = "rmle"
path = "src/rmle.rs"
test = false
bench = false

# The entropy service (milestone 56, DECISIONS §44): owns a virtio-rng device and is the only thing
# that can read it; clients hold an endpoint that means "you may obtain randomness". The driver rides
# in it, the way both RTC drivers ride in the clock service, because a separate driver process would
# add a hop and hold exactly the same authority. `initrd_aarch64` packs it as "entropy" for both ISAs.
[[bin]]
name = "entropy"
path = "src/entropy.rs"
test = false
bench = false

# The JH7110 TRNG driver (milestone 159): an alternate entropy_protocol backend for real riscv64
# hardware (the VisionFive 2), beside "entropy"'s virtio-rng one. Wired since 2026-09-01: the
# riscv64 boot tour spawns it through entropy_service's Jh7110 bus when this machine's device tree
# describes the device, which on every machine CI boots it does not. It ran on radon for the first
# time on 2026-09-04. Name: PROVISIONAL, matching the crate it is the volatile shell over.
[[bin]]
name = "jh7110_entropy"
path = "src/jh7110_entropy.rs"
test = false
bench = false

# The EL0 NVMe block server (milestone 261, DECISIONS §86's option 2a): the queue mechanics that
# used to be `kernel/src/non_volatile_memory_express.rs`'s, now a process holding one page of BAR0,
# a run of DMA pages, and two endpoints. It shares its crate's name, which calef ruled on
# 2026-09-18 under DECISIONS §154: `non_volatile_memory_express_server` is 34 bytes against
# `nifefs`'s 32-byte `NAME_LEN`, and the crate-and-program pair is what the sharing means.
[[bin]]
name = "non_volatile_memory_express"
path = "src/non_volatile_memory_express.rs"
test = false
bench = false

# The credential service (milestone 56, the credential half): holds the credential store and serves
# one question over one endpoint. Its authority to *write* the store is a second endpoint that it
# deletes at the seal, so a client cannot be refused permission to write it; there is no object left
# for the request to travel through. `initrd_aarch64` packs it as "credentialer" for both ISAs.
[[bin]]
name = "credentialer"
path = "src/credentialer.rs"
test = false
bench = false

# The login service (milestone 49, provisional name): authenticates a presented identity against
# the credential service and, on success, mints a fresh directory capability (a
# `fs_subtree_caretaker` built for that principal) and a fresh budget rather than mutating anything
# ambient. `initrd_aarch64` packs it as "login" for both ISAs.
[[bin]]
name = "login"
path = "src/login.rs"
test = false
bench = false

# The login audit receiver (milestone 49 (users and attribution), its boot-wiring update;
# `audit_sink` until calef's 2026-09-13 ruling was performed on 2026-09-24): drains `login`'s AUDIT
# endpoint so its blocking `send` on a successful login never parks the whole service, holding
# nothing but that one READ capability. `initrd_aarch64` packs it as "login_audit_receiver" for both
# ISAs.
[[bin]]
name = "login_audit_receiver"
path = "src/login_audit_receiver.rs"
test = false
bench = false

# The provisioning tool (milestone 155, provisional name; DECISIONS §117): a `useradd`-equivalent
# that PUTs an identity and secret into the credential store and creates that identity's home
# subtree as one act, so a principal never exists with one and not the other for longer than this
# tool's own two calls. `initrd_aarch64` packs it as "identity_provisioner" for both ISAs.
[[bin]]
name = "identity_provisioner"
path = "src/identity_provisioner.rs"
test = false
bench = false

# The boot-time re-deriver (milestone 152's third piece, provisional name; DECISIONS §123): a
# `root_supervisor`-shaped boot-only process that reads the durable schedule store's manifest, reads
# and parses each named identity's own schedule file, mints and tears down a synthetic stand-in for
# the session it would re-derive, then deletes its own construction budget and store-read capability
# and proves both gone. `initrd_aarch64` packs it as "session_reviver" for both ISAs.
[[bin]]
name = "session_reviver"
path = "src/session_reviver.rs"
test = false
bench = false

# The network time client (milestone 51, renamed and split by milestone 290): holds a network
# capability and the right to PROPOSE a time, and deliberately not the right to set one. It used to
# be one binary with three `arg0` roles; the other two are test-only and live in `fixtures/` now
# (`network_time_test_server` and `unwritable_clock_witness`), which is the line milestone 175 drew.
# Both ISAs' archives carry it.
[[bin]]
name = "network_time_client"
path = "src/network_time_client.rs"
test = false
bench = false

# The consumer (milestone 50): counts what arrives on its input and writes the numbers to its
# output, both over the sink contract. The first program in the tree that reads a stream, and the
# right-hand side of `date | wc`. Portable, so both ISAs' archives carry it.
[[bin]]
name = "wc"
path = "src/wc.rs"
test = false
bench = false

# `timetable` (milestone 129): scheduled execution whose every entry is a grant. A service, not a
# prompt command: it holds a construction budget and builds supervised children, which is authority
# no shell should be able to hand out, so it has no `grant_plan::Prog` variant and is spawned at a
# wiring site instead. Portable, so both ISAs' archives carry it.
[[bin]]
name = "timetable"
path = "src/timetable.rs"
test = false
bench = false

# `ps` (milestone 126): the process listing, over a supervision domain rather than over /proc. Its
# whole authority is one endpoint the progenitor endows and the command line cannot name; the listing itself
# is crates/ps, host-tested. Portable, so both ISAs' archives carry it: the confinement claim is
# about the capability model and would not be a claim if it held on one instruction set.
[[bin]]
name = "ps"
path = "src/ps.rs"
test = false
bench = false

# `pgrep` (milestone 126): the same domain, filtered, with the matching tids as its whole answer. It
# holds exactly `ps`'s three capabilities and not one right more, which is the argument: it can name
# the domain's members and there is no `pkill` beside it, because a tid is a name and not a
# capability. The filter is crates/pgrep, host-tested. Portable for `ps`'s reason.
[[bin]]
name = "pgrep"
path = "src/pgrep.rs"
test = false
bench = false

# `top` (milestone 282 (a thread's CPU time, and the `top` it makes possible)): the same domain `ps` lists, walked twice and ranked by each thread's
# scheduled CPU time. Holds `ps`'s three capabilities exactly; the difference is the question asked,
# not the authority held. The summary line is crates/top, the walk and the table are crates/ps, both
# host-tested. Portable for `ps`'s reason.
[[bin]]
name = "top"
path = "src/top.rs"
test = false
bench = false

# `pmap` (milestone 126, DECISIONS §114): `ps`'s split one object type over, listing an address
# space's mappings through `abi::aspace::LIST` gated by `ENUMERATE` rather than `WRITE`. Proven
# end to end against a real `Object::Aspace` in `kernel::user::pmap_tests`; not reachable from the
# interactive shell today, because nothing in this tree delegates an address-space capability to a
# program other than its own builder (see `crates/pmap`'s BUGS). The listing itself is
# crates/pmap, host-tested. Portable for `ps`'s reason.
[[bin]]
name = "pmap"
path = "src/pmap.rs"
test = false
bench = false

# `uptime` (milestone 126): how long the ambient monotonic counter has been running. Needed no
# new capability at all: `monotonic_nanos` is granted to every process unconditionally
# (kernel/src/arch/*/timer.rs's documented exception to DECISIONS §10), so this program's manifest
# is `least_authority_demo`'s, not `date`'s. The formatting is crates/uptime, host-tested. Portable, so both
# ISAs' archives carry it. **Provisional name**: the same word upstream `procps` uses; see the
# crate's module docs for why it is flagged anyway.
[[bin]]
name = "uptime"
path = "src/uptime.rs"
test = false
bench = false

# `uuid` (milestone 111): a version-4 UUID drawn from the entropy service. The first program a
# person can type that needs randomness: before it, the entropy grant reached only what the system
# spawned (`credentialer` at boot, `disk_partitioner` under the kernel's test harness), so a
# program's dependence on unpredictable bytes was real and unreachable from a prompt. Its whole
# authority is `grant_plan::Manifest::entropy`, which the progenitor endows and no token on the command line
# can name. Portable, so both ISAs' archives carry it. **Provisional name.**
[[bin]]
name = "uuid"
path = "src/uuid.rs"
test = false
bench = false

# `mdr` (milestone 40): the markdown renderer. Markdown on its input, rendered text on its
# output, and nothing else in its cspace; the rendering itself is `crates/documentation`, which
# is host-tested. Name ratified 2026-09-13 by calef.
[[bin]]
name = "mdr"
path = "src/mdr.rs"
test = false
bench = false

# The least-authority demo (milestone 19f.2): the first program that is its own binary rather than
# a role of `hello`. It shares this package's `build.rs` link.ld (linked at 0x40_0000) but no code.
# Same test/bench reasoning as `hello`'s block. `initrd_aarch64` packs it into the archive under
# the name "least_authority_demo".
[[bin]]
name = "least_authority_demo"
path = "src/least_authority_demo.rs"
test = false
bench = false

[lints]
workspace = true

[dependencies]
# The boot ladder's console markers (milestone 268). `swish` prints the last rung; the kernel
# prints the ones below it and `crates/board_console` matches all of them, so AGENTS.md rule 7
# makes the strings a crate rather than copies. `no_std`, no dependencies of its own.
boot_ladder = { version = "0.1.0", path = "../crates/boot_ladder" }
abi = { version = "0.1.0", path = "../crates/abi" }
supervision_protocol = { version = "0.1.0", path = "../crates/supervision_protocol" }
# The interactive system, built once (milestone 96). Both inits are a table of the slot numbers
# their own kernel granted plus a call into this.
system_initializer = { version = "0.1.0", path = "../crates/system_initializer" }
# The supervisor half of milestone 23's component manifest: the operator wires a component from
# its declaration rather than from literals, and `chatty` and `broker` derive their own slot
# numbers from the declarations they are built against.
component_plan = { version = "0.1.0", path = "../crates/component_plan" }
swap_protocol = { version = "0.1.0", path = "../crates/swap_protocol" }
virtio = { version = "0.1.0", path = "../crates/virtio" }
socket_protocol = { version = "0.1.0", path = "../crates/socket_protocol" }
bitmap_font = { version = "0.1.0", path = "../crates/bitmap_font" }
block_roster = { version = "0.1.0", path = "../crates/block_roster" }
globally_unique_identifier_partition_table = { version = "0.1.0", path = "../crates/globally_unique_identifier_partition_table" }
file_allocation_table = { version = "0.1.0", path = "../crates/file_allocation_table" }
# Milestone 198 rung 2b: the two-slot boot state the installer writes into GPT attribute bits and
# `uefi_loader`'s chooser reads back. One implementation, two programs.
boot_slot = { version = "0.1.0", path = "../crates/boot_slot" }
calendar = { version = "0.1.0", path = "../crates/calendar" }
grant_plan = { version = "0.1.0", path = "../crates/grant_plan" }
clock_protocol = { version = "0.1.0", path = "../crates/clock_protocol" }
environment_protocol = { version = "0.1.0", path = "../crates/environment_protocol" }
credentialer = { version = "0.1.0", path = "../crates/credentialer" }
credential_protocol = { version = "0.1.0", path = "../crates/credential_protocol" }
login_protocol = { version = "0.1.0", path = "../crates/login_protocol" }
compositor = { version = "0.1.0", path = "../crates/compositor" }
nifefs = { version = "0.1.0", path = "../crates/nifefs" }
pgrep = { version = "0.1.0", path = "../crates/pgrep" }
pmap = { version = "0.1.0", path = "../crates/pmap" }
ps = { version = "0.1.0", path = "../crates/ps" }
top = { version = "0.1.0", path = "../crates/top" }
uptime = { version = "0.1.0", path = "../crates/uptime" }
elf = { version = "0.1.0", path = "../crates/elf" }
entropy_protocol = { version = "0.1.0", path = "../crates/entropy_protocol" }
# The JH7110 TRNG's register logic, DTB discovery and byte pool (milestone 159), host-tested in
# the crate itself; jh7110_entropy.rs is the volatile shell of register writes over it.
jh7110_entropy = { version = "0.1.0", path = "../crates/jh7110_entropy" }
# login's own measured-boot consultation before it will build a caretaker from an archive entry
# (milestone 49's BUGS, resolved): the same crate `crates/system_initializer` already depends on.
measured_boot = { version = "0.1.0", path = "../crates/measured_boot" }
filesystem_protocol = { version = "0.1.0", path = "../crates/filesystem_protocol" }
# The NVMe queue arithmetic (milestone 53's crate, milestone 261's consumer):
# non_volatile_memory_express.rs is the volatile shell of ring copies and doorbell writes over it,
# the crate/program pair this tree already uses for `jh7110_entropy` and `line_editor`.
non_volatile_memory_express = { version = "0.1.0", path = "../crates/non_volatile_memory_express" }
graphics_protocol = { version = "0.1.0", path = "../crates/graphics_protocol" }
# The aperture arithmetic `framebuffer_driver` copies through: the clipped geometry, the stride and
# the byte order, host-tested there because this program has no harness of its own.
screen_console = { version = "0.1.0", path = "../crates/screen_console" }
line_editor = { version = "0.1.0", path = "../crates/line_editor" }
network_time_protocol = { version = "0.1.0", path = "../crates/network_time_protocol" }
documentation = { version = "0.1.0", path = "../crates/documentation" }
byte_sink_protocol = { version = "0.1.0", path = "../crates/byte_sink_protocol" }
# The shell's own logic (milestone 70): the routing of a typed line, the pattern-versus-text
# question, and every sentence the prompt prints. `components/src/swish.rs` is the same program it was and
# keeps all the IO; what moved is what a host test can reach. `glob` left this manifest in the same
# change, because `has_magic` was reached only from the function that moved.
swish = { version = "0.1.0", path = "../crates/swish" }
# The scheduler's decision half (milestone 129): the document, the four answers registration can
# give, and the fire arithmetic. `components/src/timetable.rs` keeps the budget, the counter and the
# loader; everything a host test or Kani can reach is in the crate.
timetable = { version = "0.1.0", path = "../crates/timetable" }
# The durable schedule store's shared names (milestone 152, DECISIONS §122/§125): the fixed file
# names `session_reviver.rs` and `fs_test_client.rs`'s `ROLE_SCHEDULE_SEED` both depend on, and the
# manifest format neither `timetable` nor `filesystem_protocol` already owns.
schedule_store = { version = "0.1.0", path = "../crates/schedule_store" }
user_mode_runtime = { version = "0.1.0", path = "../crates/user_mode_runtime" }
video_terminal = { version = "0.1.0", path = "../crates/video_terminal" }
# The register-block idiom milestone 139 round 5 brought to `user` (calef, 2026-08-24, "take the
# dependency for user"): compile-time-checked device register layouts for the fixed-layout blocks
# console.rs's and input.rs's PL011 half and jh7110_entropy.rs use, matching
# kernel/src/drivers/pl011.rs's own idiom for the identical hardware. Pinned to the same version as
# kernel/Cargo.toml's own dependency so the two crates using this library never skew.
tock-registers = "0.10"
# smoltcp (milestone 30, piece 3): the userspace TCP/IP stack. no_std, heap over user_mode_runtime's
# UntypedHeap. Pinned at 0.13.1; divergence policy is the vendored-engine discipline (DECISIONS §18).
smoltcp = { version = "0.14.0", default-features = false, features = [
    "alloc",
    "medium-ethernet",
    # No "multicast". Milestone 55's mDNS stack half enabled it (group membership, IGMP, and RX
    # acceptance of datagrams addressed to a joined group) and milestone 298 took it out again on
    # 2026-09-15 with the only thing that joined a group. Without it the IPv4 input path accepts
    # multicast only to 224.0.0.1, so a future multicast client must put it back; notes/mdns.md
    # records what it took and what QEMU could and could not prove.
    "proto-ipv4",
    "proto-dhcpv4",
    "socket-udp",
    "socket-tcp",
    "socket-dhcpv4",
] }
