# Name: provisional (milestone 219). The provenance block, with the refusals, lives in src/lib.rs,
# which is where the naming gate reads it and where a reader meets the crate.
[package]
name = "soak_page"
version = "0.1.0"
edition = "2024"
license.workspace = true

[lints]
workspace = true

[dependencies]
# The user address-space map (milestone 206 (a program image has under 896 KiB)): every fixed window here is checked against its band.
address_space_map = { version = "0.1.0", path = "../address_space_map" }
