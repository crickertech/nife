Falsifies `elf::note::verification::a_note_segment_is_read_inside_itself`.

The defect: the walk reads a note header without first checking that twelve bytes remain. A segment
whose length is not a whole number of notes, which a hostile file can arrange freely, then indexes
past its end.

Expected to fail: an index out of bounds in `u32le`, reached from `find_in_segment`.

diff --git a/crates/elf/src/note.rs b/crates/elf/src/note.rs
index 89c4cb5d7..d21175c8f 100644
--- a/crates/elf/src/note.rs
+++ b/crates/elf/src/note.rs
@@ -251,9 +251,6 @@ fn find_in_segment<'a>(
     let mut found = None;
     let mut at = 0;
     while at < seg.len() {
-        if seg.len() - at < NHDR_SIZE {
-            return Err(NoteError::Truncated);
-        }
         let namesz = u32le(seg, at);
         let descsz = u32le(seg, at + 4);
         let ntype = u32le(seg, at + 8);
