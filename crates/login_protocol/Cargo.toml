[package]
name = "login_protocol"
version = "0.1.0"
edition = "2024"
license.workspace = true

[lints]
workspace = true

[dependencies]
credential_protocol = { version = "0.1.0", path = "../credential_protocol" }
