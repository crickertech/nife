Falsifies `manifest_note::verification::decode_never_panics`.

The defect: `decode` reads the version and then trusts the length. A note's descriptor size is a
word in the file, so a hostile program can carry a four-byte descriptor that says version 1, and
every later field read then indexes past it.

Expected to fail: an index out of bounds in `decode`.

diff --git a/crates/manifest_note/src/lib.rs b/crates/manifest_note/src/lib.rs
index 9e9c85537..deb6eacde 100644
--- a/crates/manifest_note/src/lib.rs
+++ b/crates/manifest_note/src/lib.rs
@@ -295,9 +295,6 @@ pub fn decode(d: &[u8]) -> Result<Manifest, Error> {
     if u32::from_le_bytes([d[0], d[1], d[2], d[3]]) != VERSION {
         return Err(Error::UnknownVersion);
     }
-    if d.len() != DESCRIPTOR_LEN {
-        return Err(Error::WrongLength);
-    }
     let arg = match d[ARG] {
         0 => ArgSpec::Forbidden,
         1 => ArgSpec::Required,
