/dts-v1/;

/*
 * The JH7110's STG clock-and-reset generator **as mainline Linux describes it**. NOT a dump from
 * any board: transcribed 2026-09-04 from arch/riscv/boot/dts/starfive/jh7110.dtsi, mainline,
 * fetched via raw.githubusercontent.com/torvalds/linux/master/...
 *
 *     stgcrg: clock-controller@10230000 {
 *         compatible = "starfive,jh7110-stgcrg";
 *         reg = <0x0 0x10230000 0x0 0x10000>;
 *         #clock-cells = <1>;
 *         #reset-cells = <1>;
 *     };
 *
 * One window, so `reg-names` is absent and `discover` takes the node's only `reg`. The soc
 * wrapper and its two-cell addressing are the real tree's; a four-cell `reg` only decodes
 * correctly under them.
 *
 * Regenerate with:
 *
 *     dtc -I dts -O dtb -o crates/jh7110_crg/tests/fixtures/jh7110-stgcrg-mainline.dtb \
 *                          crates/jh7110_crg/tests/fixtures/jh7110-stgcrg-mainline.dts
 */

/ {
	#address-cells = <2>;
	#size-cells = <2>;
	compatible = "starfive,jh7110";

	soc {
		compatible = "simple-bus";
		#address-cells = <2>;
		#size-cells = <2>;
		ranges;

		clock-controller@10230000 {
			compatible = "starfive,jh7110-stgcrg";
			reg = <0x0 0x10230000 0x0 0x10000>;
			#clock-cells = <1>;
			#reset-cells = <1>;
		};
	};
};
