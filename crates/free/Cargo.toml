[package]
name = "free"
version = "0.1.0"
edition = "2024"
license.workspace = true

[lints]
workspace = true

[dependencies]
machine_statistics_protocol = { version = "0.1.0", path = "../machine_statistics_protocol" }
page_frames = { version = "0.1.0", path = "../page_frames" }
