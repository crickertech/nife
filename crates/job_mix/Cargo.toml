# Name: provisional (milestone 168). The provenance block, with the refusals, lives in src/lib.rs,
# which is where the naming gate reads it and where a reader meets the crate.
[package]
name = "job_mix"
version = "0.1.0"
edition = "2024"
license.workspace = true

[lints]
workspace = true
