//! **The glyph bitmaps** (milestone 29). Generated data, kept in its own file so the code that
//! *uses* a font is not buried in 128 rows of hexadecimal.
//!
//! # Where these bits come from, and what they owe
//!
//! **They are ours.** This is an original drawing in the style of the Kaypro II's character
//! generator, not that ROM and not any other font's table. It was drawn glyph by glyph in
//! `crates/bitmap_font/kaypro-style-7x8.art`, which is the authoring source and the thing to edit;
//! the rows below are that file, transcribed. `the_art_file_and_this_table_agree` (in this
//! file) parses the
//! `.art` back and fails if the two ever drift, so the picture a reviewer reads and the bits a
//! terminal draws cannot disagree.
//!
//! Nothing here is licensed from anyone, so nothing travels with the image. That is the property
//! the old table had for a different reason (`font8x8` was public domain) and it is worth keeping:
//! a bitmap font is compiled into the kernel and into every binary that draws text, so its licence
//! is a licence on the artefact rather than on a build-time tool.
//!
//! **Why the Kaypro's style and not the Kaypro's bits.** calef owned a Kaypro II and its font won
//! a poll. The dumped ROM is excluded: `ivanizag/kaypro-disassembly` carries no `LICENSE` at all,
//! which is all-rights-reserved by default, and this tree's standing rule is that ambiguous is
//! treated as obliged. A *typeface as typeface* is not copyrightable subject matter (37 CFR
//! 202.1(e)), so the look is free to reproduce while the specific file is somebody's work. See
//! notes/glyphs.md, which records the whole reasoning and the ROM's hashes.
//!
//! # The geometry, which is the Kaypro's and is why the grid got wider
//!
//! Seven columns, of which the **middle five carry ink** and the outer two are gutter. That is what
//! the machine did in hardware: MAME's `kaypro_v.cpp` shifts out `0`, five ROM bits, `0`. Eight
//! rows: 0..=6 are the body with the baseline at row 6, and row 7 is the one-row descender. Caps
//! and digits fill rows 0..=6, x-height letters rows 2..=6.
//!
//! # The bit order, which is the one thing a font table gets wrong
//!
//! One byte per row, top row first. **Bit 0 is the LEFTMOST pixel** (little-endian within the row),
//! which is the opposite of the convention most bitmap fonts use and exactly the sort of thing that
//! produces a mirrored alphabet nobody notices until it is on a screen. `lib.rs` asserts it against
//! a glyph whose shape is asymmetric. Bits 0 and 6 are the gutter and are clear in every glyph,
//! which `lib.rs` also asserts.

/// The rows of every glyph in the basic-latin block, indexed by byte. Bytes 0x00..0x20 and
/// 0x7f are blank in this font: they are control codes, and a terminal never puts one in a cell.
pub static BASIC: [[u8; 8]; 128] = [
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x00 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x01 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x02 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x03 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x04 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x05 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x06 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x07 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x08 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x09 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x0a control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x0b control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x0c control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x0d control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x0e control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x0f control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x10 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x11 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x12 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x13 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x14 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x15 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x16 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x17 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x18 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x19 control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x1a control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x1b control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x1c control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x1d control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x1e control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x1f control
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x20 space
    [0x08, 0x08, 0x08, 0x08, 0x08, 0x00, 0x08, 0x00], // 0x21 '!'
    [0x14, 0x14, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x22 '''
    [0x00, 0x14, 0x3e, 0x14, 0x3e, 0x14, 0x00, 0x00], // 0x23 '#'
    [0x08, 0x3c, 0x0a, 0x1c, 0x28, 0x1e, 0x08, 0x00], // 0x24 '$'
    [0x06, 0x26, 0x10, 0x08, 0x04, 0x32, 0x30, 0x00], // 0x25 '%'
    [0x0c, 0x12, 0x0a, 0x04, 0x2a, 0x12, 0x2c, 0x00], // 0x26 '&'
    [0x08, 0x08, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x27 '''
    [0x10, 0x08, 0x04, 0x04, 0x04, 0x08, 0x10, 0x00], // 0x28 '('
    [0x04, 0x08, 0x10, 0x10, 0x10, 0x08, 0x04, 0x00], // 0x29 ')'
    [0x00, 0x08, 0x2a, 0x1c, 0x2a, 0x08, 0x00, 0x00], // 0x2a '*'
    [0x00, 0x00, 0x08, 0x08, 0x3e, 0x08, 0x08, 0x00], // 0x2b '+'
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x08, 0x04], // 0x2c ','
    [0x00, 0x00, 0x00, 0x00, 0x3e, 0x00, 0x00, 0x00], // 0x2d '-'
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x08, 0x00], // 0x2e '.'
    [0x20, 0x10, 0x10, 0x08, 0x04, 0x04, 0x02, 0x00], // 0x2f '/'
    [0x1c, 0x22, 0x32, 0x2a, 0x26, 0x22, 0x1c, 0x00], // 0x30 '0'
    [0x08, 0x0c, 0x08, 0x08, 0x08, 0x08, 0x1c, 0x00], // 0x31 '1'
    [0x1c, 0x22, 0x20, 0x10, 0x08, 0x04, 0x3e, 0x00], // 0x32 '2'
    [0x3e, 0x10, 0x08, 0x10, 0x20, 0x22, 0x1c, 0x00], // 0x33 '3'
    [0x10, 0x18, 0x14, 0x12, 0x3e, 0x10, 0x10, 0x00], // 0x34 '4'
    [0x3e, 0x02, 0x1e, 0x20, 0x20, 0x22, 0x1c, 0x00], // 0x35 '5'
    [0x18, 0x04, 0x02, 0x1e, 0x22, 0x22, 0x1c, 0x00], // 0x36 '6'
    [0x3e, 0x20, 0x10, 0x08, 0x04, 0x04, 0x04, 0x00], // 0x37 '7'
    [0x1c, 0x22, 0x22, 0x1c, 0x22, 0x22, 0x1c, 0x00], // 0x38 '8'
    [0x1c, 0x22, 0x22, 0x3c, 0x20, 0x10, 0x0c, 0x00], // 0x39 '9'
    [0x00, 0x00, 0x00, 0x08, 0x00, 0x00, 0x08, 0x00], // 0x3a ':'
    [0x00, 0x00, 0x00, 0x08, 0x00, 0x00, 0x08, 0x04], // 0x3b ';'
    [0x00, 0x00, 0x10, 0x08, 0x04, 0x08, 0x10, 0x00], // 0x3c '<'
    [0x00, 0x00, 0x00, 0x3e, 0x00, 0x3e, 0x00, 0x00], // 0x3d '='
    [0x00, 0x00, 0x04, 0x08, 0x10, 0x08, 0x04, 0x00], // 0x3e '>'
    [0x1c, 0x22, 0x20, 0x10, 0x08, 0x00, 0x08, 0x00], // 0x3f '?'
    [0x1c, 0x22, 0x3a, 0x2a, 0x3a, 0x02, 0x1c, 0x00], // 0x40 '@'
    [0x1c, 0x22, 0x22, 0x3e, 0x22, 0x22, 0x22, 0x00], // 0x41 'A'
    [0x1e, 0x22, 0x22, 0x1e, 0x22, 0x22, 0x1e, 0x00], // 0x42 'B'
    [0x1c, 0x22, 0x02, 0x02, 0x02, 0x22, 0x1c, 0x00], // 0x43 'C'
    [0x1e, 0x22, 0x22, 0x22, 0x22, 0x22, 0x1e, 0x00], // 0x44 'D'
    [0x3e, 0x02, 0x02, 0x1e, 0x02, 0x02, 0x3e, 0x00], // 0x45 'E'
    [0x3e, 0x02, 0x02, 0x1e, 0x02, 0x02, 0x02, 0x00], // 0x46 'F'
    [0x1c, 0x22, 0x02, 0x3a, 0x22, 0x22, 0x1c, 0x00], // 0x47 'G'
    [0x22, 0x22, 0x22, 0x3e, 0x22, 0x22, 0x22, 0x00], // 0x48 'H'
    [0x1c, 0x08, 0x08, 0x08, 0x08, 0x08, 0x1c, 0x00], // 0x49 'I'
    [0x20, 0x20, 0x20, 0x20, 0x22, 0x22, 0x1c, 0x00], // 0x4a 'J'
    [0x22, 0x12, 0x0a, 0x06, 0x0a, 0x12, 0x22, 0x00], // 0x4b 'K'
    [0x02, 0x02, 0x02, 0x02, 0x02, 0x02, 0x3e, 0x00], // 0x4c 'L'
    [0x22, 0x36, 0x2a, 0x22, 0x22, 0x22, 0x22, 0x00], // 0x4d 'M'
    [0x22, 0x26, 0x2a, 0x32, 0x22, 0x22, 0x22, 0x00], // 0x4e 'N'
    [0x1c, 0x22, 0x22, 0x22, 0x22, 0x22, 0x1c, 0x00], // 0x4f 'O'
    [0x1e, 0x22, 0x22, 0x1e, 0x02, 0x02, 0x02, 0x00], // 0x50 'P'
    [0x1c, 0x22, 0x22, 0x22, 0x2a, 0x12, 0x2c, 0x00], // 0x51 'Q'
    [0x1e, 0x22, 0x22, 0x1e, 0x0a, 0x12, 0x22, 0x00], // 0x52 'R'
    [0x3c, 0x02, 0x02, 0x1c, 0x20, 0x20, 0x1e, 0x00], // 0x53 'S'
    [0x3e, 0x08, 0x08, 0x08, 0x08, 0x08, 0x08, 0x00], // 0x54 'T'
    [0x22, 0x22, 0x22, 0x22, 0x22, 0x22, 0x1c, 0x00], // 0x55 'U'
    [0x22, 0x22, 0x22, 0x22, 0x22, 0x14, 0x08, 0x00], // 0x56 'V'
    [0x22, 0x22, 0x22, 0x22, 0x2a, 0x36, 0x22, 0x00], // 0x57 'W'
    [0x22, 0x22, 0x14, 0x08, 0x14, 0x22, 0x22, 0x00], // 0x58 'X'
    [0x22, 0x22, 0x14, 0x08, 0x08, 0x08, 0x08, 0x00], // 0x59 'Y'
    [0x3e, 0x20, 0x10, 0x08, 0x04, 0x02, 0x3e, 0x00], // 0x5a 'Z'
    [0x1c, 0x04, 0x04, 0x04, 0x04, 0x04, 0x1c, 0x00], // 0x5b '['
    [0x02, 0x04, 0x04, 0x08, 0x10, 0x10, 0x20, 0x00], // 0x5c '\\'
    [0x1c, 0x10, 0x10, 0x10, 0x10, 0x10, 0x1c, 0x00], // 0x5d ']'
    [0x08, 0x14, 0x22, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x5e '^'
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x3e], // 0x5f '_'
    [0x04, 0x08, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x60 '`'
    [0x00, 0x00, 0x1c, 0x20, 0x3c, 0x22, 0x3c, 0x00], // 0x61 'a'
    [0x02, 0x02, 0x1e, 0x22, 0x22, 0x22, 0x1e, 0x00], // 0x62 'b'
    [0x00, 0x00, 0x1c, 0x02, 0x02, 0x02, 0x1c, 0x00], // 0x63 'c'
    [0x20, 0x20, 0x3c, 0x22, 0x22, 0x22, 0x3c, 0x00], // 0x64 'd'
    [0x00, 0x00, 0x1c, 0x22, 0x3e, 0x02, 0x1c, 0x00], // 0x65 'e'
    [0x18, 0x04, 0x1e, 0x04, 0x04, 0x04, 0x04, 0x00], // 0x66 'f'
    [0x00, 0x00, 0x3c, 0x22, 0x22, 0x22, 0x3c, 0x0e], // 0x67 'g'
    [0x02, 0x02, 0x1e, 0x22, 0x22, 0x22, 0x22, 0x00], // 0x68 'h'
    [0x08, 0x00, 0x0c, 0x08, 0x08, 0x08, 0x1c, 0x00], // 0x69 'i'
    [0x10, 0x00, 0x18, 0x10, 0x10, 0x10, 0x12, 0x0c], // 0x6a 'j'
    [0x02, 0x02, 0x12, 0x0a, 0x06, 0x0a, 0x12, 0x00], // 0x6b 'k'
    [0x0c, 0x08, 0x08, 0x08, 0x08, 0x08, 0x18, 0x00], // 0x6c 'l'
    [0x00, 0x00, 0x3e, 0x2a, 0x2a, 0x2a, 0x2a, 0x00], // 0x6d 'm'
    [0x00, 0x00, 0x1e, 0x22, 0x22, 0x22, 0x22, 0x00], // 0x6e 'n'
    [0x00, 0x00, 0x1c, 0x22, 0x22, 0x22, 0x1c, 0x00], // 0x6f 'o'
    [0x00, 0x00, 0x1e, 0x22, 0x22, 0x22, 0x1e, 0x02], // 0x70 'p'
    [0x00, 0x00, 0x3c, 0x22, 0x22, 0x22, 0x3c, 0x20], // 0x71 'q'
    [0x00, 0x00, 0x1a, 0x06, 0x02, 0x02, 0x02, 0x00], // 0x72 'r'
    [0x00, 0x00, 0x3c, 0x02, 0x1c, 0x20, 0x1e, 0x00], // 0x73 's'
    [0x04, 0x04, 0x1e, 0x04, 0x04, 0x04, 0x18, 0x00], // 0x74 't'
    [0x00, 0x00, 0x22, 0x22, 0x22, 0x22, 0x3c, 0x00], // 0x75 'u'
    [0x00, 0x00, 0x22, 0x22, 0x22, 0x14, 0x08, 0x00], // 0x76 'v'
    [0x00, 0x00, 0x22, 0x22, 0x2a, 0x2a, 0x14, 0x00], // 0x77 'w'
    [0x00, 0x00, 0x22, 0x14, 0x08, 0x14, 0x22, 0x00], // 0x78 'x'
    [0x00, 0x00, 0x22, 0x22, 0x22, 0x14, 0x08, 0x04], // 0x79 'y'
    [0x00, 0x00, 0x3e, 0x10, 0x08, 0x04, 0x3e, 0x00], // 0x7a 'z'
    [0x10, 0x08, 0x08, 0x04, 0x08, 0x08, 0x10, 0x00], // 0x7b '{'
    [0x08, 0x08, 0x08, 0x08, 0x08, 0x08, 0x08, 0x08], // 0x7c '|'
    [0x04, 0x08, 0x08, 0x10, 0x08, 0x08, 0x04, 0x00], // 0x7d '}'
    [0x00, 0x00, 0x00, 0x24, 0x2a, 0x12, 0x00, 0x00], // 0x7e '~'
    [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00], // 0x7f DEL
];

#[cfg(test)]
mod tests {
    use super::BASIC;
    extern crate std;
    use std::string::String;

    /// **The picture and the bits are the same font.**
    ///
    /// `kaypro-style-7x8.art` is what a person edits and reviews, because hexadecimal is not a
    /// medium anyone can see a letter in; the table above is what compiles. Two representations of
    /// one thing is a drift bug waiting to happen, so the `.art` is parsed here and compared. A
    /// glyph fixed in one and not the other fails at `cargo test -p bitmap_font` rather than on a
    /// screen.
    #[test]
    fn the_art_file_and_this_table_agree() {
        let art = include_str!("../kaypro-style-7x8.art");
        let mut drawn = [[0u8; 8]; 128];
        let mut current: Option<usize> = None;
        let mut row = 0usize;
        for line in art.lines() {
            let line = line.trim_end();
            if line.trim_start().starts_with(';') || line.trim().is_empty() {
                continue;
            }
            if let Some(rest) = line.trim_start().strip_prefix('@') {
                let code = rest.split_whitespace().next().unwrap_or("");
                current = usize::from_str_radix(code, 16).ok();
                assert!(current.is_some(), "unparsable code point in {line:?}");
                row = 0;
                continue;
            }
            let code = current.expect("a picture row before any @ code point");
            assert!(row < 8, "glyph {code:#04x} has more than eight rows");
            assert_eq!(
                line.chars().count(),
                7,
                "glyph {code:#04x} row {row} is not seven columns"
            );
            for (x, ch) in line.chars().enumerate() {
                assert!(ch == '#' || ch == '.', "glyph {code:#04x} has {ch:?} in it");
                if ch == '#' {
                    drawn[code][row] |= 1 << x;
                }
            }
            row += 1;
        }
        for byte in 0..128usize {
            assert_eq!(
                drawn[byte],
                BASIC[byte],
                "{:#04x} differs between kaypro-style-7x8.art and this table:\n{}",
                byte,
                side_by_side(&drawn[byte], &BASIC[byte]),
            );
        }
    }

    /// **Every row of the table is read, which is what the coverage floor is actually asking for.**
    ///
    /// `the_art_file_and_this_table_agree` compares all 128 glyphs, but it reads them through a
    /// loop over a parsed picture, so a line-coverage tool sees the parser execute and the 128
    /// literal rows never "run". That is the shape of every generated-data file and the reason
    /// `script/coverage`'s exemption list exists; this file does not belong on that list, because
    /// its own comment says the list is for things that **physically cannot run**, never for things
    /// that are merely untested.
    ///
    /// So this asserts properties of the whole table rather than exempting it. Each is cheap and
    /// each would have caught a real transcription accident:
    ///
    /// - **No printable character is blank.** A row of zeroes where a letter should be is the
    ///   commonest way a hand-transcribed table goes wrong, and it is invisible until somebody
    ///   types that character.
    /// - **Space is blank**, which is the same assertion from the other side.
    /// - **Nothing sets a bit outside the seven drawn columns.** The cell is 7 wide with a
    ///   one-pixel gutter each side, so bit 7 must be clear in every row of every glyph; a stray
    ///   bit there would smear into the neighbouring cell.
    /// - **No two printable glyphs are identical.** Two characters that draw the same picture is a
    ///   copy-paste that a reader cannot see and a specimen sheet barely shows.
    #[test]
    fn every_glyph_in_the_table_is_read_and_plausible() {
        assert_eq!(BASIC[0x20], [0; 8], "space must be blank");

        for (byte, rows) in BASIC.iter().enumerate().take(0x7F).skip(0x21) {
            assert_ne!(
                *rows, [0; 8],
                "{byte:#04x} ({:?}) is blank in the table",
                byte as u8 as char
            );
            for (row, bits) in rows.iter().enumerate() {
                assert_eq!(
                    bits & !0x7F,
                    0,
                    "{byte:#04x} row {row} sets a bit outside the seven drawn columns"
                );
            }
        }

        for (a, ra) in BASIC.iter().enumerate().take(0x7F).skip(0x21) {
            for (b, rb) in BASIC.iter().enumerate().take(0x7F).skip(a + 1) {
                assert_ne!(
                    ra, rb,
                    "{a:#04x} ({:?}) and {b:#04x} ({:?}) draw the same glyph",
                    a as u8 as char, b as u8 as char
                );
            }
        }
    }

    /// **The failure diagnostic is exercised, because a message nobody has seen is one nobody can
    /// trust.**
    ///
    /// `side_by_side` runs only when a glyph differs, so on a green tree it never executes and the
    /// coverage floor counts it against this file. That is worth fixing rather than exempting: this
    /// helper is what a person reads at 2am when the table and the picture disagree, and a
    /// transposed loop or a reversed bit test in it would send them looking at the wrong glyph.
    /// This feeds it a known difference and checks the picture it draws.
    #[test]
    fn the_failure_diagnostic_draws_both_shapes() {
        let art = [0b000_0001u8, 0, 0, 0, 0, 0, 0, 0];
        let table = [0b100_0000u8, 0, 0, 0, 0, 0, 0, 0];
        let out = side_by_side(&art, &table);
        let first = out.lines().nth(1).expect("a first picture row");

        // Bit 0 is the leftmost drawn column and bit 6 the rightmost, so these two differ at
        // opposite ends: the assertion fails if the bit test is ever reversed.
        assert_eq!(first, "#......  ......#  ", "got {first:?} from\n{out}");
        assert_eq!(out.lines().count(), 9, "a header and eight rows:\n{out}");
        assert!(out.starts_with("  art     table\n"), "no header in\n{out}");
    }

    /// The two shapes as pictures, because a diff of sixteen hex bytes says nothing.
    fn side_by_side(art: &[u8; 8], table: &[u8; 8]) -> String {
        let mut out = String::from("  art     table\n");
        for y in 0..8 {
            for &rows in &[art, table] {
                for x in 0..7 {
                    out.push(if rows[y] >> x & 1 != 0 { '#' } else { '.' });
                }
                out.push_str("  ");
            }
            out.push('\n');
        }
        out
    }
}
