[no text block returned; the model spent its budget on thinking]

Let me carefully review this commit. It's a documentation-only change to the nife repository: a new roadmap milestone file (526), deletion of a proposal file, and edits to two notes files (benchmarks.md and tsc-under-tcg.md).

Let me look for issues:

1. **The commit message and the milestone file say "+1153%" but the new section header in benchmarks.md says "wrong by up to 12x"**. Let me check: +1153% means the value is 12.53x the true value (100% + 1153% = 1253% of truth, i.e., 12.53x). The section title in benchmarks.md says "the x86 boot calibration was wrong by up to 12x". Hmm, +1153% is 12.53x, so "12x" is roughly right but inconsistent with "up to +1153%" stated elsewhere. Actually "wrong by up to 12x" — a 125 ms window reported as 10 ms is 12.5x. Saying "12x" is a rounding; might be fine but slightly inconsistent. Actually wait — elsewhere in the same file the correction note says "wrong by up to **+1153%**". The section header says "wrong by up to 12x". These are two different ways of saying the same thing (12.53x ≈ 12x). It's a mild inconsistency but arguably fine. Hmm, but is it? "+1153%" is the error; "12x" is the ratio. A reader might be confused. This is a stylistic concern at most.

2. **notes/tsc-under-tcg.md says "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** Tense issue: "are wrong" — the 200 boots were runs of the old estimator? Actually the table says for 16 windows: 0/200 boots wrong by >1%, worst +0.47%. These are from the same 200-boot sweep where each boot timed 16 windows, so "after the fix" refers to the min-of-16 estimator applied to the same data. The phrasing "0 of those 200 boots are wrong" is okay-ish. But wait — the fix isn't min-of-16 always; it's min with early stopping at agreement within 1/1000, capped at 16. The milestone says the stopping rule reaches "exactly the accuracy of taking the full cap every time, boot for boot". So saying "after the fix, 0 of those 200 boots are wrong by more than one per cent" is consistent with the milestone's claim. OK.

3. **The deleted proposal file was titled "can be wrong by 4x" and the note tsc-under-tcg.md previously pointed to it. The new text says "which is what the proposal this paragraph used to point at became."** That's fine.

4. **Check the milestone file's claims for internal consistency:**

- "Twenty-two boots of one binary stored 1001 MHz to 4330 MHz. This lane widened that to 490 boots at three host loads" — but the table shows 200 boots at load 30. And the tsc-under-tcg.md note says "Milestone 526's sweep ran 490 boots at three host loads and saw +1153%, with 56 of 200 boots at load 30 wrong by more than one per cent." OK consistent.

- Wait, there's an inconsistency: the milestone says "This lane widened that to 490 boots at three host loads and the shape held without a single exception: every boot high, none ever low." Then later: "Measured over the same 490 boots, this reaches exactly the accuracy of taking the full cap every time, boot for boot, at a mean of: ... Ordinary session load 3.53 windows..." OK.

- The table "Windows | Median error | 99th percentile | Worst of 200 | Boots wrong by >1%" — the row "1 (what shipped)" shows worst +1153%. But earlier the text says "the worst this lane saw, +1153%, is a 125 ms one." Check: 1000 MHz true, +1153% → 12,530 MHz. A 125 ms window reported as 10 ms → 12.5x → 12,500 MHz → +1150%. Close enough (1153% vs 1150%, rounding). OK.

- "Five still leaves one boot in eighteen wrong by more than a per cent" — 11/200 = 1 in 18.2. OK.

- "At ordinary session load one window's worst of 30 boots was +773%, and min-of-9 got to +0.01%; at load 22 the same estimators gave +318% and +0.34%." Hmm, 490 boots at three host loads: 200 at load 30, and... 30 at ordinary, and some at load 22? 200 + 30 = 230; remaining 260 at load 22? But then "min-of-9 got to +0.01%" at ordinary load — fine. Actually wait, "at load 22 the same estimators gave +318% and +0.34%" — "the same estimators" = one window and min-of-9. OK.

Hmm, but the note tsc-under-tcg.md says "56 of 200 boots at load 30 wrong by more than one per cent" — consistent with the table row 1: 56/200. Good.

5. **A key inconsistency: the milestone says the table is "Two hundred boots on a host deliberately saturated to load 30 on eight cores, each timing sixteen windows and reporting every one". But the benchmarks.md new section says "Two hundred boots against a counter known to tick at 1000.000 MHz, on a host deliberately saturated to load 30 on eight cores" — consistent.**

6. **Now, the correction note inserted into benchmarks.md at line ~2664 says "the rate each of these eleven boots stored was not written down". Earlier: "Six boots debug, five release" — that's eleven boots. OK consistent.**

Wait — actually let me check: the text after the inserted block says "Six boots debug, five release; `ns/iter` computed from the guest's own calibrated TSC". So eleven boots. The commit message says "each of those eleven boots stored a different wrong rate and none was written down." OK.

7. **The correction note says "the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly."** Hmm — is the 4.2x a ratio within a boot? I can't verify from the truncated file, but the claim that ratios within a single boot cancel is mathematically sound if the calibration is constant within a boot (it is — stored once at boot). But wait: is the debug-versus-release comparison a ratio within one boot? Debug and release are different binaries, hence different boots! A debug build and a release build would be run in separate boots, each with its own calibration. So a ratio between a debug number and a release number would NOT cancel the calibration, because the two boots stored different rates!

Hmm, but the note says "the debug-versus-release argument ... are ratios within a boot". Let me think. The 2026-08-24 section (the x86 ns/iter table) — "Six boots debug, five release; ns/iter computed from the guest's own calibrated TSC". If the debug-versus-release comparison compares debug boots against release boots, the calibration does NOT cancel across boots, since each boot had a different wrong rate. The correction note claims "every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly."

If the debug-versus-release argument compares a debug boot's figure to a release boot's figure, that's a ratio across boots, not within a boot, and the calibration does not cancel. Unless the 4.2x was measured within a single boot somehow (e.g., both debug and release binaries run in one boot? Unlikely).

This is a potential real concern: the claim "the calibration cancels exactly in a ratio, and that is all of them" is only valid for ratios of two rows from the same boot. The commit message says "every conclusion resting on a ratio between two rows of one boot, because the calibration cancels exactly in a ratio, and that is all of them." The benchmarks.md correction says "the debug-versus-release argument and the 4.2x are ratios within a boot."

Without the full 2026-08-24 section (it's in the truncated part of benchmarks.md), I can't fully verify whether the debug-vs-release comparison is within-boot or cross-boot. But given "Six boots debug, five release", the debug and release numbers come from different boots. If the "debug-versus-release argument" compares the debug table to the release table, that's cross-boot and the calibration does not cancel — the ratio would be off by the ratio of the two boots' calibration errors (which could be large, e.g., one boot +0.3% and another +300%).

Hmm wait, but actually let me reconsider. ns/iter computed from the guest's calibrated TSC: ns/iter = ticks / rate. If a boot's rate is inflated by factor f, then ns/iter is deflated by f. A ratio of two rows within the same boot: (ticks1/rate) / (ticks2/rate) = ticks1/ticks2 — calibration cancels. A ratio across two boots with rates r1 and r2: (ticks1/r1)/(ticks2/r2) = (ticks1/ticks2)·(r2/r1) — calibration does not cancel; it's multiplied by r2/r1, the ratio of the two boots' errors.

So if the debug-vs-release argument is cross-boot, the correction note's claim is wrong. The note explicitly asserts "the debug-versus-release argument and the 4.2x are ratios within a boot". Given the section reports six debug boots and five release boots, any debug-vs-release comparison is necessarily across boots (different binaries can't be in the same boot... well, actually could they? A single boot could run both debug and release benchmarks if both binaries were included — but that's not how it works; the bench kernel is one binary, debug or release).

Hmm, but wait — maybe the "debug-versus-release argument" in the 2026-08-24 section is about something else. The file was truncated at 1200 lines, and the 2026-08-24 section is around line 2664, so we don't have it. The concern is plausible but I can't verify. As a reviewer, I could flag it as a question: "the debug-versus-release argument compares numbers from different boots (six debug boots, five release boots), so the calibration does not cancel — each boot stored a different wrong rate."

Actually, hold on. Let me re-read the correction note: "every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly."

Hmm, "the same run" vs "within a boot". If the debug-versus-release argument is about the ratio between debug and release, and debug and release were run in separate boots, then the claim is false. This seems like a genuine potential concern worth flagging as a question.

Actually, wait. Let me think about what the 2026-08-24 x86 section likely contains. From the correction: "The 2026-08-24 x86 `ns/iter` table (`yield_switch`, `tss_iomap_switch`, debug and release)". So there are two benchmarks, each in debug and release. The "4.2x" might be the debug-to-release tax on x86 (similar to the ~6.7x on aarch64). If the 4.2x is computed as debug ns/iter ÷ release ns/iter, that's across boots. The debug boots' calibration errors differ from the release boots' errors. So the 4.2x ratio does NOT cancel the calibration.

Unless... the ratio is computed per-boot somehow? No — a debug boot has no release numbers and vice versa.

So the claim "the calibration cancels exactly in a ratio, and that is all of them" appears to be wrong for the debug-versus-release argument, unless each boot's stored rate happened to be similar (which they weren't — "each of those eleven boots stored a different wrong rate").

Hmm, but hold on. Actually, maybe I should think about this differently. What if the debug-vs-release comparison in that section compares ratios that are each within-boot? E.g., "the ratio of yield_switch to tss_iomap_switch is X in debug and Y in release" — each ratio is within a boot, and comparing ratios-of-ratios across boots would still carry the calibration ratio... no wait, ratios within boot cancel calibration entirely, so comparing two within-boot ratios across boots is fine. E.g., if the argument is "tss_iomap_switch is 4.2x yield_switch" — is that within one boot? If both benchmarks run in the same boot, then yes, the ratio cancels. The "debug-versus-release argument" might be: "the debug-to-release ratio for yield_switch is A and for tss_iomap_switch is B" — hmm, that's cross-boot per benchmark.

Actually, "the debug-versus-release argument and the 4.2x" — these might be the same thing: the debug build is 4.2x slower than release. That would be cross-boot.

I genuinely can't verify without the section. As a reviewer, the right move is to flag this as a question/concern: the claim that the debug-versus-release ratio cancels the calibration is only true if both numbers come from the same boot, but the section says six debug boots and five release boots, so the comparison is across boots with different wrong rates. This is a legitimate concern to raise.

8. **"and that is all of them" — the commit message says every conclusion rests on a ratio between two rows of one boot. But the benchmarks.md correction also says "What is not safe is quoting any single figure below as a nanosecond count." OK.**

9. **The new section in benchmarks.md says "`coremark`'s self-reported rate on x86 read the stored number, so any x86 CoreMark score computed from it carried the same error. No such score is published; the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'."** Wait — "the 2026-09-21 section above". The new section is dated 2026-09-21 and is appended at the end of the file. It refers to "the 2026-09-21 section above" — is there another 2026-09-21 section above it? The file ends with a section about the fastpath gate (the last lines before the new section: "...already carries `x86_trap`, is already 152 bytes, and whose gate is green with room (+1.0%, +1.4%, +3.9%)..."). Hmm, the CoreMark "reports correctness, not yet a score" quote is from the milestone-19e section ("This binary reports correctness, not yet a score"), which is not dated 2026-09-21 — it's from 2026-07. So the reference "the 2026-09-21 section above" seems wrong. Let me check the truncated file for a 2026-09-21 section... There are sections "The floors name the nightly they were read against (2026-09-21)" and "The emulator is the other half of the same fact (2026-09-21)". Neither mentions CoreMark. The CoreMark quote is in "Compute vs. OS primitives: two benchmarks that measure different things (milestone 19e)" — "This binary reports correctness, not yet a score; timing a run needs a userspace clock..." That section is dated 2026-07 (milestone 19e era). So "the 2026-09-21 section above" is a wrong cross-reference. It should say something like "the milestone-19e section above". This is a genuine concern: a wrong cross-reference in a file that's part of the deliverable.

Let me double check by searching the before-file content for "reports correctness, not yet a score": Yes, in the 19e section: "This binary reports correctness, not yet a score; timing a run needs a userspace clock (enabling the EL0 virtual-counter read, as Linux does for its vDSO), which lands with the cross-OS suite rather than here." That section header is "## Compute vs. OS primitives: two benchmarks that measure different things (milestone 19e)". No date 2026-09-21. So the cross-reference is wrong. Also, could there be a 2026-09-21 section in the truncated portion (lines 1200–2664)? Possibly! The file was truncated at 1200 lines by the review script, and the CoreMark section visible is around line ~600. Hmm, wait — the truncation note says "[... truncated at 1200 lines by scripts/review-bundle.sh ...]" and then the diff shows the insertion at line 2664. So there could be a 2026-09-21 section between lines 1200 and 2664 that mentions CoreMark. Hmm. But the quote "reports correctness, not yet a score" matches exactly the 19e section text. And the new section says "the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'". Given the exact quote match with the 19e section, and the 19e section is not dated 2026-09-21, this looks like a misreference. But I can't be 100% sure there isn't another 2026-09-21 section in the truncated middle that also says it. The exact phrase match strongly suggests the 19e section is the referent. I'll flag it as a likely-wrong cross-reference.

Actually wait, also: there might be a 2026-09-21 coremark section in the truncated region — the file has many dated sections. The truncation happened at line 1200 of ~3800 lines. The visible 19e section is within the first 1200 lines. The phrase "reports correctness, not yet a score" appears in the visible part. If a later 2026-09-21 section repeated that phrase, we can't see it. But the new section also says "No such score is published" — and refers to "the 2026-09-21 section above". Given uncertainty, I'll flag it as a question with the evidence.

Hmm, hold on. Also the new section says "the 2026-09-15 lazy-TSS section and the icount baselines are not affected. They quote deterministic ticks under -icount and say so". Is there a 2026-09-15 lazy-TSS section? Possibly in the truncated region. Can't verify. Fine.

10. **The correction note inserted at the 2026-08-24 table says "See the 2026-09-21 section at the end of this file, and `kernel/src/arch/x86_64/timer.rs`'s `BUGS`."** OK, the new section is at the end. Fine.

11. **Markdown formatting issue in benchmarks.md: the new section is appended directly after the previous section's last paragraph without a blank line.** Look at the diff:

```
 teaches people to route around it.
+## 2026-09-21: the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above
```

The added line starts with `## 2026-09-21...` immediately after "teaches people to route around it." with no blank line between. In Markdown, a heading needs a blank line before it to be recognized (in most flavors; CommonMark actually allows ATX headings after a paragraph without a blank line? Let me recall: In CommonMark, an ATX heading can interrupt a paragraph — yes, ATX headings can interrupt paragraphs in CommonMark. But many renderers (GitHub uses CommonMark with GFM extensions) do recognize it. Actually GFM/CommonMark: "ATX headings ... can interrupt a paragraph" — yes, unlike setext headings, ATX headings can interrupt a paragraph. So it would render. But it's sloppy style for the repo. Also the previous section ends and the new `##` heading begins without a blank line — in the raw text it reads as run-together. Many Markdown linters (MD022) require blank lines around headings. This is a formatting nit worth flagging.

12. **Similarly, in tsc-under-tcg.md the added text: check the diff context.**

```
 in the x86 suite. Deadlines fail safe (an inflated rate makes a two-second timeout longer in real
 time, never shorter); the reported numbers do not.
 
-**This is tracked**: `design/roadmap/proposals/the-x86-boot-calibrates-once-and-can-be-wrong-by-4x.md`,
-and `arch::x86_64::timer`'s `BUGS` section now says it where a reader meets the feature.
+**FIXED, 2026-09-21, the same day**, by milestone 526 ...
```

Looks fine with blank lines.

13. **Now check the milestone file for issues:**

- "**Status: BUILT** 2026-09-21." — fine.
- "*(Number provisional until the merge queue lands it; 524 onward is contested between in-flight branches.)*" — fine, they note it.
- "Milestone 524 (the three x86_64 boot gates: NX, SYSCALL, and the invariant TSC) had just argued that a non-invariant TSC 'cannot be caught by measuring harder'." OK.
- The table: "1 (what shipped) | +0.36% | +884% | **+1153%** | 56 / 200". Hmm, the 99th percentile of 200 boots is the 198th value; fine.
- "Five still leaves one boot in eighteen wrong by more than a per cent and a worst case of +51%." 11/200 ≈ 1/18. OK.
- "The tail only closes at sixteen." OK.
- "A tighter tolerance (one part in 2000) moved the mean by 0.07 windows and the error distribution not at all" OK.
- "`script/test --arch x86_64` boots the kernel **four** times, and those four boots took 3, 3, 4 and 3 windows. So the suite pays **90 ms** more than the one-window design did" — check: old design = 1 window per boot = 4 windows total = 40 ms. New: 3+3+4+3 = 13 windows = 130 ms. Extra = 9 windows = 90 ms. OK. "against a 3m38s runtime: 0.04%" — 90 ms / 218 s = 0.0413%. OK.
- "**calef's five is what this costs; sixteen is only what it is willing to spend when the host is fighting.** That the measured mean on a saturated host came out at 5.20 is a coincidence and is recorded as one." OK.
- The CPUID 0x15 discussion: "QEMU's TCG does not offer the leaf under any invocation tried" — plausible.
- RTC discussion: "Its seconds register has one-second granularity... `tsc_probe` gets its precision by watching for an edge and then spanning thirty-two seconds" — consistent with tsc-under-tcg.md.
- "What is in the diff" — mentions `kernel/src/main.rs`, `timer.rs`, `timer_calibration_tests.rs`, `tsc_probe.rs`, `scripts/qemu-runner-x86_64.sh`, `notes/benchmarks.md`. But the commit under review only contains the docs! The commit message says "Record the calibration fix: milestone 526, the note, and the suspect numbers" — so this commit is only the design records; the code changes presumably landed in a separate commit. That's fine for this review since we're reviewing this commit. But wait — the milestone says Status: BUILT and describes code changes (timer.rs, main.rs, timer_calibration_tests.rs, tsc_probe.rs, qemu-runner-x86_64.sh) that are NOT in this commit. As a PR review, that's a process question: does the PR contain only docs while claiming BUILT? The prompt says "the commit as it would appear in a pull request" — and the diff only has the three markdown files. The milestone claims BUILT status and lists code files in "What is in the diff" — but none of those code changes are in this diff. Hmm. Is that a concern? The commit message explicitly says "Record the calibration fix: milestone 526, the note, and the suspect numbers" — it's a records commit. The repo treats design records as part of the deliverable. It's plausible the code landed in a preceding commit and this commit records it. I shouldn't flag missing code as a concern per se, but the milestone's "What is in the diff" section listing files not in this commit could be confusing — though "the diff" likely refers to the milestone's whole change set, not this commit. This is ambiguous; I might flag it lightly or not at all. Given the instruction "one concern per item, each naming the file and stating what is wrong and why it matters" — I should focus on real defects.

- BUGS section: "0 of 200 boots at load 30 were wrong by more than 1%; that is a measured distribution and not a guarantee." OK.

- Follow-on: "**Done.** The proposal this promotes, `design/roadmap/proposals/the-x86-boot-calibrates-once-and-can-be-wrong-by-4x.md`, is removed by this milestone: its two open questions are both answered above (the boot does print its confidence, and the local APIC timer is derived from the same windows)." — Wait, "The proposal this promotes" — the proposal was titled "can be wrong by 4x". The milestone promotes it to BUILT and removes it. OK. But: "its two open questions are both answered above ... the local APIC timer is derived from the same windows". The BUGS section says "The local APIC timer's rate gets the same treatment and has no CPUID escape. It is the minimum over the same windows, taken independently of the TSC's." Hmm — "the minimum over the same windows, taken independently" — so the APIC timer is measured in each window and takes its own min. "Derived from the same windows" is consistent-ish. OK.

- "Index row" section: "The x86 boot measured the TSC against a single 10 ms PIT window... wrong by up to +1153% against a counter known to tick at 1000.000 MHz, high on every one of 490 boots and never low". OK.

- "This milestone takes the smallest of several windows instead, which is the right estimator precisely because the error is one-sided, and stops as soon as two windows agree to one part in a thousand, so the mean cost is 3.5 windows on a quiet host and 5.2 on a saturated one against a cap of sixteen." OK.

- "calef's ruling said five; five was measured and found to leave a +51% worst case, which is why the cap is sixteen and the five is what it spends." OK.

14. **Now, a mathematical/statistical claim to check: "An average would be a biased estimator for precisely the reason the minimum is an unbiased one"**. Hmm — is the minimum an *unbiased* estimator? No! The minimum of several windows is itself biased upward: each window is an upper bound on... wait, each window's measured rate is ≥ true rate (since measured window ≥ 10 ms real time attributed to 10 ms → rate overestimated). The minimum of several overestimates is still an overestimate (the smallest overestimate), so it's still biased, just less so. The text says "the minimum of several is the tightest bound taken, and it converges on the truth from above. An average would be a biased estimator for precisely the reason the minimum is an unbiased one: it carries every descheduling into the answer instead of discarding it."

Calling the minimum "an unbiased [estimator]" is statistically wrong: the minimum is still biased (E[min] > true rate whenever there's any chance all windows are inflated; even in the best case, the min converges to the true rate only as the number of windows → ∞, making it consistent/asymptotically unbiased, not unbiased). Actually, let me think more carefully. If some windows are exactly clean (poll notices terminal count with some delay even without descheduling — polling granularity means even a clean window notices late by up to one poll iteration), then even clean windows overestimate slightly. The data shows min-of-16 at median +0.00% and worst +0.47% — so it's small but the estimator is biased upward by construction. The milestone's own BUGS says "This is a probabilistic filter, not a bound. The minimum is only as good as the best window the host allowed" — which contradicts the "unbiased" claim. The proposal (deleted) said "the minimum of several windows converges on the truth from above" — that's the correct characterization (converges from above = asymptotically unbiased / consistent). The milestone text "the minimum is an unbiased one" overstates. Is this worth flagging? The sentence: "An average would be a biased estimator for precisely the reason the minimum is an unbiased one: it carries every descheduling into the answer instead of discarding it." The min does NOT discard every descheduling — it discards all but the least-descheduled window; if all windows are descheduled, the min carries the smallest descheduling into the answer. And the milestone's own BUGS section says exactly that ("a host that never leaves the vCPU alone for ten unbroken milliseconds produces an inflated rate however many windows are taken"). So the "unbiased" claim is contradicted within the same file. That's a genuine technical inaccuracy in a design record. Worth flagging.

Also the earlier statement in the same section: "Every window is an *upper bound* on the true rate, so the minimum of several is the tightest bound taken, and it converges on the truth from above." — "converges on the truth from above" is the right framing; "unbiased" later is the wrong word. Flag it.

15. **Check: "One descheduling of the QEMU thread inside that window inflates the answer without bound: a 4330 MHz reading is a 43 ms window reported as a 10 ms one, and the worst this lane saw, +1153%, is a 125 ms one."** +1153% → 12.53 GHz → 125.3 ms window. OK.

16. **Check the "agreement" stopping rule claim: "for two windows to land within a thousandth of each other they must both have been left alone, because two independent deschedulings agreeing to three decimal places is not a thing that happens."** Hmm — is that reasoning sound? Two windows could both be clean and agree. Two windows could both be inflated by similar amounts if the deschedulings are similar in magnitude — e.g., a periodic host interrupt or a timer tick that deschedules the vCPU for a consistent duration could inflate consecutive windows by nearly the same amount. E.g., if the host has a periodic 1 ms timer tick that preempts the QEMU thread once per window, every window would be inflated by ~1 ms → all windows read ~1100 MHz and agree within 1/1000. Then the stopping rule would stop at two windows and accept a +10% error. Is that scenario realistic? "two independent deschedulings agreeing to three decimal places is not a thing that happens" — but correlated/periodic deschedulings are a thing. The data (490 boots) apparently validated the rule empirically ("reaches exactly the accuracy of taking the full cap every time, boot for boot"). So empirically fine on that host. The milestone's BUGS does say "This is a probabilistic filter, not a bound." I think the empirical validation covers it; the rhetorical overstatement "is not a thing that happens" is a bit strong but they measured it. Probably not worth flagging as a top concern, though a reviewer might note the correlated-preemption scenario. Given they measured 490 boots at three loads and the stopping rule matched the full cap exactly, it's defensible. I'll leave it.

17. **"Measured over the same 490 boots, this reaches exactly the accuracy of taking the full cap every time, boot for boot"** — wait, the 200-boot table was load 30 with sixteen windows each. The 490 boots across three loads — did each boot time sixteen windows? "Two hundred boots ... each timing sixteen windows and reporting every one". For the other 290 (30 at ordinary, 260 at load 22?), they said "one window's worst of 30 boots was +773%, and min-of-9 got to +0.01%" — so those boots timed at least 9 windows. OK, plausible.

18. **tsc-under-tcg.md edit: "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** Wait — "those 200 boots" — the 200 boots at load 30. But the fix isn't "min of 16"; it's min with early stop. The milestone says the stopping rule matches the full cap exactly, so 0/200 and +0.47% holds for the fix. OK. But "are wrong" — present tense for boots that already happened; minor. Not worth flagging.

19. **The note's FIXED paragraph says "the mean cost is 3.5 windows on a quiet host" — matches milestone's 3.53. OK.**

20. **Check the benchmarks.md new section: "Two `script/bench --x86 --real` runs of the same tree, differing in one constant (`CALIBRATION_WINDOW_CAP` at 1 and at 16), so the before case is the old estimator exactly."** Hmm — with cap at 1, does the stopping rule matter? The stopping rule "stops as soon as two windows agree" requires at least 2 windows; with cap 1, it takes 1 window. So cap=1 reproduces the old estimator. OK.

But wait: "differing in one constant (CALIBRATION_WINDOW_CAP at 1 and at 16)" — is the constant named CALIBRATION_WINDOW_CAP? We can't verify code. Fine.

21. **"Before (one window) | 1003.7 MHz | +0.37%" and "at this host's load roughly one boot in four was wrong by more than a per cent"** — hmm, where does "one boot in four" come from? The load-30 table says 56/200 = 28% ≈ one in four. But the bench runs were presumably at ordinary session load, not load 30. At ordinary load, one window's worst of 30 boots was +773%... but how many of the 30 were >1%? Not stated. The claim "at this host's load roughly one boot in four was wrong by more than a per cent" — "this host's load" during the before/after pair is presumably ordinary session load. The 1-in-4 figure comes from load 30 (56/200). Applying it to "this host's load" (ordinary) may be a misattribution. Hmm, but maybe the host was loaded during the pair? Not stated. This is a potential inconsistency: the 56/200 figure is from the deliberately saturated host, but the sentence presents it as "at this host's load" for the bench pair. Let me re-read:

"**That before figure is one draw and a lucky one**, which is the honest caveat and the reason the table further up is the real evidence: at this host's load roughly one boot in four was wrong by more than a per cent, and the worst seen was +1153%."

Hmm, "at this host's load" — ambiguous. If it means "at the load this host was under during the sweep" (load 30), then 56/200 ≈ 1 in 4 is right. If it means the load during the before/after pair, it's unsourced. The antecedent "this host's load" most naturally reads as the load on the host (patagonia) — but which load? The 1-in-4 comes from the saturated sweep. A careful reader would want "at load 30" stated. This is a minor precision issue. Could flag, but it's borderline. Actually, "roughly one boot in four" = 56/200 = 28%. Yes ≈ 1/4. And "the worst seen was +1153%" also from load 30. So the sentence is describing the load-30 sweep but says "at this host's load" without saying load 30. Since the before/after pair was likely run at ordinary load (no mention of saturation), and at ordinary load the one-window worst of 30 boots was +773% with unknown fraction >1%... Actually at ordinary load, the 12 boots in the original note ranged 1001–1042, i.e., up to +4.2%, and "a single ad-hoc run" caught 1326 MHz (+32.6%). So at ordinary load, >1% errors were common too (1004, 1005, 1042 are >1%... let's see: 1001 = +0.1%, 1002 = +0.2%, 1003 = +0.3%, 1004 = +0.4%, 1005 = +0.5%, 1042 = +4.2%. So 1 of 12 at ordinary load was >1%). Hmm, so "one boot in four" doesn't match ordinary load (1/12). The sentence conflates. But it's hedged with "roughly" and the table is right there. Borderline; maybe flag as precision.

Actually, hmm, wait. Let me re-read the table in benchmarks.md: "| 1 (what shipped) | +0.36% | +884% | **+1153%** | 56 / 200 |". So 56/200 = 28% at load 30. The sentence "at this host's load roughly one boot in four was wrong by more than a per cent" — I think "this host's load" refers to the load-30 condition from the table "further up" which is "the real evidence". The sentence structure: "the reason the table further up is the real evidence: at this host's load roughly one boot in four..." — the table further up IS the load-30 table. So "this host's load" = the load in that table = load 30. It's compressed but defensible. I won't flag it, or maybe lightly. Eh.

22. **Check the milestone's "The defect, which another lane found while looking for something else" section: "Milestone 524 ... had just argued that a non-invariant TSC 'cannot be caught by measuring harder'. The `tscdrift` lane then went to check whether TCG's TSC drifts at all, built an RTC-referenced probe, and found that it does not: the counter ticks at exactly 1000.000 MHz, constant to within 42 ppm whatever the host is doing."** Consistent with the note.

23. **"Twenty-two boots of one binary stored 1001 MHz to 4330 MHz."** The note says twelve boots at ordinary load and ten saturated = 22. Range 1001–4330. OK.

24. **tsc-under-tcg.md: "**FIXED, 2026-09-21, the same day**, by milestone 526 (the x86 TSC calibration takes the smallest of several windows), which is what the proposal this paragraph used to point at became."** OK.

25. **"This note recorded 4330 MHz as the worst of twenty-two boots. Milestone 526's sweep ran 490 boots at three host loads and saw **+1153%**, with 56 of 200 boots at load 30 wrong by more than one per cent."** — wait, "+1153%" as "the worst" — 4330 MHz was +333%. The sweep saw +1153%. Consistent with milestone. OK.

26. **Commit message: "notes/tsc-under-tcg.md's finding is marked fixed, with the correction that measuring harder made the defect worse: 4330 MHz over twenty-two boots became +1153% over 490."** OK.

27. **Now the "12x" vs "+1153%" in the benchmarks.md section title.** "the x86 boot calibration was wrong by up to 12x" — +1153% means the stored rate was up to 12.53x the true rate. "Wrong by 12x" — the error as a multiplier is 12.53x. Saying "up to 12x" slightly understates (12.53 > 12). Also inconsistent register with the rest of the tree which says +1153%. Also the milestone says "wrong by up to **+1153%**". The proposal was titled "wrong by 4x" (4330 MHz = 4.33x). Hmm, "wrong by up to 12x" — is that an understatement? 12.53x rounds to 13x if anything, or "12.5x". "12x" is a floor. It's a minor numeric inconsistency in a headline. Worth flagging? It matters because the tree elsewhere says +1153% (which is 12.53x), and a reader comparing "12x" and "+1153%" might not immediately reconcile them (1153% = 11.53x if you misread "%" as "x" — the confusion potential is real: "wrong by 1153%" vs "wrong by 12x" — actually these are consistent-ish: 12.53x ≈ 12x). Hmm, "wrong by a factor of 12.5" vs "wrong by up to 12x" — fine-ish. But a headline number stated two ways in one tree is a small inconsistency. I'll consider flagging it as a minor item. Actually, I think this is worth a mention: the title says "12x" while everything else says "+1153%"; "12x" is also slightly inaccurate (12.53x). Minor.

28. **The missing blank line before the new `##` heading in benchmarks.md** — flag as formatting. In the diff:

```
 already carries `x86_trap`, is already 152 bytes, and whose gate is green with room (+1.0%, +1.4%,
 +3.9%). The exemption is measured; asserting a property the tree does not hold is how a gate
 teaches people to route around it.
+## 2026-09-21: the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above
```

Yes — no blank line between "teaches people to route around it." and the new heading. In CommonMark, ATX headings can interrupt a paragraph, so it renders as a heading on GitHub. But in the raw file it's run together, and the repo's own style everywhere else has blank lines. Also some Markdown parsers (e.g., older ones, or strict linters) won't recognize it. Also worth noting: is it even the end of that section? The previous section's last line is "...teaches people to route around it." and then the new section begins. The file's other sections are separated by blank lines. This is a real formatting concern for a docs-as-deliverable repo. Flag it.

29. **Check the correction block placement in benchmarks.md:** It's inserted between "### The numbers" and the paragraph "QEMU 11.0.2 (`.qemu-version`, pinned), `-machine q35 -cpu max`, one hart, plain TCG. Six boots debug, five release; ...". Wait — the insertion is a blockquote right after "### The numbers" and before the paragraph describing the setup. Let me look:

```
 ### The numbers
 
+> **CORRECTION, 2026-09-21: every `ns/iter` in this section is suspect, and the `ns` column alone.**
...
+
 QEMU 11.0.2 (`.qemu-version`, pinned), `-machine q35 -cpu max`, one hart, plain TCG. Six boots
 debug, five release; `ns/iter` computed from the guest's own calibrated TSC, the same arithmetic
 `xtask`'s `run_bench` already does for every other leg.
```

OK fine.

30. **"every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot"** — back to concern #7. Let me think really hard about whether the debug-vs-release argument could be within-boot.

The 2026-08-24 section: "Six boots debug, five release". The benchmarks are `yield_switch` and `tss_iomap_switch`. The "debug-versus-release argument" presumably says the debug-to-release tax is 4.2x (or similar). Since debug and release are different binaries booted separately, each boot stored its own wrong rate. The ratio debug_ns/release_ns = (debug_ticks/debug_rate) / (release_ticks/release_rate) = (debug_ticks/release_ticks) × (release_rate/debug_rate). The calibration factor (release_rate/debug_rate) does not cancel — it's the ratio of two different boots' errors. Both errors are ≥ 0 and can differ wildly (e.g., debug boot +0.3%, release boot +300%). So the 4.2x could be badly wrong.

Unless the section's "debug-versus-release argument" is about comparing within-boot ratios... I can't see the section. But the natural reading, and the correction note's own wording ("the debug-versus-release argument and the 4.2x are ratios within a boot"), asserts something that depends on both figures coming from one boot, which contradicts "Six boots debug, five release" — unless each "boot" ran both debug and release? No — a boot runs one kernel binary, either debug or release.

Hmm wait, actually — could the section compare "debug vs release" where both were computed from ticks? No, the correction says the ns column alone is suspect, ticks unaffected. If the debug-vs-release argument were made in ticks, the calibration wouldn't matter at all (ticks are raw rdtsc). But the correction note says the ns/iter figures are suspect and the conclusions survive because they're ratios within a boot. If the debug-vs-release argument were in ticks, the correction would say "it's in ticks, unaffected" — instead it says it's a ratio within a boot.

Actually, hmm, wait. Let me reconsider: maybe the debug-vs-release argument is: "for each benchmark, debug ns/iter ÷ release ns/iter". That IS cross-boot. OR maybe the argument is "within the debug table, the ratio of tss_iomap_switch to yield_switch is X; within the release table it's Y; the release build narrows the gap" — each of those ratios is within-boot (cancels), and comparing them across boots is fine because each is calibration-free. In that case "the debug-versus-release argument ... [is a ratio] within a boot" would be loose but arguably true if the argument is built from within-boot ratios.

I can't resolve this without the section text. As a reviewer, I'd flag it as a question: "Please confirm the debug-versus-release comparison is computed from within-boot ratios; debug and release figures come from different boots (six and five respectively), each of which stored a different wrong rate, so a direct debug/release ns ratio does not cancel the calibration." That's a legitimate review concern — the note makes a strong claim ("the calibration cancels exactly ... and that is all of them") that warrants scrutiny.

Actually, let me reconsider the commit message: "What survives untouched is every conclusion resting on a ratio between two rows of one boot, because the calibration cancels exactly in a ratio, and that is all of them." The strong claim "that is all of them" — if the debug-vs-release argument is cross-boot, this claim is false. Given the section explicitly says "Six boots debug, five release", any debug-vs-release comparison is across boots. I'm fairly confident this is a real logical error in the correction note. Unless the debug-vs-release argument is about within-boot ratios. The safest framing for the review: the claim that the debug-versus-release argument is "a ratio within a boot" is inconsistent with the section's own description of six debug boots and five release boots — a debug/release ratio spans two boots with two different wrong rates, so the calibration does not cancel in it. Flag it.

31. **Another check on the milestone: "the worst this lane saw, +1153%, is a 125 ms one" and the table "Worst of 200" for 1 window is +1153%.** But the table is 200 boots at load 30; the milestone also says "At ordinary session load one window's worst of 30 boots was +773%". So the overall worst across 490 was +1153% at load 30. OK.

32. **"Five still leaves one boot in eighteen wrong by more than a per cent"** — 11/200 = 5.5% = 1 in 18.2. OK.

33. **Check "the suite pays 90 ms more than the one-window design did, against a 3m38s runtime: 0.04%"** — 90/218000 = 0.0413%. OK.

34. **"an order of magnitude below the suite's own run-to-run variance"** — unverifiable, fine.

35. **"Two hundred boots on a host deliberately saturated to load 30 on eight cores, each timing sixteen windows and reporting every one, give the error of the min over the first k of them"** — so the table's rows are computed by taking the min over the first k of the 16 windows per boot. OK, method sound.

36. **The milestone says "`kernel/src/arch/x86_64/timer_calibration_tests.rs`, new: five tests of the stopping rule fed sequences by hand, and two boot assertions"** — code not in this commit. As noted, this commit is docs-only. The "What is in the diff" section of the milestone lists code files. If this PR contains only the markdown, then either the code landed separately (fine) or the milestone is claiming BUILT without code (a problem). The commit message "Record the calibration fix: milestone 526, the note, and the suspect numbers" suggests the fix landed elsewhere and this records it. Reviewing just this commit, I can't verify the code exists. Should I flag it? The milestone says Status: BUILT 2026-09-21 and lists specific code files changed; if this PR is the whole change, the claim is unsubstantiated. But the prompt frames the commit as the records portion. Hmm. The instruction says "Review it the way you would review a pull request." A PR containing only docs that declare a milestone BUILT and reference code changes not in the PR would normally get a question: "where's the code?" But the commit message is explicit that this commit records the fix — implying the fix is elsewhere. I think a brief question is fair but might be noise. Let me think about what a careful reviewer would do. The milestone file says "What is in the diff" and lists `kernel/src/arch/x86_64/timer.rs` etc. — if those changes are in a different commit/PR, the milestone's "What is in the diff" is at least ambiguous about which diff. But typically in this repo's workflow, a milestone document describes the whole change, and the docs commit may follow the code commit. I'll flag it lightly as a question only if I have room; actually, the prompt wants real concerns. I'll mention it as a verification question — hmm. Let me weigh: the task says "If you have no concerns, reply NO CONCERNS" — they expect concerns to be genuine. I have several genuine doc concerns already. I'll include the code-absence question only if I think it's real. Given the commit message says "Record the calibration fix", the intent is clear that this is the records commit; flagging "where's the code" would likely be answered "in the commit being recorded". I'll skip it or make it a low-priority question. Actually — the milestone says "Status: BUILT" and this commit deletes the proposal. If the code weren't landed, marking BUILT would be wrong. But we can't verify from this diff. A reviewer of this PR alone would ask. I'll include it as a final question item, phrased as needing confirmation that the code changes landed in a sibling commit, since the milestone asserts BUILT and enumerates code diffs not present here. Hmm... Actually, let me reconsider. The prompt: "Below is the commit as it would appear in a pull request, followed by the full contents of each changed file as it stood BEFORE the change." The commit is the whole PR. The repo "design records and roadmap are kept in-tree as Markdown and are treated as part of the deliverable." The commit is clearly the docs half of a change whose code half exists elsewhere (the commit message says "Record the calibration fix"). Reviewing this PR, flagging "no code" would be like flagging a docs PR for not containing code — usually not a concern if the workflow separates them. But the milestone doc's "What is in the diff" lists code files — meaning the milestone describes a diff that includes code. If this PR is only docs, "the diff" it refers to must be a larger change. This is internally fine as long as the code exists in the tree. I'll leave it out — too speculative.

37. **Check the deleted proposal's open questions vs milestone answers.** Proposal's two questions: (1) whether the boot should say how confident it is; (2) whether the local APIC timer's rate should be derived from the same windows. Milestone Follow-on says both answered: boot prints confidence ("best of N windows, worst M MHz"), APIC derived from same windows. OK.

38. **The milestone's title/number: "526. The x86 TSC calibration takes the smallest of several windows" and the file name "526-the-x86-tsc-calibration-takes-the-smallest-of-several-windows.md".** Consistent. The provisional-number note is there. OK.

39. **Benchmarks.md new section: "The `tscdrift` lane measured the TSC against the CMOS RTC and found the counter ticks at exactly 1000.000 MHz under plain TCG."** OK.

40. **"the fix is `kernel/src/arch/x86_64/timer.rs` and milestone 526 (the x86 TSC calibration takes the smallest of several windows)."** OK.

41. **"**`script/bench --x86` and `script/bench --x86 --real` are different instruments...** Without `--real`, `bench --x86` adds `-icount shift=0,sleep=off`, and under `-icount` a guest nanosecond is a function of the instruction stream rather than of real time: the `tscdrift` lane measured the implied rate moving **37% between two workloads inside one boot**."** Consistent with tsc-under-tcg.md (37% between the two burns). OK.

42. **"**Every section of this file that quotes x86 numbers should say which one it used, and from here on they do.**"** — "from here on they do" — does the 2026-08-24 section (earlier in the file) say which it used? It says "ns/iter computed from the guest's own calibrated TSC" — and the correction note marks it. The sentence says "from here on", i.e., from this section onward. Fine.

43. **"The calibration defect below affects the `--real` instrument only, and within it the `ns` column only. Ticks are raw `rdtsc` counts and are untouched."** OK.

44. **"### What was wrong, and by how much" — "Two hundred boots against a counter known to tick at 1000.000 MHz, on a host deliberately saturated to load 30 on eight cores:" then the table. Then "Every one of the 490 boots measured across three host loads was **high**, never low".** OK.

45. **"### Which published numbers this reaches" — bullets. "- **The 2026-09-15 lazy-TSS section and the icount baselines are not affected.** They quote deterministic ticks under `-icount` and say so; `bench/baseline-*.txt` holds ticks."** Can't verify the 2026-09-15 lazy-TSS section exists (truncated region), plausible. OK.

46. **"- **`Instant`, `uptime` and the shell's `time` on x86** all read it through `counter_frequency_protocol`'s page. Not published figures, and not therefore harmless: every process's sense of elapsed time was off by that boot's error."** — "Not published figures, and not therefore harmless" — awkward double negative meaning "not published, but not harmless". Reads as intended? "Not published figures, and not therefore harmless" — hmm, "not therefore harmless" = "not, therefore, harmless" = harmful despite not being published. OK, grammatical. Fine.

47. **"- **Nothing went red over any of it**, which is why it lasted. `kernel::user::wait_for` takes `now()` plus two seconds, so an inflated rate makes a timeout *longer* in real time, never shorter. The defect could only ever fail safe."** OK, consistent with the note.

48. **"### Before and after, as a controlled pair" — "Two `script/bench --x86 --real` runs of the same tree, differing in one constant (`CALIBRATION_WINDOW_CAP` at 1 and at 16), so the before case is the old estimator exactly: | | Implied stored rate | Error against 1000.000 MHz | ... Before (one window) | 1003.7 MHz | +0.37% | After (min of N) | 1000.1 MHz | +0.01% |"** OK.

49. **"**The `ticks` columns of those two runs differ by up to 4x, and that is not the calibration.** It is host load between the two runs..."** OK.

50. **Now back to the milestone file, check "The bug this lane tripped over on its way, and fixed" section:** "`scripts/qemu-bounded.sh`'s bound did nothing on x86_64... `qemu-runner-x86_64.sh` is the only one of the three runners that does not `exec` QEMU... The runner now runs QEMU in the background, traps TERM and HUP, forwards them, and reaps." — And the tsc-under-tcg.md BUGS section (before) says: "**`scripts/qemu-bounded.sh`'s killer did not fire for this lane**, repeatedly: a bounded run whose output consumer exited early left `qemu-system-x86_64` orphaned to `launchd` past its bound, five times in one afternoon... This note is not where that gets fixed; see the lane's report and `fix/qemu-bounded-stdin-under-dash`, which is a different symptom of the same script."

Now — the commit edits tsc-under-tcg.md's "This is tracked" paragraph to say FIXED, but does NOT update the BUGS entry about qemu-bounded.sh. The milestone says the x86_64 runner is fixed ("The runner now runs QEMU in the background, traps TERM and HUP, forwards them, and reaps") and that the wrapper's own BUGS "is left for whoever next touches that file." But tsc-under-tcg.md's BUGS entry still says "This note is not where that gets fixed; see the lane's report and `fix/qemu-bounded-stdin-under-dash`" — referring to a different branch for a different symptom. The milestone says the orphaning bug (runner not exec'ing) is fixed in this lane. The note's BUGS entry describes the orphaning ("left qemu-system-x86_64 orphaned to launchd past its bound, five times") and says it's not fixed here, pointing at a different branch that fixes "a different symptom of the same script". After this commit, the orphaning IS fixed (per the milestone), but the note's BUGS entry still says "This note is not where that gets fixed" — which is now stale/misleading: the fix landed (in the code commit accompanying this milestone), and the note is exactly where the finding was recorded. Hmm — is this a real inconsistency? The note's BUGS entry says the fix is elsewhere (the lane's report and another branch). The milestone says the runner fix is part of this lane's diff ("scripts/qemu-runner-x86_64.sh: forwards SIGTERM to QEMU... this is a separate bug that this lane's own sweep tripped over several hundred times"). So after the milestone lands, the note's BUGS entry "This note is not where that gets fixed" remains literally true (the note is not where it got fixed — the runner script is), but it omits that it DID get fixed, and points only to a different-symptom branch. A reader of the note would think the orphaning is still open. Given the commit updates the same file's calibration finding to FIXED, leaving the qemu-bounded BUGS entry stale in the same file is an inconsistency worth flagging. Hmm, but the milestone's Follow-on explicitly says: "**Recorded.** `scripts/qemu-bounded.sh`'s `BUGS` section still describes SIGKILL as the way its bound is defeated, and does not mention that a runner which does not `exec` defeats it with an ordinary TERM. The x86_64 runner is fixed here and now says so at the code; the wrapper's own `BUGS` is the place a reader would look first and is left for whoever next touches that file." So they deliberately left qemu-bounded.sh's BUGS stale. But tsc-under-tcg.md's BUGS entry is a different place — it's the note that reported the orphaning, and it now contains a FIXED stamp for the calibration while the BUGS entry for the orphaning still says "not where that gets fixed; see ... fix/qemu-bounded-stdin-under-dash". The milestone says the runner fix is in THIS lane ("The runner now runs QEMU in the background..."), so the note's pointer to only the other branch is incomplete. A careful reviewer might flag: tsc-under-tcg.md's BUGS entry for the qemu-bounded orphaning should now point at milestone 526's runner fix, not just at the stdin-under-dash branch. That's a legit docs-consistency concern. I'll include it.

51. **Also check: the milestone says "This lane's calibration sweep orphaned an emulator on every boot until someone looked" — and the note said "five times in one afternoon". Consistent narrative.**

52. **Check the milestone's claim about rung three of AGENTS.md's ladder** — references we can't verify; fine.

53. **"**That is a general hazard rather than this lane's**: any bounded x86_64 run in this tree, in any lane, leaked its emulator at the bound."** OK.

54. **Potential issue: the milestone's "Index row" says "wrong by up to **+1153%** against a counter known to tick at 1000.000 MHz, high on every one of 490 boots and never low, because a poll can only notice the PIT's terminal count late."** OK.

55. **"Everything time-derived on x86 read it: `bench --x86 --real`'s ns/iter, `Instant`, `uptime`, `coremark`'s self-reported rate."** OK.

56. **Now let me double-check the "+1153%" vs "12x" more carefully for the benchmarks.md heading.** +1153% error means measured = true × (1 + 11.53) = 12.53 × true. "Wrong by up to 12x" — if "wrong by Nx" means "the reported value is N times the truth", then 12.53x, so "12x" understates slightly. If "wrong by Nx" means "the error is N times the truth", then 11.53x, so "12x" overstates. Either way it's approximate. But the bigger deal: the same file, a few hundred lines earlier in the inserted correction, says "wrong by up to **+1153%**", and the milestone and note both say +1153%. The heading introducing "12x" is a third representation. It's a headline-number inconsistency worth a gentle flag. Also, "wrong by up to 12x" vs the correction note's "by anything from a fraction of a per cent to a factor of several" — "a factor of several" for a 12.5x error is a serious understatement. Hmm: "so these figures may read faster than the run really was, by anything from a fraction of a per cent to a factor of several." A factor of 12.5 is "a factor of several"? "Several" usually means 3–7ish. 12.5 is more than "several". Minor wording understatement. Could fold into the same item.

57. **Check tsc-under-tcg.md edit once more for the claim "and it got worse when measured harder" framing — "The numbers in this section are the defect, and they got worse when measured harder."** Fine.

58. **One more check on the milestone: "the reasoning is the one-sidedness used the other way round. A window is inflated by a descheduling, an event of essentially arbitrary size; for two windows to land within a thousandth of each other they must both have been left alone, because two independent deschedulings agreeing to three decimal places is not a thing that happens."** — one part in a thousand = agreement to ~1 MHz at 1000 MHz, i.e., three decimal places in GHz. OK.

59. **"A tighter tolerance (one part in 2000) moved the mean by 0.07 windows and the error distribution not at all, which says the choice is not delicate: clean windows agree to within the PIT's own quantisation and dirty ones are nowhere near."** OK.

60. **Check "The measured boot cost, stated plainly" — "`script/test --arch x86_64` boots the kernel **four** times"** — plausible.

61. **Now, is there an issue with the milestone saying "the suite pays 90 ms more ... 0.04%, an order of magnitude below the suite's own run-to-run variance. That is a computed figure and deliberately not a measured delta, because there is nothing to measure a 90 ms change against."** OK.

62. **Check the milestone's BUGS: "**The local APIC timer's rate gets the same treatment and has no `CPUID` escape.** It is the minimum over the same windows, taken independently of the TSC's. Unlike the TSC there is no path that skips the measurement, so on a part that reports leaf 0x15 the windows are still timed."** Wait — "on a part that reports leaf 0x15 the windows are still timed" — for the APIC timer, yes. But earlier the milestone says init_frequency already prefers CPUID 0x15 for the TSC. If 0x15 exists, TSC windows are skipped but APIC windows still timed. Consistent. OK.

63. **Check that the correction blockquote in benchmarks.md says "See the 2026-09-21 section at the end of this file" — the new section IS at the end of the file (appended at line 3794+). OK.**

64. **Check the new section's heading date and the file's convention: other sections have "## 2026-07-28: ..." style headings. OK.**

65. **One more possible issue: the new benchmarks.md section says "the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'."** — I flagged this as item 9. Let me also double-check whether there's any 2026-09-21 section above the new one in the visible text. Visible 2026-09-21 sections: "The floors name the nightly they were read against (2026-09-21)" and "The emulator is the other half of the same fact (2026-09-21)". Neither mentions CoreMark. The CoreMark quote is in the 19e section. But there could be another 2026-09-21 section in the truncated middle (lines 1200–2664) — e.g., a coremark-related section dated 2026-09-21. The exact quote "reports correctness, not yet a score" appears in the 19e section. If a later section repeated it, the quote would presumably be attributed. I'll flag it as "appears to be a wrong cross-reference — the quoted wording is in the undated milestone-19e section, not in any 2026-09-21 section visible in the file" with appropriate hedging.

Hmm wait, actually, let me reconsider. The file is ~3800 lines; we saw ~1200. Between line 1200 and 2664 there's a lot we can't see. There might be a 2026-09-21 coremark section. But the phrase "reports correctness, not yet a score" with "No such score is published" — if there were a 2026-09-21 section about coremark, it might say that. I need to hedge. I'll phrase it as: the quoted sentence appears verbatim in the milestone-19e section (which is not dated 2026-09-21), so unless another 2026-09-21 section repeats it, the cross-reference is wrong; please check. That's fair review behavior.

Actually, wait. Let me re-read the new section's bullet: "- **`coremark`'s self-reported rate on x86** read the stored number, so any x86 CoreMark score computed from it carried the same error. No such score is published; the 2026-09-21 section above records that CoreMark "reports correctness, not yet a score"."

Hmm — "the 2026-09-21 section above". Also possibly they mean "the section above dated 2026-09-21" — but the new section itself is dated 2026-09-21 and it's at the end. Sections above it include the fastpath-gate section (undated in visible part, ends with "teaches people to route around it"). Could that section be dated 2026-09-21? Its tail mentions "x86_trap", "152 bytes", "gate is green with room (+1.0%, +1.4%, +3.9%)" — sounds like a fastpath footprint exemption section. It doesn't mention coremark in the visible tail. So the reference is likely wrong. Flag with hedge.

66. **Also, "the 2026-09-15 lazy-TSS section" — can't verify; the file's truncated region may contain it. Not flagging.**

67. **Let me reconsider the "unbiased estimator" claim once more, since it's my strongest technical item.** Milestone text: "Every window is an *upper bound* on the true rate, so the minimum of several is the tightest bound taken, and it converges on the truth from above. An average would be a biased estimator for precisely the reason the minimum is an unbiased one: it carries every descheduling into the answer instead of discarding it."

- The minimum is NOT unbiased: E[min] > true rate whenever P(all windows inflated) > 0, and even clean windows are inflated by polling latency (the poll loop's own iteration time; the note measured poll cost 165–1031 TSC counts for the RTC probe; the PIT poll has similar granularity). The data confirms residual bias: min-of-16 worst +0.47%, and at load 22 min-of-9 +0.34%.
- The file's own BUGS section contradicts "unbiased": "The minimum is only as good as the best window the host allowed, so a host that never leaves the vCPU alone for ten unbroken milliseconds produces an inflated rate however many windows are taken."
- The correct statement is the one in the same paragraph and in the deleted proposal: the minimum is a consistent estimator that converges from above (a bound, not an unbiased estimator).

So the word "unbiased" is wrong and internally contradicted. Solid flag.

68. **Another possible item: the commit message says "Nothing can recover the true values: each of those eleven boots stored a different wrong rate and none was written down."** — fine.

69. **Check the milestone's statement "Two hundred boots ... give the error of the min over the first k of them" vs benchmarks.md's identical table. Consistent.**

70. **Check the note's statement "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** — "those 200 boots" = the load-30 200. With the fix (early-stopping min), per the milestone, identical to min-of-16: 0/200, +0.47%. OK.

71. **Possible formatting check in tsc-under-tcg.md diff:** the new paragraph "`arch::x86_64::timer`'s `BUGS` section says all of this where a reader meets the feature, including what it still does not bound." then blank line then "## What this measurement cannot see". Fine.

72. **One more: the milestone file's "What is in the diff" lists `