1. **The roadmap index is not updated (missing from this commit; the changed files are `design/roadmap/555-...md` and `design/roadmap/556-...md`).** Both files carry an `## Index row` section, which is the text of their row in the roadmap's index. Renumbering changes each file's number *and* its path, so the index's existing rows for 525 and 527 now point at files that no longer exist, and slots 555/556 have no rows. Since the design records are treated as part of the deliverable, landing the renumber without the corresponding index update (or a note that the index is regenerated mechanically) ships a stale deliverable with dangling links.

2. **Inbound references to the old numbers are not checked (`design/roadmap/555-a-program-that-asks-the-cpu-dies-without-saying-so.md`, `design/roadmap/556-a-second-risc-v-implementation-for-rent.md`).** Each status paragraph records that the milestone was "Promoted from the proposal `a-program-that-asks-the-cpu-dies-without-saying-so`" / "`a-second-risc-v-implementation-for-rent`". Those proposal files, and any notes or milestones that cite "525"/"527" or link to the old paths, now point at numbers and paths that no longer exist. The commit message shows no evidence of a tree-wide sweep for the old numbers. It matters because the provenance chain these documents explicitly maintain ("promoted from proposal X on date Y") breaks silently the moment the number changes.

3. **Both renamed files: the renumbering sentence is appended without rewrapping the paragraph.** The files are hard-wrapped at roughly 120 columns, but "Renumbered from 525 to 555 on 2026-09-22 …" is tacked onto the existing final line of the status paragraph, pushing it well past the wrap (in 556, to ~185 columns). Minor, but these files are deliverables with a consistent wrap style; the paragraph should be rewrapped rather than extended.