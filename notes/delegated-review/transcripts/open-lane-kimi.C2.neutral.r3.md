[no text block returned; the model spent its budget on thinking]

Let me analyze this PR carefully. The commit is a documentation-only change (design records, notes) to the nife repository. It:

1. Adds a new milestone file `design/roadmap/526-the-x86-tsc-calibration-takes-the-smallest-of-several-windows.md`
2. Deletes the proposal `design/roadmap/proposals/the-x86-boot-calibrates-once-and-can-be-wrong-by-4x.md`
3. Modifies `notes/benchmarks.md` (adds a correction blockquote at the 2026-08-24 section and a new dated section at the end)
4. Modifies `notes/tsc-under-tcg.md` (marks the finding as fixed)

Let me look for issues:

**Issue 1: Number inconsistency — "+1153%" vs "12x" vs "4x"**

The new benchmarks.md section header says: "the x86 boot calibration was wrong by up to 12x". But +1153% means the stored rate was 12.53x the true rate... wait, +1153% of 1000 MHz = 1000 + 11530 = 12530 MHz? Hmm, actually +1153% means value = 1000 * (1 + 11.53) = 12,530 MHz. Hmm, but earlier text says "the worst this lane saw, +1153%, is a 125 ms one" — a 125 ms window reported as 10 ms gives 12.5x the rate, i.e., 12,500 MHz. So "+1153%" = 12.53x. And "12x" is a rounding of 12.53x. That's consistent-ish.

But wait — the milestone 526 file says "4330 MHz is a 43 ms window reported as a 10 ms one, and the worst this lane saw, +1153%, is a 125 ms one." OK.

But the deleted proposal was titled "can be wrong by 4x" and the commit message says "4330 MHz over twenty-two boots became +1153% over 490". The tsc-under-tcg.md before version says "the stored rate ranges from +0.1% to +333%". 4330 MHz is +333% above 1000. OK consistent.

Now check: the milestone says "every boot high, none ever low" across 490 boots. The table says at 1 window, "56 / 200" boots wrong by >1% at load 30. And in benchmarks.md: "at this host's load roughly one boot in four was wrong by more than a per cent" — wait, 56/200 = 28%, which is "roughly one boot in four"? 28% is closer to one in four than one in three, ok. But wait, in benchmarks.md the "before and after" section says "at this host's load roughly one boot in four was wrong by more than a per cent". Hmm, 56/200 = 28%. "Roughly one in four" — that's fine-ish.

But the milestone 526 says for 5 windows: "Five still leaves one boot in eighteen wrong by more than a per cent" — 11/200 = 5.5%, which is one in eighteen. OK consistent.

**Issue 2: The tsc-under-tcg.md says "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** That's consistent with the 16-window row of the table. But the fix is min-of-N with early stopping at agreement within 1 part in 1000, capped at 16. The milestone says the stopping rule "reaches exactly the accuracy of taking the full cap every time, boot for boot". OK.

**Issue 3: benchmarks.md correction blockquote placement.** The blockquote is inserted before "QEMU 11.0.2 (`.qemu-version`, pinned)... Six boots debug, five release". The blockquote says "these eleven boots" — six debug + five release = eleven. OK consistent.

**Issue 4: The new benchmarks.md section header says "wrong by up to 12x"** but everywhere else in the same diff says "+1153%". 12x would be +1100%. +1153% is 12.53x. Saying "up to 12x" is a slight understatement/rounding; arguably fine but could be flagged as inconsistent. Actually "up to 12x" vs "up to +1153%" — 12.53x rounds to 13x if rounding to nearest, or "more than 12x". Saying "up to 12x" is technically wrong since 12.53x > 12x. Minor.

**Issue 5: The milestone number.** The file is named 526 and says "Number provisional until the merge queue lands it; 524 onward is contested between in-flight branches." That's acknowledged in the file itself.

**Issue 6: The milestone file references milestone 524 as "the three x86_64 boot gates: NX, SYSCALL, and the invariant TSC"** — consistent with tsc-under-tcg.md's reference.

**Issue 7: The commit message says "notes/tsc-under-tcg.md's finding is marked fixed, with the correction that measuring harder made the defect worse: 4330 MHz over twenty-two boots became +1153% over 490."** Consistent with the file changes.

**Issue 8: The deleted proposal's removal is recorded in the milestone's Follow-on section as "Done."** OK.

**Issue 9: In the milestone file, "Measured over the same 490 boots"** — but the table above it says "Two hundred boots on a host deliberately saturated to load 30 on eight cores, each timing sixteen windows and reporting every one". The 490 boots figure is "490 boots at three host loads". The stopping-rule measurement "Measured over the same 490 boots" — plausible.

Wait, there's an inconsistency: the defect table says "Two hundred boots ... each timing sixteen windows and reporting every one, give the error of the min over the first k of them". Then later: "At ordinary session load one window's worst of 30 boots was +773%, and min-of-9 got to +0.01%; at load 22 the same estimators gave +318% and +0.34%." So 200 + 30 + ... hmm, 490 total across three loads. 200 at load 30, and the rest split between ordinary and load 22. The tsc-under-tcg.md says "Milestone 526's sweep ran 490 boots at three host loads and saw +1153%, with 56 of 200 boots at load 30 wrong by more than one per cent." OK.

But wait — the milestone says "the worst this lane saw, +1153%, is a 125 ms one" and the table says worst of 200 at 1 window is +1153%. And "At ordinary session load one window's worst of 30 boots was +773%". So the +1153% was at load 30. Consistent.

**Issue 10: benchmarks.md new section says "Every one of the 490 boots measured across three host loads was high, never low"** — consistent with milestone.

**Issue 11: The correction blockquote in benchmarks.md says "the debug-versus-release argument and the 4.2x are ratios within a boot".** Hmm — "the 4.2x" — we don't have the full context of the 2026-08-24 section (truncated at 1200 lines). Can't verify. The debug-versus-release comparison: if debug and release numbers come from *different boots* (six boots debug, five release), then a ratio between a debug figure and a release figure is a ratio *across* boots, not within a boot, and the calibration does NOT cancel. Let me re-read.

The blockquote says: "every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly."

Hmm. If the section measured six boots debug and five release, then the debug-vs-release comparison is across boots. Each boot has a different wrong calibration. A ratio of debug ns/iter to release ns/iter computed from different boots would NOT have the calibration cancel — unless the comparison is done in ticks (raw rdtsc counts), which are unaffected. The blockquote says "The *ticks* they were derived from are unaffected". So if the debug-vs-release argument is made in ticks, fine. But if it's made in ns/iter across different boots, the calibration error does not cancel.

Actually wait — let me think again. The claim is "every conclusion resting on a ratio between two rows of one boot, because the calibration cancels exactly in a ratio, and that is all of them." If the debug-versus-release argument compares a debug boot's number to a release boot's number, those are two rows from *different* boots. The calibration differs per boot ("each of those eleven boots stored a different wrong rate"). So the ratio across boots would be contaminated.

But we can't see the 2026-08-24 section content (truncated). The visible text near the insertion point says "Six boots debug, five release; `ns/iter` computed from the guest's own calibrated TSC". If the debug-vs-release argument compares medians across boots in ns/iter, the calibration does not cancel. Hmm, but the ratio might be computed per... we can't tell. This is a potential concern but uncertain. Actually — hold on. If both debug and release figures are inflated by *different* calibration errors per boot, a cross-boot ratio in ns is off by the ratio of the two calibration errors. The claim "the calibration cancels exactly in a ratio" only holds within one boot. The blockquote explicitly says "ratios within a boot" and claims the debug-versus-release argument is one. If the debug and release numbers are from different boots (six debug boots, five release boots), then the debug-vs-release ratio is *not* within a boot. That looks like a real inconsistency worth flagging — unless the section compared them in ticks. The blockquote hedges: "The *ticks* they were derived from are unaffected, and so is every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot".

Hmm, actually maybe the 2026-08-24 section compared debug vs release by running both in the same boot? "Six boots debug, five release" suggests separate boots per build. But maybe each boot ran both? Can't tell from what's visible. Given uncertainty, I could flag it as a question: the claim that the debug-vs-release ratio is "within a boot" needs the debug and release figures to come from the same boot, but the section describes six debug boots and five release boots, which reads as separate boots; if so the calibration does not cancel in that ratio and the correction notice overstates what is safe.

Actually, let me reconsider. The inserted blockquote is at the 2026-08-24 x86 section ("### The numbers" under something about yield_switch and tss_iomap_switch, per the later reference: "The 2026-08-24 x86 `ns/iter` table (`yield_switch`, `tss_iomap_switch`, debug and release)"). So the section compares yield_switch vs tss_iomap_switch in debug and release. The "4.2x" is presumably the ratio between tss_iomap_switch and yield_switch or debug vs release. If debug and release are different boots, cross-boot ratios in ns are suspect. But the per-boot ratio of tss_iomap_switch to yield_switch within one boot cancels. The "debug-versus-release argument" might be stated as "debug is X% slower" computed per... ugh, can't verify. I'll flag it as a concern with the reasoning, phrased carefully.

Hmm, wait. Actually, let me reconsider: is it possible the ratio "debug-versus-release" was computed as the ratio of ratios? Like (tss/yield in debug) vs (tss/yield in release)? Each within-boot ratio cancels calibration, so comparing those ratios across boots is fine. The blockquote says "the debug-versus-release argument and the 4.2x are ratios within a boot". Plausibly the argument is: the TSS iomap switch costs 4.2x a plain yield in both debug and release — each 4.2x computed within a boot. That's fine. Without the section text I can't be sure. Given the file was truncated at 1200 lines by the review bundle script, the 2026-08-24 section presumably exists somewhere in the truncated portion. I should be careful about asserting. I can note it as a thing to verify, but the instruction says "One concern per item, each naming the file and stating what is wrong and why it matters." Flagging a plausible-but-unverifiable inconsistency is legitimate in review.

Let me look for more concrete issues.

**Issue 12: "12x" in the benchmarks.md heading vs "+1153%" elsewhere.** "wrong by up to 12x" — +1153% is 12.53x, so "up to 12x" understates. Also the deleted proposal said "4x" and the tsc note says "+333%" for 4330 MHz (4.33x). The new worst is 12.53x. "12x" is a rounding down. Minor but a consistency thing. Actually more precisely: +1153% means the stored rate is 12.53× the truth, i.e., "wrong by a factor of 12.5". "Up to 12x" is wrong by a bit. Could flag.

**Issue 13: The benchmarks.md new section title says "and what that does to the numbers above"** — it's appended at the very end of the file. Fine.

**Issue 14: Formatting — missing blank line before the new section?** The diff shows:

```
 teaches people to route around it.
+## 2026-09-21: the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above
```

The `+` line immediately follows the existing last line "teaches people to route around it." with no blank line between. In Markdown, a heading needs a blank line before it to be recognized by many parsers (CommonMark actually allows ATX headings to interrupt paragraphs? No — CommonMark: "An ATX heading can interrupt a paragraph"? Let me recall. In CommonMark, ATX headings *can* interrupt a paragraph (unlike setext). Actually yes, CommonMark spec: ATX headings can interrupt a paragraph. But many renderers (GitHub follows CommonMark, so it would render). Still, stylistically the file consistently uses blank lines; and some linters (markdownlint MD022) require blank lines around headings. The lack of a blank line means in some parsers the heading could be swallowed into the paragraph. GitHub's CommonMark would render it as a heading. Hmm, MD022 is a common lint. This is a legitimate nit: missing blank line before the new `##` heading. Let me double check the diff:

```
 already carries `x86_trap`, is already 152 bytes, and whose gate is green with room (+1.0%, +1.4%,
 +3.9%). The exemption is measured; asserting a property the tree does not hold is how a gate
 teaches people to route around it.
+## 2026-09-21: the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above
```

Yes — no blank line. The rest of the file has blank lines before every heading. This is a real formatting concern (markdownlint MD022, and risks rendering as part of the paragraph in stricter parsers). Worth one item.

**Issue 15: The correction blockquote references "the 2026-09-21 section at the end of this file"** — the new section is indeed at the end. OK.

**Issue 16: The blockquote says "See ... `kernel/src/arch/x86_64/timer.rs`'s `BUGS`."** Fine.

**Issue 17: In benchmarks.md new section: "**The 2026-09-15 lazy-TSS section and the icount baselines are not affected.**" Is there a 2026-09-15 lazy-TSS section? The visible portions mention "The floors name the nightly they were read against (2026-09-21)" and a 2026-09-15 toolchain pin. There's a reference earlier in the visible file: "on 2026-09-15 the pin went to `nightly-2026-09-15`". A "lazy-TSS section" dated 2026-09-15 might exist in the truncated region. Can't verify. Skip.

**Issue 18: The new section says "`coremark`'s self-reported rate on x86 read the stored number... No such score is published; the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'."** Wait — the "reports correctness, not yet a score" line is in the CoreMark workload section dated... it's in the milestone 19e section ("This binary reports correctness, not yet a score"). The new text says "the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'". Hmm, is the CoreMark section dated 2026-09-21? The visible text: "### The CoreMark workload (`crates/coremark`, `fixtures/src/coremark.rs`)" under "## Compute vs. OS primitives: two benchmarks that measure different things (milestone 19e)". Not dated 2026-09-21. There are two sections dated 2026-09-21 visible: "The floors name the nightly they were read against (2026-09-21)" and "The emulator is the other half of the same fact (2026-09-21)". Neither mentions CoreMark. So the cross-reference "the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'" appears to be a wrong pointer — the CoreMark section is the milestone-19e section, not a 2026-09-21 section. Unless there's another 2026-09-21 section in the truncated 1200 lines that mentions CoreMark reporting a score. Possible but the quoted phrase exactly matches the 19e section's "This binary reports correctness, not yet a score". So the date attribution looks wrong. This is a decent catch: misattributed cross-reference. Let me double-check the exact new text:

"- **`coremark`'s self-reported rate on x86** read the stored number, so any x86 CoreMark score computed from it carried the same error. No such score is published; the 2026-09-21 section above records that CoreMark "reports correctness, not yet a score"."

The phrase "reports correctness, not yet a score" appears in the milestone-19e CoreMark section: "This binary reports correctness, not yet a score; timing a run needs a userspace clock..." That section is headed "## Compute vs. OS primitives: two benchmarks that measure different things (milestone 19e)" — not a 2026-09-21 section. So the pointer is wrong. Also, wait — also in the 19e section it says timing "lands with the cross-OS suite rather than here", i.e., coremark does NOT self-report a rate yet? But tsc-under-tcg.md says "coremark's self-reported rate" reads the stored number. Hmm, the EL0 suite section says "The CoreMark workload is the first program to use it, self-timing its run and reporting `[crc, ticks, freq]`". So coremark reports ticks and freq. On x86 it would use the TSC-derived frequency. OK.

So concern: benchmarks.md new section points to "the 2026-09-21 section above" for the CoreMark correctness-not-score fact, but that fact lives in the milestone-19e section; the 2026-09-21 sections are about toolchain floors and the emulator stamp. This misdirects a reader. Good catch.

**Issue 19: The milestone file says the mean windows on saturated host is 5.20 and "calef's five is what this costs". Also "That the measured mean on a saturated host came out at 5.20 is a coincidence and is recorded as one."** Fine.

**Issue 20: The milestone's BUGS says "0 of 200 boots at load 30 were wrong by more than 1%"** — consistent with table's 16-window row. But note the BUGS statement conflates: the estimator shipped is min-with-early-stop, and the milestone claims it reaches "exactly the accuracy of taking the full cap every time, boot for boot". OK.

**Issue 21: The milestone file says "Ten milliseconds per window ... the suite boots repeatedly" and later "`script/test --arch x86_64` boots the kernel **four** times, and those four boots took 3, 3, 4 and 3 windows. So the suite pays **90 ms** more than the one-window design did".** Let me check: 3+3+4+3 = 13 windows. Old design: 4 windows (one per boot). Extra = 9 windows = 90 ms. OK correct.

"against a 3m38s runtime: **0.04%**" — 90 ms / 218 s = 0.0413%. OK.

**Issue 22: The milestone says "Two hundred boots ... give the error of the min over the first k of them" with row "1 (what shipped) ... Worst of 200: +1153%".** And earlier: "This lane widened that to 490 boots at three host loads and the shape held without a single exception: every boot high, none ever low." Then the table is from 200 of those. Fine.

**Issue 23: tsc-under-tcg.md edit: "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** Consistent with the 16-window row. But the fix isn't "16 windows" — it's min-of-N with early stop at agreement, cap 16. The milestone claims the early-stop reaches exactly the full-cap accuracy boot for boot over the 490 boots. So OK.

**Issue 24: tsc-under-tcg.md still says in "Reproducing it": "the calibration fix proposed above is what it exists to be measured against."** The proposal is now a built milestone; "proposed above" is stale wording — the fix is no longer proposed, it's built. Minor staleness. Also the BUGS section of tsc-under-tcg.md still says "**`scripts/qemu-bounded.sh`'s killer did not fire for this lane**... This note is not where that gets fixed; see the lane's report and `fix/qemu-bounded-stdin-under-dash`". But the milestone 526 says the x86_64 runner IS fixed in this lane ("The runner now runs QEMU in the background, traps TERM and HUP, forwards them, and reaps"). Hmm — the milestone says `scripts/qemu-runner-x86_64.sh` is fixed, and the tsc note's BUGS refers to `scripts/qemu-bounded.sh`'s killer not firing, which the milestone says is the same root cause (runner not exec'ing QEMU). The milestone's Follow-on says: "`scripts/qemu-bounded.sh`'s `BUGS` section still describes SIGKILL as the way its bound is defeated... The x86_64 runner is fixed here and now says so at the code; the wrapper's own `BUGS` is the place a reader would look first and is left for whoever next touches that file."

So the tsc-under-tcg.md BUGS entry says the killer "did not fire for this lane, repeatedly" and "This note is not where that gets fixed; see the lane's report and `fix/qemu-bounded-stdin-under-dash`". After this commit, the underlying cause (runner not forwarding TERM) IS fixed — by this very lane/commit (the milestone claims the runner fix is part of this milestone's diff). The tsc note's BUGS entry is now stale: it still describes the bug as unfixed and points to a branch for a "different symptom". The commit updated the "tracked" paragraph to FIXED but left the BUGS entry describing the qemu-bounded killer as an open problem. Given the milestone says the runner fix landed in this milestone, the note's BUGS entry should have been updated too. This is a real inconsistency: tsc-under-tcg.md's BUGS still presents the qemu-bounded killer failure as unfixed ("This note is not where that gets fixed"), while milestone 526 (same commit) records that this lane fixed the runner side of it. Worth flagging.

Hmm, but wait: is the runner fix part of *this* commit? The commit only touches markdown files. The milestone describes code changes (timer.rs, main.rs, tsc_probe.rs, qemu-runner-x86_64.sh, timer_calibration_tests.rs) that presumably landed in a *separate* commit (the milestone's implementation commit), and this commit is the documentation follow-up ("Record the calibration fix: milestone 526, the note, and the suspect numbers"). The milestone file itself is being added in this docs commit with status BUILT. So the code fix exists elsewhere in the tree by the time this lands (or lands together in the PR). Either way, the tsc note's BUGS entry about qemu-bounded remains describing an unfixed bug while the milestone in the same commit says the runner is fixed. The milestone even acknowledges the wrapper's BUGS is left stale deliberately ("left for whoever next touches that file") — but that's about qemu-bounded.sh's own BUGS, not tsc-under-tcg.md's BUGS. The tsc note's BUGS entry says "This note is not where that gets fixed; see the lane's report and `fix/qemu-bounded-stdin-under-dash`, which is a different symptom of the same script." Now the milestone says the killer-didn't-fire root cause is the runner not exec'ing, fixed by trapping/forwarding. So the tsc note's BUGS entry is outdated. It matters because the tree treats these notes as deliverables and the milestone claims the general hazard is fixed for x86_64; a reader of tsc-under-tcg.md would think it's still open. I'd flag it as a minor staleness concern.

**Issue 25: The milestone file references "AGENTS.md's ladder" rungs.** Can't verify; skip.

**Issue 26: "patagonia" and "xenon" host names** — consistent with tsc note.

**Issue 27: Milestone says "under any invocation tried, which is why every boot in this tree takes the calibrated path"** re CPUID 0x15. Consistent with tsc note's statement that TCG refuses to advertise invariant TSC. Different leaves but fine.

**Issue 28: The new benchmarks.md section says "Two `script/bench --x86 --real` runs of the same tree, differing in one constant (`CALIBRATION_WINDOW_CAP` at 1 and at 16), so the before case is the old estimator exactly".** Hmm — is cap=1 exactly the old estimator? With cap 1, you take one window and min over one window = that window. Yes, equivalent. OK.

**Issue 29: "**That before figure is one draw and a lucky one**, ... at this host's load roughly one boot in four was wrong by more than a per cent"** — 56/200 = 28% ≈ one in four. OK.

**Issue 30: Check the correction blockquote's claim "each of those eleven boots stored a different wrong rate and none was written down" (commit message) / "the rate each of these eleven boots stored was not written down, and it was a different number each time" (blockquote).** "It was a different number each time" — is that known? The rates weren't written down, so claiming they were all different is unknowable; but the 22-boot sweep showed each boot differing, so it's a reasonable inference. The commit message says "each of those eleven boots stored a different wrong rate and none was written down". Slight overclaim but hedged by the sweep evidence. Probably not worth flagging.

**Issue 31: benchmarks.md correction says "by anything from a fraction of a per cent to a factor of several".** The worst seen was +1153% (12.5x), which would make ns/iter read 12.5x too small — "a factor of several" understates "a factor of twelve". Hmm: "so these figures may read faster than the run really was, by anything from a fraction of a per cent to a factor of several." A factor of 12.5 is more than "several" (several ≈ 3-7?). This understates the known worst case in the very notice meant to warn readers. But the notice also says "wrong by up to +1153%" earlier in the same blockquote, so the reader has the number. Still, "a factor of several" is loose. Maybe fold into the "12x" item? These are both understatement nits. Could flag separately or together. Let me keep them as one item: the benchmarks.md heading says "up to 12x" while the body says +1153% (12.5x); and the in-place notice says "a factor of several". Actually, separate locations: heading of the new section vs the blockquote. Both in benchmarks.md. I might combine: "the new section's heading rounds +1153％ down to '12x'". And separately the blockquote's "factor of several". Hmm, one concern per item; I can have two items both about benchmarks.md.

Actually, let me reconsider the severity. The heading "wrong by up to 12x" — +1153% is indeed "more than 12x off" (12.53x). "Up to 12x" is just wrong by rounding; should be "up to 13x" or "more than 12x" or "+1153%". Given this repo's evident care about precise numbers, this is the kind of thing they'd want flagged. Yes.

**Issue 32: The milestone's index row says "wrong by up to **+1153%** against a counter known to tick at 1000.000 MHz, high on every one of 490 boots and never low"** — consistent.

**Issue 33: Milestone says "the mean cost is 3.5 windows on a quiet host and 5.2 on a saturated one against a cap of sixteen"** — table says 3.53 and 5.20. OK.

**Issue 34: tsc-under-tcg.md updated paragraph says "the mean cost is 3.5 windows on a quiet host"** — consistent.

**Issue 35: The milestone says the stopping rule tolerance is "one part in a thousand" and later "A tighter tolerance (one part in 2000) moved the mean by 0.07 windows".** OK.

**Issue 36: The milestone claims "for two windows to land within a thousandth of each other they must both have been left alone, because two independent deschedulings agreeing to three decimal places is not a thing that happens."** Rhetorical; fine for a design doc.

**Issue 37: The milestone says the old design's table row "1 (what shipped)" with median +0.36%.** And tsc note originally said typical boots 1001-1005 MHz (+0.1% to +0.5%). Consistent.

**Issue 38: benchmarks.md new section: "The `tscdrift` lane measured the TSC against the CMOS RTC and found the counter ticks at exactly 1000.000 MHz under plain TCG."** The tsc note says "Exactly 1,000,000,000 Hz, measured to within 42 ppm". "Exactly 1000.000 MHz" is the note's own shorthand. OK.

**Issue 39: The new section says "This lane took that further, fixed it, and this section is what the fix means for anything already published. `notes/tsc-under-tcg.md` owns the measurement; the fix is `kernel/src/arch/x86_64/timer.rs` and milestone 526".** Fine.

**Issue 40: The correction blockquote says "See the 2026-09-21 section at the end of this file"** — but there are already multiple sections dated 2026-09-21 in this file ("The floors name the nightly..." and "The emulator is the other half..."). The new section is also dated 2026-09-21. "The 2026-09-21 section at the end of this file" is ambiguous among three same-dated sections, though "at the end of this file" disambiguates. OK, fine.

**Issue 41: The milestone file's header: "*(Number provisional until the merge queue lands it; 524 onward is contested between in-flight branches.)*"** — acknowledged in-file. Fine.

**Issue 42: Deleted proposal — the milestone's Follow-on says the proposal "is removed by this milestone: its two open questions are both answered above (the boot does print its confidence, and the local APIC timer is derived from the same windows)."** Wait — the milestone's BUGS says "The local APIC timer's rate gets the same treatment and has no `CPUID` escape. It is the minimum over the same windows, taken independently of the TSC's." Hmm — "the minimum over the same windows, taken independently" — the proposal's question was "Whether the local APIC timer's rate should be derived from the same windows." The answer: it gets min-over-windows treatment. OK consistent.

**Issue 43: The milestone says "script/bench --x86 pins the virtual clock and --real does not, and only the latter reads the calibrated rate."** Wait — is that right? Under -icount, does the TSC calibration still happen at boot and get used for ns/iter? The tsc note says under -icount the TSC's rate against real time moved 37% between workloads, and bench --x86 (icount) numbers are "a deterministic function of the instruction stream". The bench computes ns/iter from the guest's calibrated TSC... hmm. Under icount, ticks are rdtsc counts (virtual nanoseconds under icount), and ns/iter = ticks / iterations presumably, or ticks converted via stored frequency? Let me think.

The tsc note says: "`bench --x86 --real`'s ns/iter, `Instant` and `uptime` ... all read that number" (the stored calibrated rate). And the icount bench's ns column "is not wall time and never was". Under icount, the TSC ticks at 1 virtual ns per instruction; the calibrated rate under icount would be measured against the PIT which under icount is also virtual... The new benchmarks.md section says "The calibration defect below affects the `--real` instrument only, and within it the `ns` column only. Ticks are raw `rdtsc` counts and are untouched."

The commit message says: "script/bench --x86 pins the virtual clock and --real does not, and only the latter reads the calibrated rate." Hmm, "only the latter reads the calibrated rate" — is that exactly true? Under icount, the calibration still runs at boot (init_frequency still executes), and ns/iter might still be computed as ticks / stored_rate... Actually if under icount the PIT is also driven by virtual time, the calibration would be self-consistent (both clocks virtual), so ns/iter = ticks/rate would be deterministic regardless. But does the icount bench's ns column use the calibrated rate? The commit message claim "only the latter reads the calibrated rate" is a stronger claim than the benchmarks.md section's claim ("The calibration defect below affects the --real instrument only"). If the icount leg does read the calibrated rate but the rate is itself deterministic under icount (PIT virtual too), then the defect doesn't affect it but it does "read" it. The commit message might be slightly wrong, but commit messages aren't files. The benchmarks.md text is more careful. I won't flag the commit message.

Actually wait, the benchmarks.md new section says: "**`script/bench --x86` and `script/bench --x86 --real` are different instruments...** Without `--real`, `bench --x86` adds `-icount shift=0,sleep=off`, and under `-icount` a guest nanosecond is a function of the instruction stream rather than of real time". Fine.

**Issue 44: "Every section of this file that quotes x86 numbers should say which one it used, and from here on they do."** But the in-place correction at the 2026-08-24 table — does that section say which instrument it used? The blockquote says "These were computed from the guest's calibrated TSC" — implies --real. OK. But are there other x86-quoting sections earlier in the file that don't say? The visible file mentions "script/bench --x86" tripwire etc. The 2026-08-15 RISC-V sections are riscv. The claim "from here on they do" is forward-looking. Fine.

**Issue 45: The milestone file says "`kernel/src/main.rs`: the boot line prints `best of N windows, worst M MHz`."** Fine.

**Issue 46: The milestone "What is in the diff" lists `notes/benchmarks.md` and code files, but does NOT list `notes/tsc-under-tcg.md`, which this commit also changes.** The milestone describes the whole milestone's diff (code + docs), and this commit is the docs part. The milestone's diff list includes "notes/benchmarks.md: the 2026-08-24 x86 ns/iter table is marked in place, and a new dated section records what the defect reaches and what it does not." but omits notes/tsc-under-tcg.md's update (marking the finding fixed). Minor omission in the "What is in the diff" inventory. Also omits the deletion of the proposal file itself, though the Follow-on section mentions the proposal "is removed by this milestone". Hmm, the Follow-on covers the proposal removal. The tsc-under-tcg.md change is genuinely unlisted. Worth a minor item? Possibly. The milestone is meant to record what the milestone changed; leaving out one of the changed files makes the record incomplete. I'd flag it as a minor concern.

**Issue 47: Bigger question — the commit message says "notes/benchmarks.md marks the affected table where a reader meets it rather than quietly restating it."** OK.

**Issue 48: The new benchmarks.md section says "Two hundred boots against a counter known to tick at 1000.000 MHz, on a host deliberately saturated to load 30 on eight cores"** — the milestone says the same. OK.

**Issue 49: Check "49" — the milestone says "Twenty-two boots of one binary stored 1001 MHz to 4330 MHz. This lane widened that to 490 boots at three host loads".** And "Measured over the same 490 boots, this reaches exactly the accuracy of taking the full cap every time, boot for boot, at a mean of: Ordinary session load 3.53..." Wait — but the error table was built from "Two hundred boots ... each timing sixteen windows and reporting every one". Were all 490 boots timing sixteen windows and reporting every one? If only 200 did, then "Measured over the same 490 boots" for the stopping rule implies all 490 reported every window. The ordinary-load row says "one window's worst of 30 boots was +773%" — 30 boots at ordinary load. 200 + 30 = 230; load 22 count unknown; total 490. Hmm, 490 - 200 - 30 = 260 at load 22? The text says "at load 22 the same estimators gave +318% and +0.34%" without a count. It's all plausible. Not a flaggable inconsistency.

Hmm wait, actually there's something: the milestone says "Twenty-two boots of one binary stored 1001 MHz to 4330 MHz. This lane widened that to 490 boots at three host loads and the shape held without a single exception: **every boot high, none ever low.**" Then: "One descheduling of the QEMU thread inside that window inflates the answer without bound: a 4330 MHz reading is a 43 ms window reported as a 10 ms one, and the worst this lane saw, +1153%, is a 125 ms one." OK.

**Issue 50: benchmarks.md blockquote: "on 2026-09-21 the x86 boot calibration was found to be wrong by up to **+1153%**, always high, differently on every boot."** OK.

**Issue 51: Now the big structural question — the commit deletes the proposal file and adds milestone 526, but does the roadmap index need updating?** The milestone file contains an "Index row" section, which suggests the index is generated or manually maintained elsewhere (design/roadmap/index?). We only see changed files; if there's an index file that lists milestones and proposals, it would need regeneration, but it may be auto-generated or updated in the code commit. Can't verify from the bundle. Also the proposal's deletion — was the proposal referenced anywhere else? tsc-under-tcg.md previously referenced it; the commit updates that reference. Good. Any other references to the proposal path? The benchmarks.md before-version doesn't reference it (visible parts). OK.

**Issue 52: tsc-under-tcg.md: the edited paragraph "**FIXED, 2026-09-21, the same day**, by milestone 526 (the x86 TSC calibration takes the smallest of several windows), which is what the proposal this paragraph used to point at became."** OK.

**Issue 53: tsc-under-tcg.md edited text: "Milestone 526's sweep ran 490 boots at three host loads and saw **+1153%**, with 56 of 200 boots at load 30 wrong by more than one per cent."** Consistent.

**Issue 54: Now, a subtle one: the milestone's claim "An average would be a biased estimator for precisely the reason the minimum is an unbiased one".** The minimum is NOT an unbiased estimator — it's biased high (it converges from above, always ≥ truth). The text itself says "the minimum of several is the tightest bound taken, and it converges on the truth from above" — i.e., it's a one-sided/biased estimator that converges. Calling the minimum "an unbiased one" contradicts the document's own correct explanation two sentences earlier ("Every window is an *upper bound* on the true rate, so the minimum of several is the tightest bound taken"). An estimator that is always ≥ the true value and only converges in the limit is biased (conservatively biased). The proposal was more careful: "the minimum of several windows converges on the truth from above. That is not an argument from taste; it is why averaging would be the wrong estimator here and the minimum is the right one." The milestone's phrasing "the minimum is an unbiased one" is technically wrong and self-contradictory with "converges on the truth from above". The table even shows min-of-16 median +0.00% but worst +0.47% — always ≥ 0, i.e., still one-sided, i.e., biased (small bias). This is a genuine technical wording error in a design record. Worth flagging: file design/roadmap/526..., the sentence "An average would be a biased estimator for precisely the reason the minimum is an unbiased one" mischaracterizes the minimum as unbiased; the document itself correctly describes it as an upper bound converging from above, which is a (deliberately, safely) biased estimator. It matters because the document's core argument is the one-sidedness; calling the min "unbiased" undercuts the precision the record relies on.

**Issue 55: The milestone says "0 of 200 boots at load 30 were wrong by more than 1%; that is a measured distribution and not a guarantee."** OK.

**Issue 56: In the milestone: "The `tscdrift` lane then went to check whether TCG's TSC drifts at all, built an RTC-referenced probe, and found that it does not: the counter ticks at exactly 1000.000 MHz, constant to within 42 ppm whatever the host is doing."** OK.

**Issue 57: I should double check the "4.2x" claim in the blockquote once more. The blockquote: "The *ticks* they were derived from are unaffected, and so is every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly."**

Also the commit message: "What survives untouched is every conclusion resting on a ratio between two rows of one boot, because the calibration cancels exactly in a ratio, and that is all of them."

Now, "six boots debug, five release" — the debug-versus-release argument compares across boots. UNLESS the section's argument is: in each boot, both... no, a boot is either a debug build or a release build. Six debug boots, five release boots. Any debug-vs-release ratio in ns/iter is cross-boot and contaminated by differing calibration errors. BUT the ratio might be computed in ticks (unaffected). The blockquote says ticks are unaffected and ratios within a boot cancel. It then asserts the debug-versus-release argument is "ratios within a boot". If the debug figure is the median of six debug boots and release the median of five release boots, the ratio of medians is not a within-boot ratio. Unless the section compared (debug ticks vs release ticks), which are calibration-free.

Hmm wait — actually maybe the debug-vs-release argument in that section is about the *ratio* tss_iomap_switch/yield_switch being similar in debug and release (4.2x in both). Each such ratio is within-boot. The "debug-versus-release argument" might be "the 4.2x holds in both debug and release", each instance within-boot. In that case the claim is fine. Without the section text (truncated), I can't be certain. I'll flag it as a verify-this concern but phrase it as the strongest plausible reading: if the debug-vs-release comparison quotients ns/iter figures from different boots, the calibration does not cancel, and the notice's "all of them" is wrong. Given the repo's standard of precision, asking them to double-check is fair. But I want to be careful not to overstate. Let me look at what's visible right at the insertion point:

"per iteration (both threads resume once each), so the delta divided by two is th[e]..." then "### The numbers" then the blockquote, then "QEMU 11.0.2 (`.qemu-version`, pinned), `-machine q35 -cpu max`, one hart, plain TCG. Six boots debug, five release; `ns/iter` computed from the guest's own calibrated TSC, the same arithmetic `xtask`'s `run_bench` already does for every other leg."

So the section is about `yield_switch` vs `tss_iomap_switch` (per the later section's reference). "Six boots debug, five release" — each boot is one build. So the debug-vs-release comparison is necessarily cross-boot. The "4.2x" is probably tss_iomap_switch / yield_switch. If 4.2x is computed per boot (within a boot), it cancels. If the debug-vs-release argument is "debug is N times slower than release" in ns, it's cross-boot and does NOT cancel.

The blockquote asserts both are "ratios within a boot". For the debug-vs-release argument to be a within-boot ratio is impossible if debug and release run in different boots — unless the argument is constructed from per-boot ratios. This is exactly the kind of thing to flag: the notice asserts the debug-versus-release argument survives because it is a within-boot ratio, but the section's own setup (six debug boots, five release boots) makes any direct debug:release ns/iter ratio a cross-boot quantity, where the calibration does not cancel (each boot stored a different wrong rate). Either the notice overstates what's safe, or it needs to say the argument is made in ticks. I'll flag it.

Actually hold on, let me reconsider. Maybe the debug-vs-release argument compares the *ticks*: "the debug-versus-release argument" could be made on ticks (e.g., "debug costs 2.1x the ticks of release"). Ticks are unaffected by calibration. The blockquote says "The *ticks* they were derived from are unaffected, and so is every conclusion here that rests on a ratio between two rows of the same run". The sentence structure: ticks unaffected; within-boot ratios unaffected; "and that is all of them". Then: "the debug-versus-release argument and the 4.2x are ratios within a boot". Hmm, it explicitly says the debug-versus-release argument is a ratio within a boot. If in reality it's cross-boot, the sentence is wrong. I genuinely can't verify without the section. I'll flag it as a concern with reasoning, noting the section describes separate boots per build.

**Issue 58: One more check on the milestone: "Measured over the same 490 boots, this reaches **exactly** the accuracy of taking the full cap every time, boot for boot".** Then table: mean windows 3.53 etc. Fine.

**Issue 59: The milestone's "What is in the diff" says "`kernel/src/arch/x86_64/timer_calibration_tests.rs`, new: five tests of the stopping rule fed sequences by hand, and two boot assertions".** Fine.

**Issue 60: qemu-runner fix description: "It also needed `qemu-bounded.sh`'s own fd-3 dance, because backgrounding a command gives it `/dev/null` on stdin under dash and would have broken every piped-input run in CI."** And tsc note's BUGS mentions "`fix/qemu-bounded-stdin-under-dash`, which is a different symptom of the same script." OK consistent-ish.

**Issue 61: The commit title: "Record the calibration fix: milestone 526, the note, and the suspect numbers".** Fine.

**Issue 62: Now — the tsc-under-tcg.md "Reproducing it" section says "the calibration fix proposed above is what it exists to be measured against."** Since the fix is now built (milestone 526, same commit), "proposed above" is stale — the paragraph that proposed it now says FIXED. Also "**This note is its owner**, and the calibration fix proposed above is what it exists to be measured against." This sentence now reads oddly: the fix is done. Minor staleness in the same file the commit already touches; the commit updated the tracking paragraph but missed this sentence. Worth flagging as part of the stale-references item? I could combine: tsc-under-tcg.md still contains two stale bits: (a) "the calibration fix proposed above" in Reproducing it, (b) the BUGS entry presenting the qemu-bounded killer failure as unfixed while milestone 526 records the runner fix. Hmm, one concern per item. Maybe two items.

Let me also double check the tsc note's BUGS entry vs milestone. tsc note BUGS: "**`scripts/qemu-bounded.sh`'s killer did not fire for this lane**, repeatedly: a bounded run whose output consumer exited early left `qemu-system-x86_64` orphaned to `launchd` past its bound, five times in one afternoon... This note is not where that gets fixed; see the lane's report and `fix/qemu-bounded-stdin-under-dash`, which is a different symptom of the same script."

Milestone 526: "**`scripts/qemu-bounded.sh`'s bound did nothing on x86_64**... This lane's calibration sweep orphaned an emulator on every boot until someone looked... The runner now runs QEMU in the background, traps TERM and HUP, forwards them, and reaps."

So the same failure mode (orphaned qemu under bounded runs) is described in the tsc note as unfixed ("not where that gets fixed") and in the milestone as fixed by this lane. Hmm — but the tsc note's symptom is "a bounded run whose output consumer exited early" — slightly different trigger (consumer exit vs bound expiry?). "left qemu-system-x86_64 orphaned to launchd past its bound" — the killer didn't fire. The milestone's mechanism: the wrapper sends SIGTERM to the child it started (the runner shell), shell dies, QEMU reparented and runs forever. Same root cause. The milestone fixes the runner to forward TERM/HUP. Does that fix the tsc note's symptom? The tsc note's symptom: "a bounded run whose output consumer exited early" — if the output consumer (e.g., head) exits, the wrapper... hmm, that might be a different path (SIGPIPE?). The note says the branch `fix/qemu-bounded-stdin-under-dash` is "a different symptom of the same script". So the tsc note's BUGS entry might describe a symptom that the runner fix does address (killer TERM not reaching QEMU). "past its bound" strongly suggests the bound's TERM didn't kill QEMU — exactly the milestone's fixed bug. So after this commit, the tsc note's BUGS entry is stale for the killer part. The milestone even says "**That is a general hazard rather than this lane's**: any bounded x86_64 run in this tree, in any lane, leaked its emulator at the bound." — fixed now for x86_64. So yes, flag: notes/tsc-under-tcg.md's BUGS still presents the qemu-bounded killer failure as an open, unfixed item ("This note is not where that gets fixed"), but the milestone added in the same commit records that this lane fixed the root cause in the x86_64 runner; the note should say so where a reader meets it (which is the tree's own stated standard — "stated where a reader meets the feature").

**Issue 63: Let me look at the benchmarks.md blockquote again: "Nothing can recover the true values: the rate each of these eleven boots stored was not written down, and it was a different number each time."** "It was a different number each time" — unknowable for those specific eleven boots (not written down). Slight logical overreach; but the sweep evidence supports "different every boot" as a characterization. Minor; skip or fold.

**Issue 64: Check the milestone's statement about suite boots: "`script/test --arch x86_64` boots the kernel **four** times".** Can't verify; fine.

**Issue 65: The new benchmarks.md section's table duplicates the milestone's table exactly.** Fine.

**Issue 66: "Before and after, as a controlled pair" — "differing in one constant (`CALIBRATION_WINDOW_CAP` at 1 and at 16)".** Hmm, "so the before case is the old estimator exactly" — with cap 1 and the early-stop rule, you take 1 window, no agreement check possible, min of 1. Yes equivalent. OK.

**Issue 67: "**The `ticks` columns of those two runs differ by up to 4x, and that is not the calibration.** It is host load between the two runs"** — OK.

**Issue 68: Now the "12x" heading once more: "## 2026-09-21: the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above".** +1153% → 12.53×. Also the commit message subject says nothing about 12x. The tsc note says "+1153%". The milestone says "+1153%". The heading's "12x" is the only place that quantizes to 12x, and it rounds the wrong way (12.53x is more than 12x; "up to 12x" < actual). Flag.

**Issue 69: Milestone file name vs content: "526-the-x86-tsc-calibration-takes-the-smallest-of-several-windows.md" — consistent with title.**

**Issue 70: The milestone says "**x86_64 only by nature, and not a parity gap.** Under §19 ... aarch64 reads `CNTFRQ_EL0` and riscv64 reads the device tree; both are architected statements of the rate, so neither has a calibration that could be wrong."** The tsc note says the same. OK.

**Issue 71: The milestone references "milestone 161 (the x86_64 kernel port: bring up the HAL's third architecture)" and "milestone 87 (the x86_64 bare-metal machine)" in the tsc note.** Can't verify numbers; skip.

**Issue 72: The milestone says "`arch::x86_64::isa::tsc_crystal_hz` already reads it and `init_frequency` already prefers it. That path is milestone 161's work and this lane changed nothing about it. It is empirically unavailable here: QEMU's TCG does not offer the leaf under any invocation tried, which is why every boot in this tree takes the calibrated path."** Fine.

**Issue 73: The new benchmarks.md section says "`notes/tsc-under-tcg.md` owns the measurement"** — and tsc note says "This note is its owner" (of the probe). Consistent.

**Issue 74: tsc-under-tcg.md: the updated paragraph says "The boot now times several windows and keeps the smallest, because the error being one-sided is what makes the minimum the right estimator; it stops as soon as two windows agree to one part in a thousand, so the mean cost is 3.5 windows on a quiet host; and the boot line prints the worst window beside the chosen one".** Consistent with milestone.

**Issue 75: Now, does the commit message's "the note" mapping hold: "notes/tsc-under-tcg.md's finding is marked fixed, with the correction that measuring harder made the defect worse: 4330 MHz over twenty-two boots became +1153% over 490."** OK.

**Issue 76: Potential issue — the milestone file's status line: "**Status: BUILT** 2026-09-21." with a parenthetical "(Number provisional until the merge queue lands it; 524 onward is contested between in-flight branches.)"** That's self-acknowledged. Fine.

**Issue 77: Does anything still link to the deleted proposal?** tsc-under-tcg.md updated. The milestone Follow-on mentions the proposal path in prose (fine, it says removed). benchmarks.md doesn't reference it. The commit message doesn't. What about `arch::x86_64::timer`'s BUGS section — the tsc note before said "`arch::x86_64::timer`'s `BUGS` section now says it where a reader meets the feature" — code file, not in this commit. The milestone says timer.rs's BUGS was rewritten in the code commit. Fine.

**Issue 78: The proposal file's title said "can be wrong by 4x" and its content said worst 4330 MHz (+333%, i.e., 4.33x).** The new worst is 12.5x. The deleted file's title became outdated; deletion handles it. Fine.

**Issue 79: The benchmarks.md blockquote placement: it's inserted between "### The numbers" and the paragraph "QEMU 11.0.2 ... Six boots debug, five release".** A blockquote right after the heading, before the setup paragraph. Reasonable placement ("where a reader meets it"). OK.

**Issue 80: Hmm, one more numerical check: the milestone says "Five still leaves one boot in eighteen wrong by more than a per cent and a worst case of +51%."** 11/200 = 5.5% = 1/18.18. OK. Table says 5 windows: 99th +20.6%, worst +51.4%, 11/200. Consistent.

**Issue 81: "The tail only closes at sixteen."** Table: 16 windows → 0/200 >1%, worst +0.47%. OK.

**Issue 82: "A tighter tolerance (one part in 2000) moved the mean by 0.07 windows and the error distribution not at all"** — fine.

**Issue 83: The milestone's BUGS: "**The local APIC timer's rate gets the same treatment and has no `CPUID` escape.** It is the minimum over the same windows, taken independently of the TSC's. Unlike the TSC there is no path that skips the measurement, so on a part that reports leaf 0x15 the windows are still timed."** Wait — "the minimum over the same windows" but "on a part that reports leaf 0x15 the windows are still timed" — if CPUID 0x15 gives the TSC rate and init_frequency prefers it, are the PIT windows still timed for the APIC? That's what it says. OK, internally consistent.

**Issue 84: Let me reconsider the "unbiased" issue once more to phrase precisely.** Milestone text: "Every window is an *upper bound* on the true rate, so the minimum of several is the tightest bound taken, and it converges on the truth from above. An average would be a biased estimator for precisely the reason the minimum is an unbiased one: it carries every descheduling into the answer instead of discarding it."

The minimum is biased (E[min] > true rate; it's a bound, converging from above). The sentence claims the minimum is unbiased. This contradicts the preceding sentence and basic statistics. The right claim: the average is biased *and does not converge to the truth with more samples from the same contaminated distribution*... actually the average converges to E[window] > truth; the min converges to the essential infimum = truth. So the min is asymptotically consistent but biased at finite N. The doc's own table shows min-of-16 worst +0.47% — biased high. So "unbiased" is the wrong word; "consistent" or "convergent from above" is right. Flag it.

**Issue 85: Another look at benchmarks.md new section: "The `tscdrift` lane measured the TSC against the CMOS RTC and found the counter ticks at exactly 1000.000 MHz under plain TCG. It also found, without looking for it, that the rate the kernel *stores* had nothing to do with that: twenty-two boots of one binary wrote down 1001 MHz to 4330 MHz. This lane took that further, fixed it..."** — "This lane" vs "The tscdrift lane": in benchmarks.md, which lane is "this lane"? The milestone's lane (the calibration-fix lane). The benchmarks.md note says "This lane took that further, fixed it, and this section is what the fix means". OK.

**Issue 86: The correction blockquote: "the `ns` column alone"** — bold claim that only ns column is suspect; ticks unaffected. OK.

**Issue 87: Wait, there's one more thing to check in the benchmarks.md new section: "- **The 2026-09-15 lazy-TSS section and the icount baselines are not affected.** They quote deterministic ticks under `-icount` and say so; `bench/baseline-*.txt` holds ticks."** The 2026-08-24 section being marked is about yield_switch/tss_iomap_switch; the "lazy-TSS" section dated 2026-09-15 likely exists in the truncated part. Fine.

**Issue 88: The new section says "- **`Instant`, `uptime` and the shell's `time` on x86** all read it through `counter_frequency_protocol`'s page."** tsc note says "`Instant` and `uptime` through `counter_frequency_protocol`'s page". Adds "the shell's `time`". Fine.

**Issue 89: "- **Nothing went red over any of it**, which is why it lasted. `kernel::user::wait_for` takes `now()` plus two seconds, so an inflated rate makes a timeout *longer* in real time, never shorter. The defect could only ever fail safe."** Consistent with both other files.

Hmm wait, actually — let me double-check the fail-safe direction. Inflated stored rate (e.g., 4330 MHz vs true 1000 MHz). wait_for computes deadline = now() + 2 s in ticks: 2 s * 4330 MHz = 8660 Mticks. Real time for those ticks to elapse = 8660/1000 = 8.66 s. So timeout is longer in real time. "fail safe" per the note. OK consistent.

And ns/iter: measured ticks / iters → ns = ticks / rate. Inflated rate → ns too small. "may read faster than the run really was". Consistent.

**Issue 90: One more on the milestone: "the worst this lane saw, +1153%, is a 125 ms one"** — 125 ms window → 12.5× rate → +1150%. More precisely +1153% → 125.3 ms. OK.

**Issue 91: The milestone says the probe "prints every calibration window, which is what turned the probe from 'is the calibration right' into 'is the estimator right' and is how the tables above were measured."** Fine.

**Issue 92: Is there any concern about the milestone claiming "Status: BUILT" while the commit contains only docs?** The commit message frames this as recording the fix; the code presumably landed separately. Reviewing this PR alone, the milestone claims BUILT with specific code changes (timer.rs, main.rs, new test file, tsc_probe.rs changes, qemu-runner changes) — none of which are in this diff. If this PR is the whole change, the milestone records code that isn't in the tree. But the commit message says "Record the calibration fix", implying the fix commit preceded. The review bundle only shows this commit. Should I flag "the milestone describes code changes not present in this commit"? The prompt says "Below is the commit as it would appear in a pull request" — a PR with a single commit. If the PR contains only docs but the milestone says BUILT and lists code diffs, a reviewer can't verify the code exists. But this pattern (docs commit recording a milestone after the code landed) seems normal for this repo ("design records and roadmap are kept in-tree as Markdown and are treated as part of the deliverable"). The Follow-on section says things like "Recorded. ... is in `kernel/src/arch/x86_64/timer.rs`'s `BUGS`" — checkable only against the tree. I think flagging "this PR adds a BUILT milestone but contains no code" might be noise if the repo convention is separate record commits. The commit message explicitly says "Record the calibration fix", so the intent is clear. I'll skip this or... hmm. Actually, as a PR reviewer, I'd want the milestone record to land *with* the code it records, or at least reference the commit. But given the repo's conventions (records kept in-tree, possibly landed after), and the explicit commit message, I'll not flag it as a top concern. Maybe.

Let me now also double-check the "12x" heading against the commit message: commit message says "notes/benchmarks.md marks the affected table ... Every x86 wall-clock figure this tree published was derived from the one-window calibration". The heading "wrong by up to 12x" — also note the *rate* was wrong by up to 12.5x; the *calibration* being "wrong by 12x" loosely matches. The precise figure is +1153%. Since the same section's body and the milestone and the note all say +1153%, the heading's "12x" is both inconsistent and slightly understating. Flag.

**Issue 93: The tsc-under-tcg.md edit removed the "This is tracked" paragraph pointing to the proposal and replaced with FIXED. But the sentence before it (kept): "What reads the number: ... Deadlines fail safe ...; the reported numbers do not."** Fine.

**Issue 94: Hmm, tsc-under-tcg.md updated text: "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** Wait — "those 200 boots" — the 200 boots at load 30. The fix's numbers (0/200, +0.47%) come from the min-of-16 estimator applied to the recorded 16 windows per boot. And the shipped estimator is early-stop min, which the milestone says matches full-cap boot-for-boot on all 490. OK.

**Issue 95: Let me check the milestone's "Index row" section content for consistency: "The x86 boot measured the TSC against a single 10 ms PIT window and stored the result for the life of the boot, and that number was wrong by up to **+1153%** against a counter known to tick at 1000.000 MHz, high on every one of 490 boots and never low, because a poll can only notice the PIT's terminal count late."** OK. "Everything time-derived on x86 read it: `bench --x86 --real`'s ns/iter, `Instant`, `uptime`, `coremark`'s self-reported rate." OK. "stops as soon as two windows agree to one part in a thousand, so the mean cost is 3.5 windows on a quiet host and 5.2 on a saturated one against a cap of sixteen. calef's ruling said five; five was measured and found to leave a +51% worst case, which is why the cap is sixteen and the five is what it spends." OK.

**Issue 96: The milestone's heading "calef's ruling named the shape; the count was measured" with quote "Fix the calibration with min-of-five windows." (2026-09-21)** — fine.

**Issue 97: Wait, one potential factual slip in the milestone: "Milestone 524 (the three x86_64 boot gates: NX, SYSCALL, and the invariant TSC) had just argued that a non-invariant TSC 'cannot be caught by measuring harder'."** Then "The `tscdrift` lane then went to check whether TCG's TSC drifts at all... and found that it does not". Fine.

**Issue 98: Now let me reconsider benchmarks.md structure: the new section is appended at the very end after the "exemption is measured ... route around it." paragraph, which appears to be the end of some other section (about x86_trap, 152 bytes, gate green with room). The new `## 2026-09-21` heading follows immediately without a blank line. Already flagged as issue 14.

**Issue 99: Also in that heading: "the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above" — and the blockquote at the 2026-08-24 table says "See the 2026-09-21 section at the end of this file".** OK.

**Issue 100: One more possible inconsistency: the milestone says the probe span is "thirty-two seconds" ("`tsc_probe` gets its precision by watching for an **edge** and then spanning thirty-two seconds")** and BUGS says "an off-by-default feature costing thirty-five seconds a boot". tsc note says "takes thirty-five seconds of wall clock". OK consistent.

Now, which concerns are worth reporting? The instruction: "One concern per item, each naming the file and stating what is wrong and why it matters."

Let me pick the solid ones:

1. **notes/benchmarks.md — missing blank line before the new `## 2026-09-21` heading.** The heading directly abuts the previous paragraph ("...route around it." then `## 2026-09-21: ...`). Every other heading in the file is blank-line separated; strict Markdown parsers/lints (MD022) will treat/flag this, and it can render as part of the paragraph. Matters: the file is a deliverable; the correction section may not render as a heading.

2. **notes/benchmarks.md — heading says "wrong by up to 12x" but every other figure in the same commit says +1153%, which is 12.53x.** "Up to 12x" understates the recorded worst case and is inconsistent with the body of the same section ("+1153%") and with tsc-under-tcg.md and the milestone. Matters: this file's whole point is numeric precision; the headline number is the one a reader quotes.

3. **notes/benchmarks.md — the in-place correction claims "the debug-versus-release argument ... [is a] ratio within a boot", but the section's own setup is six debug boots and five release boots.** A debug:release comparison in ns/iter is necessarily across boots, and each boot stored a different wrong rate, so the calibration does not cancel there. Either the notice overstates what survives, or the argument is actually made in ticks and the notice should say so. Matters: the notice exists to tell readers exactly what is safe to quote; if it misclassifies the section's central comparison, a reader will quote a contaminated ratio as safe.

Hmm wait, I should be careful. Let me re-read the blockquote once more:

"Nothing can recover the true values: the rate each of these eleven boots stored was not written down, and it was a different number each time. The *ticks* they were derived from are unaffected, and so is every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x