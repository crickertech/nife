1. **design/roadmap/555-a-program-that-asks-the-cpu-dies-without-saying-so.md and design/roadmap/556-a-second-risc-v-implementation-for-rent.md (the renumber is performed as a two-file change, with no referrer audit).** In this tree, milestones are cited by bare number — these very files cite "Milestone 442", "Milestone 447", and "milestone 81" — and each roadmap file carries an "## Index row" section that exists to feed a number-keyed roadmap index. Renumbering 525→555 and 527→556 therefore obligates a tree-wide check for citations of the old numbers and an update to the index rows. Because the old numbers were not merely vacated but *taken by other lanes' milestones*, any surviving reference to "525" or "527" that meant these documents will not dangle — it will silently resolve to a different, unrelated milestone. No link checker or build step can catch a reference that is valid but wrong, so this is precisely the class of defect that escapes automation; the fix is to grep the tree for the old numbers and old filenames and update referrers in the same commit.

2. **Both files: the italic note "*(Number provisional until the merge queue lands it.)*" is left in place alongside the new renumbering sentence, and the commit gives no indication of whether 555/556 are the landed, final numbers.** If this PR is the merge queue landing these lanes, the provisional disclaimer is now stale and self-contradictory next to a sentence describing the number's assignment history; if 555/556 are still provisional placeholders, the renumber note "Renumbered from 525 to 555" overstates the permanence of what is still an unassigned number. Either way the status paragraph now hedges in two directions at once, and in a repository where these records are the deliverable, the document should say plainly which state the number is in.