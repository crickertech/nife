1. **Stale cross-references to the old numbers, made silently wrong rather than broken** — `design/roadmap/555-a-program-that-asks-the-cpu-dies-without-saying-so.md` and `design/roadmap/556-a-second-risc-v-implementation-for-rent.md`: The renumber updates the filenames, H1s, and status paragraphs, but nothing else in the tree. These documents themselves demonstrate that milestones are habitually cited by bare number in prose (this pair alone cites milestones 442, 447, and 81, plus DECISIONS §19/§203), and each file carries an "Index row" section — implying an index keyed by milestone number exists elsewhere in the tree. Any such reference or index entry pointing at "525" or "527" is left behind by this commit. What makes this worse than an ordinary dangling reference is the commit's own premise: the old numbers have been taken by *other* concurrently merged milestones, so stale citations won't dangle — they will silently resolve to different, unrelated work, and even a link-checker would pass them. The commit should either update the in-tree citations of 525/527 (index rows, DECISIONS, notes, sibling milestones) or state that none exist. This is exactly the class of defect no automated check can catch, and in a repo where the Markdown records are part of the deliverable, a citation that points at the wrong milestone corrupts the record.