[no text block returned; the model spent its budget on thinking]

Let me carefully review this commit. The commit is documentation-only: it adds a roadmap milestone file (526), deletes the proposal it promotes, and edits two notes files (benchmarks.md and tsc-under-tcg.md).

Let me look for inconsistencies, factual errors, broken references, and internal contradictions.

1. **The new milestone file 526** — Let me check its claims against the rest.

2. **notes/benchmarks.md changes**:
   - A correction block is inserted before "### The numbers" in a section dated 2026-08-24 (the x86 ns/iter table).
   - A new dated section at the end: "2026-09-21: the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above".

3. **notes/tsc-under-tcg.md changes**:
   - The "This is tracked" paragraph is replaced with "FIXED, 2026-09-21, the same day" text.

Let me hunt for issues:

**Issue A: Heading number inconsistency — "up to 12x" vs "+1153%"**

The new section heading in benchmarks.md says "the x86 boot calibration was wrong by up to 12x". +1153% means the stored rate is 12.53x the true rate (1000 MHz → 12,530 MHz). Actually +1153% means value = original + 11.53×original = 12.53× original. So "up to 12x" is roughly right (12.53x). Hmm, "up to 12x" — 12.53x is more than 12x. "up to 12x" understates slightly; but colloquially "12x" might be intended as "12.5x" rounded down. Actually wait — the milestone 526 file says "the worst this lane saw, +1153%, is a 125 ms one" — a 125 ms window reported as 10 ms gives 12,530 MHz stored rate, i.e., 12.53x the true rate. So "wrong by up to 12x" is a slight understatement but arguably rounding. Hmm, is this a real concern? The commit message says "4330 MHz over twenty-two boots became +1153% over 490". 4330 MHz is +333% (4.33x). The title of the deleted proposal says "wrong by 4x". So the heading "wrong by up to 12x" is consistent with +1153% ≈ 12.5x... but "up to 12x" vs 12.53x — it's fine-ish as approximation, though the rest of the doc is precise. Actually, the heading could be considered slightly wrong: +1153% is 12.53x, so "up to 12x" understates. But more importantly, is it inconsistent with the body? The body says "+1153%". A reader might compute 1000 MHz × 12 = 12,000 MHz = +1100%, not +1153%. Minor. Could flag as a nitpick but maybe not the strongest.

**Issue B: The correction block in benchmarks.md references "the 2026-09-21 section at the end of this file"** — the new section is indeed appended at the end. OK.

**Issue C: The correction block says "each of these eleven boots"** — the commit message says "each of those eleven boots stored a different wrong rate". The section says "Six boots debug, five release" — that's 11 boots. OK consistent.

**Issue D: tsc-under-tcg.md says "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** Wait — the 0/200 and +0.47% figures are for **16 windows** (the cap), per the table. But the shipped fix is not "always 16 windows" — it's min-of-N with early stopping when two agree within 1/1000, capped at 16. The milestone file claims "Measured over the same 490 boots, this reaches **exactly** the accuracy of taking the full cap every time, boot for boot". So the claim is the stopping rule reaches the same accuracy as the full cap. So "0 of those 200 boots wrong by more than one per cent, worst +0.47%" is consistent with the 16-row of the table, and the milestone asserts the early-stop version matches the cap exactly. OK, internally consistent — though it relies on the milestone's claim. Fine.

**Issue E: The tsc-under-tcg.md edit says "which is what the proposal this paragraph used to point at became."** The proposal file is deleted by this commit. The paragraph now says milestone 526. OK. But wait — does anything else still reference the deleted proposal? Let me check the before-files. In tsc-under-tcg.md before: the reference is in the paragraph being replaced. The milestone 526 file says the proposal "is removed by this milestone". The BUGS section of tsc-under-tcg.md (unchanged) — let me check... The BUGS section doesn't reference the proposal. OK.

But wait — the commit only shows changes to these files. Other files in the repo might reference the proposal (e.g., `kernel/src/arch/x86_64/timer.rs`'s BUGS, the roadmap index). We can't see those. The milestone says "(Number provisional until the merge queue lands it; 524 onward is contested between in-flight branches.)" — fine.

Hmm, but the review is of the commit as presented, with before-contents of changed files. We should review what's in front of us. Concerns should be about the changed files.

**Issue F: benchmarks.md new section — "the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'."** Wait, the new section is dated 2026-09-21 and is at the END of the file. It refers to "the 2026-09-21 section above" — is there another 2026-09-21 section above? Looking at the before file: there are sections "The floors name the nightly they were read against (2026-09-21)" and "The emulator is the other half of the same fact (2026-09-21)". The CoreMark "reports correctness, not yet a score" text is in the milestone-19e section ("This binary reports correctness, not yet a score; timing a run needs a userspace clock..."). That section is not dated 2026-09-21; it's the 19e section. So the reference "the 2026-09-21 section above" is wrong — the quoted text is in the Compute vs OS primitives section (milestone 19e), not in a 2026-09-21 section. Actually wait, let me re-read: "No such score is published; the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'." Hmm. The quote "reports correctness, not yet a score" appears in the 19e section: "This binary reports correctness, not yet a score; timing a run needs a userspace clock". That section is headed "## Compute vs. OS primitives: two benchmarks that measure different things (milestone 19e)". It is not a 2026-09-21 section. So the citation is wrong — there are two 2026-09-21 sections above (the floors one and the emulator one), and neither records that CoreMark quote. That's a genuine broken cross-reference. 

Also, structurally: the new section itself is dated 2026-09-21, so "the 2026-09-21 section above" is ambiguous — there are now three 2026-09-21 sections. The intended target was probably "the milestone-19e section". This is a real concern.

**Issue G: The correction callout placement.** The correction block is inserted immediately before "### The numbers" in the 2026-08-24 section. It says "every `ns/iter` in this section is suspect". Fine.

But wait — does the correction blockquote get the section date right? It says "These were computed from the guest's calibrated TSC, and on 2026-09-21 the x86 boot calibration was found to be wrong by up to +1153%". OK.

**Issue H: "and `kernel/src/arch/x86_64/timer.rs`'s `BUGS`"** — references timer.rs BUGS; milestone says the BUGS section was rewritten. OK.

**Issue I: The heading says "wrong by up to 12x" but the correction blockquote and body say +1153%.** +1153% = 12.53x. "up to 12x" is inconsistent-ish. Actually, let me compute precisely: a 125 ms window reported as 10 ms → rate = 125/10 × 1000 MHz = 12,500 MHz = 12.5× truth = +1150%. The doc says +1153% and "a 125 ms one". 12.53x. So "up to 12x" rounds down; the actual worst is 12.5x. A reader checking the heading against the table finds 12.5x, not 12x. Given this repo's culture of precision (the docs obsess over exact figures), this mismatch is worth flagging. Though it's a minor numeric nit. Also the commit message says "became +1153% over 490" — fine.

**Issue J: The new benchmarks.md section says "Two hundred boots against a counter known to tick at 1000.000 MHz, on a host deliberately saturated to load 30 on eight cores"** then gives the table. Then: "Every one of the 490 boots measured across three host loads was high, never low". Consistent with milestone (200 boots at load 30; 490 total across three loads). OK.

**Issue K: benchmarks.md new section: "at this host's load roughly one boot in four was wrong by more than a per cent"** — check: 56/200 = 28%, roughly one in four. OK. But the milestone says "Five still leaves one boot in eighteen wrong by more than a per cent" — 11/200 = 5.5% ≈ 1 in 18. OK.

**Issue L: The before/after controlled pair table**: "Before (one window) 1003.7 MHz +0.37%; After (min of N) 1000.1 MHz +0.01%". It says `CALIBRATION_WINDOW_CAP` at 1 and at 16. But the shipped fix isn't "min of 16" — it's min with early stopping capped at 16. Setting the cap to 16 means up to 16 windows with early stop. Saying "differing in one constant (CALIBRATION_WINDOW_CAP at 1 and at 16), so the before case is the old estimator exactly" — with cap=1, one window, yes that's the old estimator. With cap=16, the early-stopping applies. The label "After (min of N)" is fine. OK, no issue.

**Issue M: "The `ticks` columns of those two runs differ by up to 4x, and that is not the calibration.** It is host load between the two runs". Hmm — ticks differ by up to 4x between two runs due to host load. Plausible for --real. OK.

**Issue N: The milestone file's "Index row" section.** Roadmap files presumably have an index row that gets added to an index. The milestone includes "## Index row" with "Built: 2026-09-21". We can't verify the index file itself was updated — it's not in the diff. If the repo convention is that the index lives in a separate file (e.g., design/roadmap/README.md or index), then this commit should have updated it. But the milestone file includes the index row text "for" the index. Hmm. We can't verify. The instructions say review the commit; changed files are only these four. If convention requires index update, its absence is a concern — but we don't know the convention for sure. The milestone says "*(Number provisional until the merge queue lands it; 524 onward is contested between in-flight branches.)*" which suggests the number/index will be fixed at merge. So maybe not a concern.

**Issue O: tsc-under-tcg.md — "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** Wait, there's a subtlety: the 200 boots at load 30 — the table says for 16 windows: 0/200 boots wrong by >1%, worst +0.47%. The milestone says the early-stopping version "reaches exactly the accuracy of taking the full cap every time, boot for boot". So the claim in tsc-under-tcg is fine per the milestone's claim. But hold on — the sweep that produced the table: "Two hundred boots on a host deliberately saturated to load 30 on eight cores, each timing sixteen windows and reporting every one, give the error of the min over the first k of them". So the 200 boots each timed 16 windows; the min over first k gives the k-row. The early-stop measurement was "over the same 490 boots". OK consistent.

**Issue P: The commit message says "notes/tsc-under-tcg.md's finding is marked fixed, with the correction that measuring harder made the defect worse: 4330 MHz over twenty-two boots became +1153% over 490."** Fine.

**Issue Q: milestone 526 file — "Milestone 524 (the three x86_64 boot gates: NX, SYSCALL, and the invariant TSC) had just argued that a non-invariant TSC 'cannot be caught by measuring harder'."** OK.

**Issue R: milestone 526 "What is in the diff" lists kernel/src/arch/x86_64/timer.rs, kernel/src/main.rs, timer_calibration_tests.rs, tsc_probe.rs, scripts/qemu-runner-x86_64.sh, notes/benchmarks.md.** But this commit contains NONE of those code changes — it's docs-only. The milestone describes a diff that isn't in this commit. Is that a concern? The commit message says "Record the calibration fix: milestone 526, the note, and the suspect numbers" — implying the code landed elsewhere (perhaps in a prior commit or parallel lane), and this commit records the documentation. Hmm. But the milestone is marked "Status: BUILT 2026-09-21" and describes code changes (timer.rs loop, main.rs boot line, new test file, qemu-runner fix) that are not part of this commit. If the code isn't actually in the tree yet, marking BUILT is premature. But we can't verify from the given materials whether the code landed in a separate commit. The commit message says "Record the calibration fix" — implying the fix exists and this records it. Since we're told to review this commit with the given files, and the before-contents of only the changed files are provided, we can't confirm the code exists. This might be a concern: the milestone claims changes to files not touched in this commit; if those changes didn't land, the doc is false. But it's plausible they landed in a companion commit. Given the review setup (only these files changed in the PR), the PR adds a milestone claiming code changes that the PR itself doesn't contain. In a real PR review, I'd ask: where's the code? The milestone says "What is in the diff" — but this PR's diff has no code. That's a legitimate review concern: either the code changes are missing from the PR, or the milestone describes another branch's diff, in which case merging this alone records as BUILT a fix that isn't in the tree.

Actually, let me re-read the commit message: "Record the calibration fix: milestone 526, the note, and the suspect numbers". And "Every x86 wall-clock figure this tree published was derived from the one-window calibration, so notes/benchmarks.md marks the affected table..." The message frames this commit as *recording* a fix. The milestone's "What is in the diff" section enumerates code files. If the code change is a separate commit in the same PR... the PR is presented as a single commit. So the PR adds docs claiming a code fix that the PR doesn't include. This is the classic "docs say BUILT but no code" problem. I think this is a legitimate top concern: the milestone is marked BUILT and lists kernel code changes (timer.rs, main.rs, new test file, qemu-runner script) none of which appear in this commit; if they land separately, the record should land with them or reference where they are.

Hmm, but maybe the repo's workflow is that lanes land code and docs separately and the merge queue assembles them. The note "(Number provisional until the merge queue lands it...)" suggests a multi-lane workflow. Still, as a reviewer, flagging "this PR contains no code, yet the milestone it adds is marked BUILT and enumerates a diff including kernel code and a new test file — either the code is missing or the status is premature" is fair and important.

**Issue S: The deleted proposal vs. references to it.** The proposal file is deleted. Does anything else in the tree reference it? In the before tsc-under-tcg.md, the reference is updated. The milestone 526 says the proposal "is removed by this milestone". Are there other references elsewhere (e.g., in timer.rs BUGS, or a proposals index)? We can't know. Not flaggable from given material... but wait — the milestone file itself says "The proposal this promotes, `design/roadmap/proposals/the-x86-boot-calibrates-once-and-can-be-wrong-by-4x.md`, is removed by this milestone: its two open questions are both answered above (the boot does print its confidence, and the local APIC timer is derived from the same windows)."

Hmm — "the local APIC timer is derived from the same windows". But the milestone's BUGS section says: "The local APIC timer's rate gets the same treatment and has no `CPUID` escape. It is the minimum over the same windows, taken independently of the TSC's." Wait — "the minimum over the same windows, taken independently" — is it derived from the same windows or measured independently? "It is the minimum over the same windows, taken independently of the TSC's" — meaning the APIC rate is computed as its own minimum over the same set of windows. OK consistent-ish. But the proposal's open question was "Whether the local APIC timer's rate should be derived from the same windows." The milestone answers: yes, same windows, independent minimum. Fine.

**Issue T: tsc-under-tcg.md edit — "The boot now times several windows and keeps the smallest... it stops as soon as two windows agree to one part in a thousand, so the mean cost is 3.5 windows on a quiet host; and the boot line prints the worst window beside the chosen one".** Consistent with milestone. OK.

**Issue U: The note's "Reproducing it" section in tsc-under-tcg.md (unchanged) still says "the calibration fix proposed above is what it exists to be measured against."** Wait — "This note is its owner, and the calibration fix proposed above is what it exists to be measured against." That sentence is in the unchanged part. After this commit, the fix is no longer "proposed" — it's done (marked FIXED at the top). The sentence "the calibration fix proposed above" is now stale: there is no proposal anymore; the fix has landed. Actually wait, let me check: the "Reproducing it" section says "It is an instrument rather than a test... **This note is its owner**, and the calibration fix proposed above is what it exists to be measured against." After the commit, the finding is marked FIXED and the proposal deleted. So "the calibration fix proposed above" is a dangling reference to a proposal that no longer exists in the tree, and the phrasing "proposed" contradicts the new "FIXED" status. This is a genuine concern: the commit updates the tracking paragraph but misses this other reference to the proposal in the same file. Let me double check the before text: "This note is its owner, and the calibration fix proposed above is what it exists to be measured against." Yes. And the commit doesn't touch it. So notes/tsc-under-tcg.md still contains a reference to "the calibration fix proposed above" — but the proposal is deleted and the paragraph that pointed at it now says FIXED. A reader looking for the proposal finds nothing. This is a real inconsistency within the same file. Good catch.

**Issue V: benchmarks.md correction block says "See the 2026-09-21 section at the end of this file"** — there are now multiple 2026-09-21 sections in the file (the floors one, the emulator one, and the new one). "The 2026-09-21 section at the end of this file" disambiguates by "at the end". OK fine.

**Issue W: The new section heading: "## 2026-09-21: the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above"** — appended directly after the previous section's last paragraph without a blank line? Let me check the diff: 

```
 teaches people to route around it.
+## 2026-09-21: the x86 boot calibration was wrong by up to 12x, and what that does to the numbers above
```

There's no blank line between "teaches people to route around it." and the new "##" heading. In Markdown, a heading immediately following a paragraph without a blank line — most Markdown parsers (CommonMark) actually do recognize ATX headings after a paragraph without a blank line? Let me think. In CommonMark, an ATX heading can interrupt a paragraph? Actually yes — in CommonMark, ATX headings can interrupt a paragraph (unlike setext headings). Wait, no: CommonMark spec says ATX headings can interrupt a paragraph. Let me recall: "An ATX heading ... can interrupt a paragraph" — hmm. In CommonMark, headings (ATX) can interrupt a paragraph; blank line not required. Actually checking the CommonMark spec: Section 4.2 ATX headings — "This is not a Markdown... " Hmm. CommonMark: "##" can interrupt a paragraph? Yes — ATX headings can interrupt paragraphs in CommonMark. But some renderers (e.g., original Markdown.pl, some strict parsers) require a blank line. GitHub (CommonMark) renders it fine. Still, style-wise the file's other headings are preceded by blank lines. This is a formatting nit. The bigger issue is missing blank line may render as part of the paragraph in some parsers. Actually in CommonMark, ATX headings DO interrupt paragraphs, so it renders fine on GitHub. It's a minor style nit. Maybe worth mentioning, but there are bigger fish.

**Issue X: The new benchmarks.md section says "within it the `ns` column only. Ticks are raw `rdtsc` counts and are untouched."** OK.

**Issue Y: "The 2026-09-15 lazy-TSS section and the icount baselines are not affected. They quote deterministic ticks under `-icount` and say so; `bench/baseline-*.txt` holds ticks."** We can't verify a 2026-09-15 lazy-TSS section exists — it may be in the truncated part of the file (the before file was truncated at 1200 lines). Hmm, the before file we got was truncated ("[... truncated at 1200 lines by scripts/review-bundle.sh ...]"). So there may be sections we can't see, including the 2026-08-24 x86 section and the 2026-09-15 lazy-TSS section. The diff context shows the correction inserted at line ~2664 before "### The numbers" with context "per iteration (both threads resume once each), so the delta divided by two is th..." — so the 2026-08-24 section exists somewhere in the truncated region. OK.

**Issue Z: The correction blockquote says "every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly."** Hmm — "the debug-versus-release argument" — is that a ratio within a boot? Debug and release are different builds and different boots! Six boots debug, five release. A ratio between a debug number and a release number is a ratio across two DIFFERENT boots, each with its own wrong calibration. The calibration cancels only within one boot. The debug-vs-release comparison divides a debug ns/iter (boot A, wrong rate r_A) by a release ns/iter (boot B, wrong rate r_B): the ratio involves r_B/r_A, which does NOT cancel. So the claim "the debug-versus-release argument ... [is a] ratio within a boot, where the calibration cancels exactly" is wrong if debug and release were measured in different boots — which they were ("Six boots debug, five release").

Wait, let me be careful. Maybe the debug-versus-release argument compares ns/iter debug vs ns/iter release. Each boot stored a different wrong rate. The ratio debug/release in ns = (ticks_debug / r_debug) / (ticks_release / r_release) = (ticks_debug/ticks_release) × (r_release/r_debug). Since r_debug ≠ r_release (each boot had a different wrong rate), the calibration does not cancel. So the correction block's claim that "every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x" — the debug-versus-release argument is NOT a ratio between two rows of the same run; it's across runs (different builds, different boots). Unless the section's debug-vs-release argument was computed in ticks (which are unaffected). Hmm. We can't see the 2026-08-24 section (truncated). The commit message says "What survives untouched is every conclusion resting on a ratio between two rows of one boot, because the calibration cancels exactly in a ratio, and that is all of them."

If the debug-vs-release comparison is between two rows of... hmm, "two rows of one boot". Debug and release can't be rows of one boot (different binaries). Unless the table has, per boot, both? No — "Six boots debug, five release". So debug rows come from debug boots and release rows from release boots. A debug-vs-release ratio is across boots. The claim that it cancels is mathematically wrong... unless the debug-vs-release argument was made in ticks, not ns. The correction says "The *ticks* they were derived from are unaffected, and so is every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly."

Hmm wait — maybe the 4.2x is the debug-vs-release tax (like "release is 4.2x faster than debug"). If computed from ns/iter of a debug boot and ns/iter of a release boot, it's cross-boot. If computed in ticks, calibration is irrelevant. Either way, calling it "a ratio within a boot, where the calibration cancels exactly" is confused: if it's in ticks, calibration isn't involved at all (nothing to cancel); if it's in ns across two boots, calibration doesn't cancel. The statement "the debug-versus-release argument ... [is a] ratio within a boot" is factually suspect because debug and release numbers necessarily come from different boots (different binaries). This looks like a genuine technical error in the correction. But since the 2026-08-24 section is truncated from our view, we can't fully verify what "the debug-versus-release argument and the 4.2x" are. The diff context line "per iteration (both threads resume once each), so the delta divided by two is th..." suggests yield_switch discussion. Given "Six boots debug, five release", the 4.2x is presumably the debug-to-release tax measured across the two sets of boots. That is NOT a within-boot ratio. So the correction's claim is wrong.

Hmm, but hold on. Let me reconsider: maybe within the 2026-08-24 section, the "debug-versus-release argument" is argued from ratios like "debug ns/iter vs release ns/iter for the same bench" — cross-boot. The calibration error differs per boot. So the ratio doesn't cancel. Unless... they paired them somehow. We can't verify. But the sentence as written asserts these are "ratios within a boot" — and a debug build and a release build cannot run in the same boot (they're different kernels). Actually wait — could a single boot run both debug and release benches? No; the kernel itself is built debug or release. So any debug-vs-release ratio spans two boots. The claim "ratios within a boot, where the calibration cancels exactly" is therefore wrong for the debug-versus-release argument. This is a solid concern, phrased carefully: the correction over-claims; a debug/release ratio is across boots with different stored rates, so the calibration does not cancel; at best the errors are independent and both unknown.

Actually, wait. Let me reconsider once more. Maybe the debug-versus-release argument in that section is stated in ticks ("debug takes 4.2x the ticks"). The correction says "The ticks they were derived from are unaffected, and so is every conclusion here that rests on a ratio between two rows of the same run". Two different things are conflated: (1) tick-based conclusions (unaffected because ticks are raw), (2) within-boot ns ratios (cancel). The debug-vs-release argument, if in ticks, is unaffected for reason (1), not because "the calibration cancels exactly in a ratio". Saying it "cancels exactly" for a cross-boot ratio is wrong. Either way the sentence misattributes. I think this is worth flagging as a concern with appropriate hedging — the reviewer can't see the section, but the sentence groups "the debug-versus-release argument" (necessarily cross-boot, since debug and release are different binaries booted separately — the section itself says six boots debug, five release) with within-boot ratios. That's an error.

Let me also check the commit message: "What survives untouched is every conclusion resting on a ratio between two rows of one boot, because the calibration cancels exactly in a ratio, and that is all of them." Same claim. The message says "each of those eleven boots stored a different wrong rate and none was written down" — eleven boots, six debug + five release. So the debug-vs-release comparison spans boots with different wrong rates. The claim that "all" conclusions survive because they're within-boot ratios is wrong for the debug-vs-release conclusion. Strong concern.

**Issue AA: milestone 526 says "Five still leaves one boot in eighteen wrong by more than a per cent"** — 11/200 = 1 in 18.2. OK.

**Issue AB: milestone 526 "The tail only closes at sixteen."** OK per table.

**Issue AC: milestone 526 "Measured over the same 490 boots, this reaches exactly the accuracy of taking the full cap every time, boot for boot, at a mean of:"** — then a table of mean windows. OK.

**Issue AD: milestone 526 "The measured boot cost, stated plainly: `script/test --arch x86_64` boots the kernel four times, and those four boots took 3, 3, 4 and 3 windows. So the suite pays 90 ms more than the one-window design did"** — compute: old design = 1 window per boot = 10 ms × 4 = 40 ms. New = (3+3+4+3) × 10 ms = 130 ms. Delta = 90 ms. OK. "against a 3m38s runtime: 0.04%" — 90 ms / 218 s = 0.0413%. OK.

**Issue AE: milestone 526 "at ordinary session load one window's worst of 30 boots was +773%, and min-of-9 got to +0.01%; at load 22 the same estimators gave +318% and +0.34%."** Consistent-ish with the rest; 490 boots = 200 at load 30 + others. 490 - 200 = 290 across other loads. "30 boots" at ordinary load and some at load 22... 200 + 30 + ? Hmm, 490 total: maybe 200 at load 30, 30 ordinary, 260 at load 22? Not stated precisely. Not a contradiction per se. The tsc note says "Milestone 526's sweep ran 490 boots at three host loads". Fine.

**Issue AF: The milestone's BUGS: "0 of 200 boots at load 30 were wrong by more than 1%"** — that's for the full cap of 16. OK.

**Issue AG: The commit deletes the proposal but the milestone says "its two open questions are both answered above (the boot does print its confidence, and the local APIC timer is derived from the same windows)."** OK.

**Issue AH: qemu-runner fix description** — "The runner now runs QEMU in the background, traps TERM and HUP, forwards them, and reaps. It also needed qemu-bounded.sh's own fd-3 dance, because backgrounding a command gives it /dev/null on stdin under dash". OK, describes code not in this commit (again the no-code issue).

**Issue AI: tsc-under-tcg.md's BUGS section (unchanged) says: "`scripts/qemu-bounded.sh`'s killer did not fire for this lane, repeatedly... This note is not where that gets fixed; see the lane's report and `fix/qemu-bounded-stdin-under-dash`, which is a different symptom of the same script."** The milestone 526 says the runner bug IS fixed in this lane ("The bug this lane tripped over on its way, and fixed", "The x86_64 runner is fixed here and now says so at the code"). The note's BUGS entry says "This note is not where that gets fixed; see the lane's report and fix/qemu-bounded-stdin-under-dash, which is a different symptom of the same script." Hmm — the note's BUGS item refers to a different symptom (stdin under dash) and points to a branch `fix/qemu-bounded-stdin-under-dash`. The milestone fixes the SIGTERM-forwarding issue. Are these the same bug or different? The note's BUGS says "a bounded run whose output consumer exited early left qemu-system-x86_64 orphaned to launchd past its bound" — that sounds like the same symptom (orphaned QEMU) that milestone 526 describes fixing ("The shell died; QEMU was reparented to pid 1 and ran forever"). The milestone says the fix is in qemu-runner-x86_64.sh (trap and forward). The note's BUGS item says the killer "did not fire" and points to a different branch about stdin under dash. After this commit, the note's BUGS entry is stale-ish: the milestone fixed the runner's TERM forwarding, which is the mechanism the note's BUGS describes (orphaned emulator). But the note's BUGS refers to "a different symptom of the same script" (stdin under dash). Actually the milestone mentions "It also needed qemu-bounded.sh's own fd-3 dance, because backgrounding a command gives it /dev/null on stdin under dash" — so the stdin-under-dash issue is entangled. The note's BUGS entry pointing to fix/qemu-bounded-stdin-under-dash might still be valid. Not clearly a concern; the commit doesn't touch that BUGS entry, and it's about a different symptom. Skip.

However — the milestone's Follow-on says: "**Recorded.** `scripts/qemu-bounded.sh`'s `BUGS` section still describes SIGKILL as the way its bound is defeated, and does not mention that a runner which does not `exec` defeats it with an ordinary TERM. The x86_64 runner is fixed here and now says so at the code; the wrapper's own `BUGS` is the place a reader would look first and is left for whoever next touches that file." — This is self-acknowledged. Fine.

**Issue AJ: The milestone file says "(Number provisional until the merge queue lands it; 524 onward is contested between in-flight branches.)"** — but the file is named `526-...` and references milestone 524 as landed. If the number is provisional, naming the file 526 might conflict. Not really a review concern; it's disclosed.

**Issue AK: benchmarks.md — the new section refers to "milestone 526 (the x86 TSC calibration takes the smallest of several windows)" and "`kernel/src/arch/x86_64/timer.rs`".** OK.

**Issue AL: The correction block says "the rate each of these eleven boots stored was not written down, and it was a different number each time."** Commit message: "each of those eleven boots stored a different wrong rate and none was written down." Consistent.

**Issue AM: "and it was a different number each time"** — strictly, could two boots have stored the same wrong rate by chance? Claim is fine.

**Issue AN: The new section says "`coremark`'s self-reported rate on x86 read the stored number, so any x86 CoreMark score computed from it carried the same error. No such score is published; the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'."** As noted in Issue F, the quoted text is in the 19e section, not a 2026-09-21 section. Wait — maybe the truncation hides a 2026-09-21 section about CoreMark? The before file visible to us includes the 19e section with "This binary reports correctness, not yet a score". The visible 2026-09-21 sections are "The floors name the nightly they were read against (2026-09-21)" and "The emulator is the other half of the same fact (2026-09-21)". Neither mentions CoreMark's score reporting. But the file was truncated at 1200 lines — the visible content seems to extend well beyond the truncation notice... wait, actually the truncation notice appears in the middle: "[... truncated at 1200 lines by scripts/review-bundle.sh ...]" and then content continues? Let me re-read the before file for benchmarks.md. The content flows: memory footprint section, cache table, then "[... truncated at 1200 lines by scripts/review-bundle.sh ...]" — and that's the end of the provided before-content for benchmarks.md? Actually the truncation marker appears near the end, after the Apple M-series row of the cache table. So everything after that (including the 2026-08-24 x86 section around line 2664) is NOT visible to us. The diff shows context lines at 2664 and 3781. So the file is ~3800+ lines; we saw ~1200.

So the 2026-08-24 section and any 2026-09-15 lazy-TSS section are in the unseen part. Could there be a 2026-09-21 section about CoreMark in the unseen part? Possibly. The new section says "the 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'". Given the visible 19e section contains exactly that quote, and the new section is itself dated 2026-09-21 (making "the 2026-09-21 section above" ambiguous between at least three sections), the reference is at best ambiguous and at worst wrong. Also the visible 19e section is the one with the quote. Unless there's another 2026-09-21 section in the unseen region that also says it. Given uncertainty, I can flag it as: the cross-reference is ambiguous/incorrect — the quoted sentence lives in the milestone-19e section ("Compute vs. OS primitives"), not in any section dated 2026-09-21; and since the new section is itself dated 2026-09-21, "the 2026-09-21 section above" is ambiguous. Flag with hedging.

**Issue AO: tsc-under-tcg.md: "After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** Hmm, "those 200 boots" — the 200 boots at load 30. The +0.47% worst is from the 16-window row. But the shipped estimator is early-stopping, not always-16. The milestone claims exact equivalence boot-for-boot. So OK per claims. But strictly, the note presents the 16-window row as "after the fix" — the fix is min-with-early-stop. The milestone says equivalence was measured. Fine, consistent.

**Issue AP: The milestone says "The brief for this lane said not to take five as given... five would have been the wrong number to ship."** Then "calef's five is what this costs; sixteen is only what it is willing to spend when the host is fighting." — mean 5.20 on saturated host ≈ 5. Cute. OK.

**Issue AQ: milestone 526: "a 4330 MHz reading is a 43 ms window reported as a 10 ms one, and the worst this lane saw, +1153%, is a 125 ms one."** 125ms/10ms = 12.5x → 12,500 MHz → +1150%. They say +1153%. 12.53x → 125.3 ms. "a 125 ms one" OK approximate.

**Issue AR: The heading "wrong by up to 12x"** — 12.53x actually. And the deleted proposal's title said "wrong by 4x" (4330 MHz = 4.33x). The new heading "up to 12x" understates the body's own +1153% (= 12.53x, "up to 13x" would round up; "12x" rounds down). Given the doc's precision culture, "up to 12x" is technically wrong since the worst was 12.53x — "up to 12x" excludes 12.53x. Flag as minor numeric inconsistency.

**Issue AS: Check "which is what the proposal this paragraph used to point at became"** — grammar OK.

**Issue AT: In the correction block: "so these figures may read faster than the run really was, by anything from a fraction of a per cent to a factor of several."** An inflated rate makes ns/iter too small — "read faster". Factor of several: +1153% inflation → ns/iter = true/12.53, i.e., reads 12.5x too fast. "a factor of several" understates (12x is more than "several"?). "Several" usually means 3-7ish. Hmm, minor. Could mention but it's a stretch. Actually "by anything from a fraction of a per cent to a factor of several" — the worst case is a factor of 12.5. "A factor of several" is arguably an understatement but not false per se. Hmm. But combined with "wrong by up to +1153%" stated two sentences earlier, the reader has the number. I'd skip or fold into the 12x nit.

**Issue AU: Milestone file: "Milestone 524 ... had just argued that a non-invariant TSC 'cannot be caught by measuring harder'."** Then the lane measured harder and found the calibration defect. The commit message says "with the correction that measuring harder made the defect worse" — i.e., the note's recorded worst got worse with more measurement. OK.

**Issue AV: The new benchmarks.md section says: "Every section of this file that quotes x86 numbers should say which one it used, and from here on they do."** — "from here on they do" — but earlier sections that quote x86 numbers (e.g., the 2026-08-24 section) are not retroactively marked with which instrument they used, except the correction block which covers the 2026-08-24 table. Actually the correction block does mark the 2026-08-24 table as calibrated-TSC-derived (i.e., --real). Are there other x86 numbers earlier in the file? The visible part has the 2026-08-15/2026-08-17 riscv sections (riscv, not x86). The unseen part may have x86 sections (2026-09-15 lazy-TSS "quotes deterministic ticks under -icount and says so"). The claim "from here on they do" is forward-looking ("from here on"), so it's about future sections. OK, not a concern. Hmm, "Every section of this file that quotes x86 numbers should say which one it used, and from here on they do" — "from here on" = from this date forward. Fine.

**Issue AW: tsc-under-tcg.md — the "Reproducing it" section's final line (unchanged): "boot any x86 kernel and read the `clocks      :` line, twenty times"** — fine.

**Issue AX: The milestone's "What is in the diff" mentions notes/benchmarks.md but not notes/tsc-under-tcg.md, which IS in this commit.** Minor: the milestone's diff list omits the tsc-under-tcg.md update. But the milestone describes the code lane's diff, and this commit is the docs record. Eh. Could fold into the no-code concern.

**Issue AY: The deleted proposal said "Gate: NONE... It is one function."** The milestone says the fix is bigger (loop, cap, stopping rule, Calibration record, tests, main.rs boot line, runner fix). No contradiction; proposals evolve.

**Issue AZ: "the worst this lane saw, +1153%, is a 125 ms one"** vs table "Worst of 200 +1153%" for 1 window. OK.

**Issue BA: The commit message: "Nothing can recover the true values: each of those eleven boots stored a different wrong rate and none was written down."** Wait — "Nothing can recover the true values" — actually, if ticks are preserved and the stored rates weren't written down, you can't convert ticks to ns retroactively. True. OK.

**Issue BB: benchmarks.md correction: "The *ticks* they were derived from are unaffected"** — the 2026-08-24 table presumably has ns/iter and maybe ticks columns. OK.

**Issue BC: One more check on tsc-under-tcg.md edit: "which is what the proposal this paragraph used to point at became."** The proposal was deleted in this same commit. Fine.

**Issue BD: The milestone says "The old 'one calibration, no averaging' bullet is gone, replaced by two that are true now"** — describes timer.rs BUGS changes not in this commit. Part of the no-code concern.

**Issue BE: Formatting — missing blank line before the new `##` heading in benchmarks.md.** Also check the correction block insertion: it's a blockquote inserted before "### The numbers" with blank lines around it. Diff shows:

```
 per iteration (both threads resume once each), so the delta divided by two is th...
 
 ### The numbers
 
+> **CORRECTION, 2026-09-21: ...**
...
+> `kernel/src/arch/x86_64/timer.rs`'s `BUGS`.
+
 QEMU 11.0.2 ...
```

Good, blank lines present there. But at the end:

```
 teaches people to route around it.
+## 2026-09-21: the x86 boot calibration was wrong by up to 12x...
+
+The `tscdrift` lane measured...
```

No blank line before the `##`. In CommonMark, ATX headings can interrupt a paragraph, so GitHub renders it as a heading. But it's inconsistent with the file's style and some parsers (e.g., Python-Markdown without extensions, or pandoc's strict mode) require a blank line; the heading could render as literal text "## 2026-09-21: ..." appended to the paragraph. Worth a minor flag.

**Issue BF: The milestone file references "AGENTS.md's ladder" (rung three, rung one).** Fine, repo-internal.

**Issue BG: milestone says "`kernel/src/arch/x86_64/timer_calibration_tests.rs`, new: five tests... and two boot assertions"** — code not in commit. Same umbrella concern.

**Issue BH: Does the milestone's "Index row" belong in the file?** Convention unknown. Skip.

**Issue BI: The tsc-under-tcg note's header says "*Name: provisional (`tsc-under-tcg`). A lane's proposal; names are calef's.*"** — unchanged. Fine.

**Issue BJ: In the new benchmarks section: "The `tscdrift` lane measured the TSC against the CMOS RTC and found the counter ticks at exactly 1000.000 MHz under plain TCG."** Consistent with the note. OK.

**Issue BK: "Two `script/bench --x86 --real` runs of the same tree, differing in one constant (`CALIBRATION_WINDOW_CAP` at 1 and at 16), so the before case is the old estimator exactly"** — Wait: with the cap at 1, is that "the old estimator exactly"? The old estimator was a single window with no min. Cap=1 → one window → same. Yes. OK.

**Issue BL: "at this host's load roughly one boot in four was wrong by more than a per cent"** — the 56/200 was at load 30 (saturated), but the before/after pair was presumably run at "this host's load" (ordinary?). Hmm: "That before figure is one draw and a lucky one, which is the honest caveat and the reason the table further up is the real evidence: at this host's load roughly one boot in four was wrong by more than a per cent". "This host's load" — the table's 56/200 = 28% ≈ 1 in 4 was at load 30 (deliberately saturated). If the before/after pair was run at ordinary session load, then "at this host's load" is wrong — the 1-in-4 figure is from the saturated condition, not ordinary load. At ordinary load, the milestone says one window's worst of 30 boots was +773%, but the >1% rate isn't given for ordinary load. The 22-boot ordinary sample in the proposal: 1001-1042 MHz, i.e., up to +4.2%, with 1042 being >1%... 1 of 12 ordinary boots >1% (1042 = +4.2%). So at ordinary load, ~1 in 12, not 1 in 4. The sentence "at this host's load roughly one boot in four was wrong by more than a per cent" — if "this host's load" means the load at which the pair was run (ordinary session), the 1-in-4 figure (56/200 at load 30) doesn't apply. Ambiguous but arguably misattributed. Hmm. Actually maybe "this host's load" refers to the load-30 condition since that's "the hardest condition measured" on this host. It's ambiguous. The milestone says the 200-boot sweep was "on a host deliberately saturated to load 30" — that IS this host (patagonia) at a (deliberate) load. So "at this host's load" could mean "at the [saturated] load [used in the sweep on] this host". It's loose but defensible. I'd not flag this strongly; maybe fold into a minor point. Actually the sentence structure: "That before figure is one draw and a lucky one ... at this host's load roughly one boot in four was wrong by more than a per cent, and the worst seen was +1153%." The +1153% worst was at load 30. So "at this host's load" = the saturated sweep condition. But the before draw (1003.7 MHz, +0.37%) was presumably at ordinary load, where the >1% probability is much lower — which is exactly why it was "lucky". Saying "at this host's load roughly one boot in four" conflates the ordinary load of the pair with the saturated load of the sweep. It's misleading. Hmm, I could flag it as a possible conflation, hedged. Let me not over-flag; the doc does say the table is "the hardest condition measured". I'll consider mentioning it briefly.

**Issue BM: The milestone's table "Windows | Median error | 99th percentile | Worst of 200 | Boots wrong by >1%"** — row 1: median +0.36%, p99 +884%, worst +1153%, 56/200. The proposal/note said 22 boots all high with max 4330 (+333%). The 200-boot sweep found +1153%. Consistent.

**Issue BN: Check the tsc note edit once more: "Milestone 526's sweep ran 490 boots at three host loads and saw +1153%, with 56 of 200 boots at load 30 wrong by more than one per cent. The one-sidedness held without a single exception across all 490. After the fix, 0 of those 200 boots are wrong by more than one per cent and the worst is +0.47%."** 

Wait — "After the fix, 0 of those 200 boots..." — the fix is early-stopping min; the 0/200 and +0.47% come from the min-of-16 row of the pre-fix sweep data (the 200 boots each timed 16 windows; the min over all 16 gives the 16-row). So "after the fix" numbers are derived by replaying the estimator over recorded windows, not from new boots with the fixed kernel. That's fine and the milestone describes it that way ("each timing sixteen windows and reporting every one, give the error of the min over the first k of them"). OK.

**Issue BO: Now the biggest structural question: the PR contains no code.** The milestone is marked BUILT and lists code changes. The commit message says "Record the calibration fix". If the code landed in a different commit/branch, the milestone file's "What is in the diff" refers to a diff not in this PR. As a PR reviewer, I'd flag: this PR adds a BUILT milestone for code it doesn't contain; either the code needs to be in this PR or the milestone should land with the code. Also the qemu-runner SIGTERM fix is described as included ("scripts/qemu-runner-x86_64.sh: forwards SIGTERM to QEMU") but isn't in the diff. This is the kind of thing that matters a lot in review: the record claims work that isn't here.

Given the repo's lane-based workflow with a merge queue, maybe code and docs land separately by design. But the milestone says "What is in the diff" — whose diff? If this commit is the whole PR, the diff doesn't contain those files. It's a legitimate concern to raise.

**Issue BP: "the 2026-09-21 section above" in the new benchmarks section — one more consideration.** The new section is at the end of the file. "Above" it are many sections, including two dated 2026-09-21 (floors; emulator). The quote "reports correctness, not yet a score" is from the 19e section. So the reference is wrong: neither 2026-09-21 section above records that quote. Unless there's yet another 2026-09-21 section in the unseen middle (lines 1200-3780) — e.g., a coremark section dated 2026-09-21. Possible but the visible 19e section has the exact quote. Also the new section itself is dated 2026-09-21, adding ambiguity. I'll flag it as likely-wrong/ambiguous cross-reference.

**Issue BQ: tsc-under-tcg.md — "The boot now times several windows and keeps the smallest, because the error being one-sided is what makes the minimum the right estimator; it stops as soon as two windows agree to one part in a thousand, so the mean cost is 3.5 windows on a quiet host"** — consistent with milestone (3.53). OK.

**Issue BR: Commit message: "script/bench --x86 pins the virtual clock and --real does not, and only the latter reads the calibrated rate."** Hmm — does `--real` read the calibrated rate while icount doesn't? Under -icount, the TSC is driven by the virtual clock; does the kernel still calibrate via PIT and store a rate? The kernel boots the same way; init_frequency still runs and stores a rate. But ns/iter under icount is computed from the same stored rate? Actually under icount, the TSC advances with virtual time (instruction count), and the PIT also advances with virtual time (QEMU_CLOCK_VIRTUAL), so the calibration would be self-consistent-ish? The note says under icount the implied TSC rate vs real time moved 37% between workloads. The bench's ns column under icount is "not wall time and never was". The commit message says "only the latter reads the calibrated rate" — hmm, both boot paths run init_frequency and store a rate; the icount leg's ns/iter is computed from ticks and the stored rate too ("`ns/iter` computed from the guest's own calibrated TSC, the same arithmetic `xtask`'s `run_bench` already does for every other leg" — that's about the 2026-08-24 section which used --real? The correction says "These were computed from the guest's calibrated TSC"). Hmm, does the icount leg use the calibrated rate to compute ns? Under icount, TSC ticks at 1 GHz virtual (or some virtual rate), and the PIT is on the same virtual clock, so calibration would measure the TSC against the PIT accurately-ish in virtual time (polling still one-sided, but under icount descheduling doesn't advance virtual time... actually under icount with sleep=off, virtual time advances per instruction; a host descheduling doesn't inflate the window because virtual time stops when the vCPU thread isn't running). So under icount the calibration is accurate in virtual terms. The claim "only the latter reads the calibrated rate" — the icount leg also reads the stored rate to compute ns, but since virtual time is instruction-driven, the ns are deterministic-ish. Hmm, the statement "script/bench --x86 pins the virtual clock and --real does not, and only the latter reads the calibrated rate" — is it true that the icount leg doesn't read the calibrated rate? The bench computes ns/iter = ticks / stored_rate presumably on both legs. Actually maybe under icount xtask computes ns differently. We can't verify. The new benchmarks.md section says "The calibration defect below affects the `--real` instrument only, and within it the `ns` column only." This implies the icount leg's ns column is also derived from the stored rate but is unaffected because... under icount the calibration can't be inflated by descheduling (virtual time doesn't advance during descheduling). The commit message's "only the latter reads the calibrated rate" is a simplification that might be technically inaccurate (both legs' ns columns derive from the stored rate; the icount leg's rate is just not corruptible this way). Hmm. But maybe in the icount leg, the TSC frequency is known to be exactly the virtual clock and... I can't verify the code. The milestone says "Every x86 wall-clock figure this tree published was derived from the one-window calibration" and "script/bench --x86 pins the virtual clock and --real does not, and only the latter reads the calibrated rate". If the icount leg also converts ticks→ns via the stored rate, then "only the latter reads the calibrated rate" is wrong. But I can't verify from the given files. The note says "bench --x86 --real's ns/iter ... all read the stored number" — listing only --real. And "an icount leg's `ns` column is not wall time and never was". It doesn't say whether the icount ns column uses the stored rate. Under icount, TSC = virtual nanoseconds at 1 GHz, so ticks ARE nanoseconds directly; maybe the icount leg divides by exactly 1 GHz and doesn't need the calibrated rate. Actually the 2026-08-24 section says "ns/iter computed from the guest's own calibrated TSC, the same arithmetic xtask's run_bench already does for every other leg" — "the same arithmetic ... for every other leg" suggests all legs compute ns from the calibrated TSC rate. Hmm, that suggests the icount legs also read the stored rate. But under icount the stored rate would be ~exactly the virtual rate (1 GHz), because both TSC and PIT are virtual. So the defect "affects the --real instrument only" because under icount the calibration is exact (no descheduling in virtual time). So "only the latter reads the calibrated rate" in the commit message is loosely worded but the substantive claim in the docs ("affects --real only") is defensible. I won't flag this; can't verify and it's plausibly fine.

**Issue BS: Double-check the "12x" heading once more.** +1153% → multiplier 12.53. "wrong by up to 12x" — hmm, "wrong by 12x" could be read as "wrong by a factor of 12" — actual 12.53. Or "wrong by 12x" = "+1200%"? +1153% ≠ +1200%. Either reading is off. "up to 12x" < 12.53x. So the heading understates the worst case it documents. Given the document's whole point is precision about this number, flag it.

**Issue BT: Milestone 526 title in tsc note: "milestone 526 (the x86 TSC calibration takes the smallest of several windows)"** — matches file name. OK.

**Issue BU: The milestone says "stops as soon as two of them agree" and "two windows agree to within one part in a thousand"** — agree with each other within 0.1%. Then "keeps the smallest". OK.

**Issue BV: The new benchmarks section: "and within it the `ns` column only. Ticks are raw `rdtsc` counts and are untouched."** OK.

**Issue BW: The correction blockquote: "See the 2026-09-21 section at the end of this file, and `kernel/src/arch/x86_64/timer.rs`'s `BUGS`."** OK.

**Issue BX: The milestone's "Follow-on" section lists items as "Recorded"/"Done" — conventions.** Fine.

**Issue BY: Milestone says "This lane widened that to 490 boots at three host loads and the shape held without a single exception: every boot high, none ever low."** OK.

**Issue BZ: "an event of essentially arbitrary size; for two windows to land within a thousandth of each other they must both have been left alone, because two independent deschedulings agreeing to three decimal places is not a thing that happens."** Rhetorical, fine.

**Issue CA: "A tighter tolerance (one part in 2000) moved the mean by 0.07 windows and the error distribution not at all"** OK.

**Issue CB: The milestone references "milestone 161 (the x86_64 kernel port...)" and "milestone 87" etc.** Can't verify numbers. Skip.

**Issue CC: qemu-runner script change described: "The runner now runs QEMU in the background, traps TERM and HUP, forwards them, and reaps."** Not in diff. Umbrella concern.

**Issue CD: The commit deletes the proposal file. The milestone says "The proposal this promotes ... is removed by this milestone".** Consistent.

**Issue CE: Anything in the deleted proposal that was NOT carried forward and should have been?** The proposal's "What it is worth" section mentions the D'-style marking discussion ("carrying 'unpromised' in the type... a marked number that is still 4x wrong is honest about the wrong thing"). The milestone doesn't mention the D' marking question at all. The proposal raised it as a consideration ("It also decides how much a D'-style marking ... is really worth"). The milestone drops it silently. Is that a concern? The proposal's two explicit "things to decide" are answered; the D' remark was a side observation. The milestone claims "its two open questions are both answered above" — the two open questions were the bullet list (confidence printing, APIC derivation). The D' paragraph wasn't one of the two. So dropping it is arguably fine, though the D' consideration is now unrecorded anywhere. Minor. Could mention: the proposal's D'-marking observation is lost with the deletion and not carried into the milestone. Low priority.

**Issue CF: The correction blockquote placement: it's in the 2026-08-24 section before "### The numbers". The diff context at line 2664 shows the preceding text "per iteration (both threads resume once each), so the delta divided by two is th..."** OK.

**Issue CG: The new section says "This lane took that further, fixed it, and this section is what the fix means for anything already published."** OK.

**Issue CH: "its rate against an independent clock moved by 37% between two workloads inside one boot"** — consistent with the note (37%). OK.

**Issue CI: The milestone says "200 boots ... each timing sixteen windows" and separately "This lane widened that to 490 boots at three host loads".** 490 = 200 (load 30) + 290 others. The load table lists ordinary (30 boots mentioned for the one-window worst +773% — "one window's worst of 30 boots was +773%"), load 22, load 30 (200). 30 + 200 = 230; load 22 boots = 260? Not stated. Fine, no contradiction visible.

**Issue CJ: "at ordinary session load one window's worst of 30 boots was +773%, and min-of-9 got to +0.01%"** — min-of-9 at ordinary load +0.01%, but the load-30 table shows min-of-9 worst +7.1%. Consistent (different conditions). OK.

**Issue CK: The milestone's statement "the suite pays 90 ms more than the one-window design did, against a 3m38s runtime: 0.04%, an order of magnitude below the suite's own run-to-run variance."** OK.

**Issue CL: tsc-under-tcg.md — the paragraph replacement keeps the sentence "and `arch::x86_64::timer`'s `BUGS` section now says it where a reader meets the feature."** The original ended with that sentence; the new text ends with "`arch::x86_64::timer`'s `BUGS` section says all of this where a reader meets the feature, including what it still does not bound." OK.

**Issue CM: Now, does the tsc note's edit create a contradiction with the note's own earlier text?** The note earlier says "**The boot's own calibration is the real defect, and it is far worse than anything drift could do.** Against a TSC that ticks at exactly 1000.000 MHz, twenty-two boots reported between 1001 MHz and 4330 MHz." — that's the historical record; the FIXED paragraph updates the worst to +1153%. The note's headline sections still describe the defect in present tense ("is the real defect") — that's fine as the finding is historical and the fix is marked. OK.

**Issue CN: The note's "Reproducing it" says "This note is its owner, and the calibration fix proposed above is what it exists to be measured against."** — Issue U. This is now stale: the fix is no longer "proposed above"; the proposal is deleted and the finding is FIXED. The sentence should have been updated to reference milestone 526. This is a genuine dangling-reference concern in the same file the commit edits. Good.

**Issue CO: Also in the note's "Reproducing it": nothing else stale.** "The calibration sweep needs no harness at all... boot any x86 kernel and read the clocks line, twenty times, once with the host loaded." — still fine post-fix (though post-fix the line shows best/worst of N windows; reading it twenty times would now show the fixed estimator. The instruction to reproduce the sweep would now demonstrate the fix, not the defect. Minor: the reproduction instructions now reproduce a fixed kernel, so "the calibration sweep" as described would no longer show the defect. But that's inherent to fixing; the note is a historical record. Eh, could mention as part of the staleness point? The note is marked FIXED; the repro section describes how the measurement was done. It's fine as history. Skip.)

**Issue CP: The commit message mentions "the note" — fine.**

**Issue CQ: Let me reconsider the debug-vs-release cancellation claim (Issue Z) — the strongest technical claim to verify.** The correction says: "every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly."

Debug vs release: different binaries, different boots (six debug boots, five release boots). Any ratio of a debug figure to a release figure uses two boots' stored rates. Calibration cancels only if both boots stored the same rate — they didn't ("it was a different number each time"). So the debug-versus-release ratio does NOT enjoy exact cancellation. The claim "the debug-versus-release argument ... [is a] ratio within a boot" is false on its face given the section's own "Six boots debug, five release". Unless the 2026-08-24 section's debug-vs-release argument is conducted in ticks (then it's unaffected, but not "because the calibration cancels in a ratio" — it's because ticks never touch the calibration). Either way the sentence is wrong or misattributed. Also "the 4.2x" — if the 4.2x is the debug/release tax, same problem; if it's a ratio of two rows within one boot (e.g., tss_iomap_switch vs yield_switch within one boot), then it's fine. We can't see the section. But the debug-versus-release argument cannot be a within-boot ratio. This is a solid concern: the correction over-claims that "all" conclusions survive; any conclusion comparing debug to release across boots is contaminated by two different unknown rates, and the true value is unrecoverable for exactly the reason the correction itself states.

Moreover, the commit message doubles down: "What survives untouched is every conclusion resting on a ratio between two rows of one boot, because the calibration cancels exactly in a ratio, and that is all of them." If the debug-vs-release argument is cross-boot, then "that is all of them" is wrong.

Additionally: even within one boot, "the calibration cancels exactly in a ratio" — true for ns figures from the same boot (same stored rate). OK.

But wait — there's a subtlety: is the debug-vs-release argument maybe within-boot in ticks? "the debug-versus-release argument and the 4.2x are ratios within a boot" — the sentence explicitly says "within a boot". Debug and release can't be within a boot. So it's wrong. Unless the section ran debug and release userspace benches within one kernel boot? The kernel build profile governs the kernel; the benches could be userspace programs compiled debug vs release run in the same boot? Hmm — "Six boots debug, five release" strongly implies kernel-profile boots. The 4.2x might be "debug kernel vs release kernel" tax. Can't be within a boot. I'm confident enough to flag with the reasoning, noting the section's own boot counts.

**Issue CR: The heading "wrong by up to 12x" — flag.**

**Issue CS: Missing blank line before the new `##` heading — flag as formatting.**

**Issue CT: The no-code concern — flag as the top item: milestone marked BUILT describing a diff (timer.rs, main.rs, new test file, tsc_probe.rs, qemu-runner script) that this commit does not contain; the PR is docs-only. If the code isn't already landed, the record is false; if it landed elsewhere, the milestone should say where. Also "What is in the diff" lists notes/benchmarks.md but not notes/tsc-under-tcg.md.**

Wait, actually — let me reconsider. The milestone's "What is in the diff" includes "notes/benchmarks.md: the 2026-08-24 x86 ns/iter table is marked in place, and a new dated section records what the defect reaches and what it does not." — that IS in this commit. And tsc-under-tcg.md changes are in this commit but not listed in "What is in the diff". So the milestone's diff list partially matches this commit (benchmarks.md) and partially doesn't (all the code). So the milestone seems to describe a larger lane diff of which this commit is a part? Or the code landed in a preceding commit and this commit is the docs sweep. Either way, as a reviewer of THIS PR: the PR adds a BUILT milestone whose described code changes are absent from the PR. If the code is already on main, fine — but then the milestone should say so or it's just fine. We genuinely can't tell. The safest framing: "The milestone is marked BUILT and enumerates kernel code changes and a new test file, but this commit contains none of them; if those changes are not already merged, this records as built something that isn't; if they are, the milestone's 'What is in the diff' conflates another diff with this one, and it also omits notes/tsc-under-tcg.md which is changed here."

Hmm, also the Follow-on item: "**Done.** The proposal this promotes ... is removed by this milestone" — that IS this commit. OK.

**Issue CU: One more: the milestone file says "Status: BUILT 2026-09-21" but also