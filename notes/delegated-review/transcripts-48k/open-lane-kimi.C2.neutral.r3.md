1. **notes/benchmarks.md (and the commit message)** — The correction's central safety claim is wrong for the section's headline conclusion. It says "every conclusion here that rests on a ratio between two rows of the same run, which is all of them: the debug-versus-release argument and the 4.2x are ratios within a boot, where the calibration cancels exactly." But the section ran six boots debug and five release; a debug:release ratio necessarily spans two boots, and the commit message itself states each of the eleven boots stored a *different* wrong rate. The calibration cancels only within one boot, so the 4.2x carries the ratio of two unknown errors and is as unrecoverable as the absolute figures. Declaring it "untouched" overstates what survives, and a reader will quote it on the strength of this correction.

2. **design/roadmap/526-the-x86-tsc-calibration-takes-the-smallest-of-several-windows.md** — The milestone is marked **BUILT** and its "What is in the diff" section enumerates changes to `kernel/src/arch/x86_64/timer.rs`, `kernel/src/main.rs`, a new `timer_calibration_tests.rs`, `tsc_probe.rs`, and `scripts/qemu-runner-x86_64.sh` — none of which are in this commit, which is Markdown-only. It also omits `notes/tsc-under-tcg.md`, which this commit *does* change. If the code landed in other commits, the record needs to name them; as merged, "BUILT" and the note's "FIXED" cannot be verified from what this change contains or references.

3. **design/roadmap/526-….md** — "By the mean, the defect was fixed at three windows and had never been very bad at one" mislabels the statistic. The table reports the *median* (+0.36% at one window, +0.00% from three up); the *mean* at one window is dominated by the tail — the +1153% and +884% outliers alone contribute ~10 points over 200 boots — so the mean would have made the defect look glaring, not invisible. The paragraph that justifies the whole estimator choice gets its own statistics backwards.

4. **notes/tsc-under-tcg.md** — The "Reproducing it" section still says "the calibration fix proposed above is what it exists to be measured against," but this commit deletes the proposal file and re-marks the finding FIXED, so "proposed above" now dangles. A reader following the note is pointed at a proposal that no longer exists.

5. **notes/tsc-under-tcg.md** — The BUGS entry for `qemu-bounded.sh` still says "This note is not where that gets fixed; see … `fix/qemu-bounded-stdin-under-dash`," while milestone 526 added in the same commit states the runner fix landed here ("runs QEMU in the background, traps TERM and HUP, forwards them, and reaps"). Two deliverables in one commit contradict each other about whether the killer bug is fixed and where the fix lives.

6. **design/roadmap/526-….md** — The Follow-on knowingly leaves `scripts/qemu-bounded.sh`'s own BUGS section describing the wrong failure mode ("left for whoever next touches that file") while explicitly calling that section "the place a reader would look first." That is the exact inverse of the "stated where a reader meets the feature" posture the milestone claims to follow, and it guarantees the next reader is misinformed at the first place they check.

7. **notes/benchmarks.md** — "The 2026-09-21 section above records that CoreMark 'reports correctness, not yet a score'" points at the wrong section: that sentence lives in the undated milestone-19e "Compute vs. OS primitives" section, and the only 2026-09-21 subsections above are about toolchain floors and the QEMU stamp. A broken cross-reference in the part of the correction whose job is to say precisely what is and isn't affected (and it is confusingly self-referential, since the new section is itself dated 2026-09-21).

8. **notes/benchmarks.md** — The heading "wrong by up to 12x" understates the body's own figure: +1153% is 12.53x the true rate (the milestone calls it a 125 ms window). An "up to" bound set below the measured maximum errs in the one direction a correction notice must not.

9. **notes/benchmarks.md** — "The rate each of these eleven boots stored was not written down, and it was a different number each time" asserts in the second clause what the first clause says is unrecoverable; the tree's own 22-boot data contains repeats (1001, 1001, 1002, 1002…), so "a different number each time" isn't even the observed shape. The commit message repeats the claim.

10. **design/roadmap/526-….md** — "The minimum is an unbiased one" contradicts the same paragraph's correct framing that every window is an upper bound and the minimum "converges on the truth from above." The minimum of upper bounds is still biased upward at any finite window count; the property being claimed is consistency, and the mislabel sits in the sentence that justifies the estimator.

11. **notes/benchmarks.md** — The before/after pair never states the host load it ran under, then cites "at this host's load roughly one boot in four was wrong by more than a per cent" — but the 56/200 figure belongs to the load-30 sweep. The milestone itself insists "the load matters and is stated rather than hidden"; this section does the hiding.

12. **notes/benchmarks.md** — The new `## 2026-09-21` heading is appended directly after the previous section's final line ("…route around it.") with no intervening blank line, unlike every other section boundary in the file; some Markdown parsers and linters (MD022) require the blank line for the heading to be recognized.