# The network stack: the appendices

[`notes/net.md`](../net.md) is the page to read. It is written so a reader can use the socket
contract without opening anything here, and its appendix table is the index. These files hold the
bring-up history, the gates' evidence, the prior art and the measurements behind it.

*Name: provisional, minted 2026-09-25 (UTC) by the lane that split the note, for the directory and
every stem in it. Naming is calef's; `script/names --unratified` lists each stem.*
