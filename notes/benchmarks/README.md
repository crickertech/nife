# Benchmarks: the appendices to notes/benchmarks.md

[`notes/benchmarks.md`](../benchmarks.md) carries the current numbers and is written so a reader can
act without opening anything here. These files carry the dated history behind them: the
measurements, the tables and the corrections. The main page's [Appendices](../benchmarks.md#appendices)
table is the index, one row per file with the dated entries it holds, so it is not repeated here.

*Name: ratified 2026-09-24 (calef, approving #1200). The directory `notes/benchmarks/` and 18 of the
21 stems were ratified as the splitting lane minted them. Three were renamed. Refused
`milestone-138-steps-1-and-3` and `milestone-138-steps-4-and-2` (named by a milestone's step numbers
rather than by what they hold, and "4 and 2" reads as a typo), now
`read-path-record-and-request-size.md` and `read-path-block-contract-and-metadata-cache.md`. Refused
`the-fixed-term` (it does not say what the file is, where its own H1 subtitle does), now
`five-blocks-per-request.md`. The three H1 titles were kept.*
