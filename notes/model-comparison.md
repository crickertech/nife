# Comparing models as this tree's lanes and maintainer

*Name: provisional (2026-09-24). Not put to calef.*

calef approved a pilot on 2026-09-24 comparing Opus 5 (`claude-opus-5`) and Opus 5.5
(`claude-opus-5-5`) as the models that run this tree's lanes and maintainer sessions. It is an
experiment, not a verdict. The protocol is committed before any run, so the analysis cannot be
chosen after the results are seen.

- [Directory index](model-comparison/README.md).
- [Protocol](model-comparison/protocol.md): the tasks, the briefs verbatim, the rubric, the blinding
  and the analysis, fixed before the first run.

## BUGS

- **Two runs per cell is a pilot.** It can show a gross difference and whether the harness works.
  It cannot rank two models that are close. The protocol's analysis section says what each result
  can and cannot carry.
