# Rust `std` on nife: the appendices

[`notes/std.md`](../std.md) is the page to read; it is written so a reader can act without opening
anything here, and its appendix table is the index. These files hold each binding's full mapping,
the caveats and their history, and the proof.

*Name: provisional, minted 2026-09-25 (UTC) by the lane that split the note, for the directory and
every stem in it. Naming is calef's; `script/names --unratified` lists each stem.*
