# VisionFive 2: the appendices to notes/visionfive2.md

[`notes/visionfive2.md`](../visionfive2.md) is the page to read: radon's board facts, the bench
runbook and every bench stop in summary. The parent is over its word budget, so a bench session
recorded in full since 2026-09-25 lives here, one file per session, and the parent carries a
pointer to each.

*Name: provisional, minted 2026-09-25 (UTC) by milestone 225 (run the soak on radon, argon and
xenon)'s radon lane, for the directory and every stem in it. Naming is calef's; `script/names
--unratified` lists each stem.*
